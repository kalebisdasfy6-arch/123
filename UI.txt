-- Miami LinoriaLib Custom Theme & UI Recreation
-- Recreated from the user-provided reference image.
-- Integrated Player, Main, Autofarm, Visuals, and Settings tabs features and logic from ddd.txt.
-- Accent Color: #ff8c00 (Orange accent)
-- Outline Color: #323232 (Dark grey outline)
-- Background Color: #141414 (Dark grey background)
-- Main Color: #1c1c1c (Dark grey panel background)
-- Font Color: #ffffff (White), with premium/success elements in green.

if game:GetService("UserInputService").TouchEnabled or game:GetService("UserInputService").GamepadEnabled then
    repo = 'https://raw.githubusercontent.com/LionTheGreatRealFrFr/MobileLinoriaLib/main/'
    print("Mobile Loaded")
else
    repo = 'https://raw.githubusercontent.com/violin-suzutsuki/LinoriaLib/main/'
    print("PC Loaded")
end

-- Swimmethode global setup
getgenv()["\83\119\105\109\77\101\116\104\111\100"] = false

task["\115\112\97\119\110"](function()
    while task["\119\97\105\116"]() do
        if getgenv()["\83\119\105\109\77\101\116\104\111\100"] then
            local a = game:GetService("\80\108\97\121\101\114\115")["\76\111\99\97\108\80\108\97\121\101\114"]
            if a and a["\67\104\97\114\97\99\116\101\114"] and a["\67\104\97\114\97\99\116\101\114"]:FindFirstChild("\72\117\109\97\110\111\105\100") then
                local b = a["\67\104\97\114\97\99\116\101\114"]["\72\117\109\97\110\111\105\100"]
                b:ChangeState(Enum["\72\117\109\97\110\111\105\100\83\116\97\116\101\84\121\112\101"]["\70\97\108\108\105\110\103\68\111\119\110"])
            end
        end
    end
end)

local Library = loadstring(game:HttpGet('https://raw.githubusercontent.com/BENZZY420/ui-/refs/heads/main/ui'))()
local ThemeManager = loadstring(game:HttpGet(repo .. 'addons/ThemeManager.lua'))()
local SaveManager = loadstring(game:HttpGet(repo .. 'addons/SaveManager.lua'))()

local Options = getgenv().Options or Options
local Toggles = getgenv().Toggles or Toggles

-- Recreating the Title exactly like the reference image:
-- "Miami | [v2.20] | License: Lifetime | Premium User"
local Window = Library:CreateWindow({
    Title = 'Miami  |  [v2.20]  |  License: <font color="rgb(0, 220, 0)">Lifetime</font>  |  <font color="rgb(255, 140, 0)">Premium User</font>',
    Center = true,
    AutoShow = true,
    TabPadding = 8,
    MenuFadeTime = 0.2
})

-- Initialize ESP Menu interface
local SenseESPInterface = loadstring(game:HttpGet("https://raw.githubusercontent.com/boshyxd/rayfield/refs/heads/main/SenseSource.luau"))()
if SenseESPInterface then
    Library:Notify("[Miami] ESP Menu\nLoading ESP...\nESP Menu library loaded successfully!\nESP Menu Initialized!", 2)
    SenseESPInterface.Load()
else
    Library:Notify("Error\nFailed to load ESP Menu.", 3)
end

-- Tab Configuration
local Tabs = {
    Player = Window:AddTab('Player'),
    Main = Window:AddTab('Main'),
    Autofarm = Window:AddTab('Autofarm'),
    Visuals = Window:AddTab('Visuals'),
    ["Safe/Dupe"] = Window:AddTab('Safe/Dupe'),
    Settings = Window:AddTab('Settings'),
}

------------------------------------------------------------
--  SHARED SERVICES / HELPERS FOR MERGED LOGIC
------------------------------------------------------------
local Players          = game:GetService("Players")
local ReplicatedStorage= game:GetService("ReplicatedStorage")
local RunService       = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local StarterGui       = game:GetService("StarterGui")
local LocalPlayer      = Players.LocalPlayer
local ProximityPromptService = game:GetService("ProximityPromptService")
local Lighting         = game:GetService("Lighting")
local CoreGui          = game:GetService("CoreGui")

local flags = {}

local function sendNotification(title, text)
    pcall(function() StarterGui:SetCore("SendNotification", { Title = title, Text = text, Duration = 5 }) end)
end

local function notifyToggle(name, enabled)
    sendNotification(name, name .. (enabled and " enabled" or " disabled"))
end

local function normalizeDropdownValue(v)
    return tostring(v)
end

------------------------------------------------------------
--  TELEPORT BYPASS LOGIC (Config:Teleport)
------------------------------------------------------------
getgenv().SwimMethod = getgenv().SwimMethod or false
local Config = {}
function Config:Teleport(targetCFrame)
    local lp = Players.LocalPlayer
    local char = lp.Character or lp.CharacterAdded:Wait()
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if not hum or not hrp or not targetCFrame then return false end

    getgenv().SwimMethod = true
    hum:ChangeState(Enum.HumanoidStateType.FallingDown)

    local n = 1
    repeat
        task.wait()
        n = n + 1
    until n >= 40 and not lp:GetAttribute("LastACPos")

    hrp.CFrame = targetCFrame
    task.wait()
    hum:ChangeState(Enum.HumanoidStateType.GettingUp)
    getgenv().SwimMethod = false
    return true
end

task.spawn(function()
    while task.wait() do
        if getgenv().SwimMethod then
            local lp = Players.LocalPlayer
            local char = lp.Character
            if char and char:FindFirstChildWhichIsA("Humanoid") then
                char:FindFirstChildWhichIsA("Humanoid"):ChangeState(Enum.HumanoidStateType.FallingDown)
            end
        end
    end
end)

------------------------------------------------------------
--  WEAPON MODS & EMOTES LOGIC (From ddd.txt)
------------------------------------------------------------
local WeaponMods = {
    InfiniteAmmo=false, InfiniteClips=false, Bullets80k=false,
    ModifyRecoilValue=false, ModifySpreadValue=false, DisableJamming=false,
    InstantReload=false, InstantEquip=false, ModifyFireRate=false,
    Automatic=false, DamageAmplified=false, SpoofAccuracy=false,
    InfiniteDamage=false,
    ReloadSpeed=0.2, EquipSpeed=0.2,
    FireRateSpeed=50, SpreadPercentage=50, RecoilPercentage=50,
}
local GunOriginalColors = {}
local GunChams = false
local RainbowGun = false
local GunChamsColor = Color3.fromRGB(0,200,0)

local function getEquippedGunSetting()
    local character = LocalPlayer.Character
    if not character then return nil, nil end
    local tool = character:FindFirstChildOfClass("Tool")
    if not tool or not tool:FindFirstChild("Setting") then return nil, nil end
    local ok, settings = pcall(require, tool.Setting)
    if ok and type(settings) == "table" then return tool, settings end
end

local function applyAllWeaponMods()
    local tool, settings = getEquippedGunSetting()
    if not settings then return end
    pcall(function()
        if WeaponMods.InfiniteAmmo then settings.Ammo=99999; settings.AmmoPerMag=99999; settings.LimitedAmmoEnabled=false end
        if WeaponMods.InfiniteClips then settings.Ammo=80000; settings.AmmoPerMag=80000; settings.LimitedAmmoEnabled=false end
        if WeaponMods.Bullets80k then
            settings.Ammo=80000; settings.AmmoPerMag=80000; settings.LimitedAmmoEnabled=false
            local gunLocal = tool:FindFirstChild("GunScript_Local")
            if gunLocal and type(getsenv) == "function" then
                pcall(function()
                    local env = getsenv(gunLocal)
                    if env and type(env.Reload) == "function" and type(debug.setupvalue) == "function" then
                        debug.setupvalue(env.Reload, 1, 80000); debug.setupvalue(env.Reload, 3, 80000)
                    end
                end)
            end
        end
        if WeaponMods.ModifyRecoilValue then settings.Recoil=0; settings.CameraRecoilingEnabled=false end
        if WeaponMods.ModifySpreadValue then settings.Spread=0; settings.SpreadX=0; settings.SpreadY=0; settings.Accuracy=1 end
        if WeaponMods.DisableJamming then settings.JamChance=0 end
        if WeaponMods.InstantReload then settings.ReloadTime = WeaponMods.ReloadSpeed or 0.1 end
        if WeaponMods.InstantEquip then settings.EquipTime = WeaponMods.EquipSpeed or 0.1; settings.EquippingTime = WeaponMods.EquipSpeed or 0.1 end
        if WeaponMods.ModifyFireRate then settings.FireRate = 0 end
        if WeaponMods.Automatic then settings.Auto = true end
        if (WeaponMods.DamageAmplified or WeaponMods.InfiniteDamage) then settings.BaseDamage = 1e9 end
        if WeaponMods.SpoofAccuracy then settings.Accuracy = 1 end
    end)
end

LocalPlayer.CharacterAdded:Connect(function(character)
    character.ChildAdded:Connect(function(c) if c:IsA("Tool") then task.defer(applyAllWeaponMods) end end)
end)
if LocalPlayer.Character then
    LocalPlayer.Character.ChildAdded:Connect(function(c) if c:IsA("Tool") then task.defer(applyAllWeaponMods) end end)
end
task.spawn(function() while task.wait(0.25) do pcall(applyAllWeaponMods) end end)

local function MianiForce80kBullets()
    local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
    if not tool then sendNotification("Gun Mods","Hold a gun first!") return end
    pcall(function()
        if tool:FindFirstChild("Setting") then
            local config = require(tool.Setting)
            if type(config) == "table" then
                config.Ammo=80000; config.AmmoPerMag=80000; config.LimitedAmmoEnabled=false
            end
        end
        local gunLocal = tool:FindFirstChild("GunScript_Local")
        if gunLocal and type(getsenv) == "function" then
            local env = getsenv(gunLocal)
            if env and type(env.Reload) == "function" and type(debug.setupvalue) == "function" then
                debug.setupvalue(env.Reload, 1, 80000); debug.setupvalue(env.Reload, 3, 80000)
            end
        end
    end)
    sendNotification("Gun Mods","Forced 80k bullets on current gun.")
end

local function MianiUpdateGunColor()
    if not GunChams then return end
    local char = LocalPlayer.Character
    local tool = char and char:FindFirstChildOfClass("Tool")
    if not tool then return end
    for _, v in pairs(tool:GetDescendants()) do
        if v:IsA("BasePart") then
            if not GunOriginalColors[v] then GunOriginalColors[v] = v.Color end
            if RainbowGun then v.Color = Color3.fromHSV((tick() % 5) / 5, 1, 1)
            else v.Color = GunChamsColor or Color3.fromRGB(0,200,0) end
            v.Material = Enum.Material.Neon
        end
    end
end
local function MianiRestoreGunColor()
    for part, color in pairs(GunOriginalColors) do
        if part and part.Parent then part.Color=color; part.Material=Enum.Material.Plastic end
    end
    GunOriginalColors = {}
end
RunService.RenderStepped:Connect(function() pcall(MianiUpdateGunColor) end)

-- EMOTES SETUP
local MianiEmotes = {
    ["Take The L"] = 98318652597666,
    ["Trip Out"]   = 135599476494116,
    ["Levitate"]   = 118574395769768,
}
local MianiEmoteNames = {}
for n in pairs(MianiEmotes) do table.insert(MianiEmoteNames, n) end
table.sort(MianiEmoteNames)
local MianiSelectedEmoteName = "Take The L"
local MianiCurrentEmoteTrack = nil
local MianiEmoteAnimCache = {}
local MianiEmoteSettings = { Loop = true }

local function MianiGetEmoteAnimator()
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local hum  = char and char:FindFirstChildOfClass("Humanoid")
    if not hum then return nil end
    local a = hum:FindFirstChildOfClass("Animator")
    if not a then a = Instance.new("Animator"); a.Parent = hum end
    return a
end
local function MianiStopAllTracks()
    local a = MianiGetEmoteAnimator()
    if a then for _, t in ipairs(a:GetPlayingAnimationTracks()) do pcall(function() t:Stop(0.1) end) end end
    MianiCurrentEmoteTrack = nil
end
local function MianiPlayEmoteById(id, name)
    id = tonumber(id); if not id then return end
    local animator = MianiGetEmoteAnimator(); if not animator then return end
    MianiStopAllTracks()
    local anim = MianiEmoteAnimCache[id]
    if not anim then
        anim = Instance.new("Animation"); anim.AnimationId = "rbxassetid://" .. tostring(id)
        MianiEmoteAnimCache[id] = anim
    end
    local ok, track = pcall(function()
        local t = animator:LoadAnimation(anim)
        t.Priority = Enum.AnimationPriority.Action
        t.Looped = MianiEmoteSettings.Loop
        t:Play(0.1)
        return t
    end)
    if ok and track then
        MianiCurrentEmoteTrack = track
        sendNotification("Emotes","Playing: "..tostring(name or id))
    end
end

------------------------------------------------------------
--  TARGETING & UTILITIES LOGIC & RUN SERVICE LOOPS (From ddd.txt)
------------------------------------------------------------
local TargetUtilities = {
    SelectedPlayer = nil,
    SpectatePlayer = false,
    BringingPlayer = false,
    BringingNearestPlayer = false,
    AutoRagdoll = false,
}
local TargetPlayerNames = {}

local function getTargetPlayerList()
    local list = {}
    for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then table.insert(list, p.Name) end end
    table.sort(list); return list
end

local function getSelectedTargetPlayer()
    local sel = normalizeDropdownValue(TargetUtilities.SelectedPlayer or flags["MainTargetPlayer"])
    if typeof(sel) == "Instance" and sel:IsA("Player") then return sel end
    if type(sel) == "string" and sel ~= "" then return Players:FindFirstChild(sel) end
end

local function isValidTargetPlayer(p)
    if not p or p == LocalPlayer then return false end
    local c = p.Character; if not c then return false end
    local h = c:FindFirstChildOfClass("Humanoid")
    local r = c:FindFirstChild("HumanoidRootPart")
    return h and h.Health > 0 and r ~= nil
end

local function getNearestTargetPlayer()
    local lr = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not lr then return nil end
    local n, nd = nil, math.huge
    for _, p in ipairs(Players:GetPlayers()) do
        if isValidTargetPlayer(p) then
            local d = (p.Character.HumanoidRootPart.Position - lr.Position).Magnitude
            if d < nd then n, nd = p, d end
        end
    end
    return n
end

local function targetGunRemote(targetName, hitPart, damage)
    hitPart = hitPart or "Head"; damage = damage or math.huge
    local target = Players:FindFirstChild(tostring(targetName))
    if not target or not target.Character or not target.Character:FindFirstChild(hitPart) then return end
    if not target.Character:FindFirstChildOfClass("Humanoid") then return end
    local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
    if not tool then sendNotification("Target","Hold a gun/tool first."); return end
    pcall(function() if tool:FindFirstChild("Setting") then require(tool.Setting).Range = 10000 end end)
    pcall(function()
        if ReplicatedStorage:FindFirstChild("InflictTarget") then
            ReplicatedStorage.InflictTarget:FireServer(
                tool, LocalPlayer, target.Character.Humanoid,
                target.Character[hitPart], damage,
                {0,0,false,false}, {false,5,3}, target.Character[hitPart],
                {false,{1930359546},1,1.5,1}, target.Character[hitPart].Position,
                Vector3.new(0,0,-1), true
            )
        end
    end)
end

-- Camera spectate loop
local targetSpectateReset = true
RunService:BindToRenderStep("MiamiTargetSpectate", Enum.RenderPriority.Camera.Value, function()
    local cam = workspace.CurrentCamera; if not cam then return end
    if TargetUtilities.SpectatePlayer then
        targetSpectateReset = false
        local t = getSelectedTargetPlayer()
        local s = t and t.Character and t.Character:FindFirstChildOfClass("Humanoid")
        if not s and LocalPlayer.Character then s = LocalPlayer.Character:FindFirstChildOfClass("Humanoid") end
        if s then cam.CameraSubject = s end
    elseif not targetSpectateReset and LocalPlayer.Character then
        targetSpectateReset = true
        local h = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if h then cam.CameraSubject = h end
    end
end)

-- Bringing loops
task.spawn(function()
    while task.wait() do
        if TargetUtilities.BringingPlayer then
            local t = getSelectedTargetPlayer()
            if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                t.Character.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame + Vector3.new(2,0,0)
            end
        end
        if TargetUtilities.BringingNearestPlayer then
            local t = getNearestTargetPlayer()
            if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                t.Character.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame + Vector3.new(2,0,0)
            end
        end
    end
end)

-- Auto ragdoll loops
task.spawn(function()
    while task.wait(2) do
        if TargetUtilities.AutoRagdoll then
            local t = getSelectedTargetPlayer()
            if t and isValidTargetPlayer(t) and t.Character.Humanoid:GetState() ~= Enum.HumanoidStateType.Physics then
                targetGunRemote(t.Name, "RightUpperLeg", 0.01)
            end
        end
    end
end)

local function applyOutfit(name, instructions)
    pcall(function() ReplicatedStorage.ClothShopRemote:FireServer("Reset Data") end)
    task.wait(0.2)
    for _, inst in ipairs(instructions) do
        local action, cat, item = inst[1], inst[2], inst[3]
        pcall(function() ReplicatedStorage.ClothShopRemote:FireServer(action, cat, item) end)
        task.wait(0.1)
    end
    sendNotification("Outfit", "Applied " .. name)
end

------------------------------------------------------------
--  FOOTBALL OVERLAY & MONEY SYSTEMS (From ddd.txt)
------------------------------------------------------------
local function StartMiamiFootballGameOverlay(opponentOverride)
    local TweenService = game:GetService("TweenService")

    local pg = LocalPlayer and LocalPlayer:FindFirstChildOfClass("PlayerGui")
    if not pg then pcall(function() pg = LocalPlayer:WaitForChild("PlayerGui", 5) end) end

    local function pick(list)
        return list[math.random(1, #list)]
    end

    local function clampScoreChance(scoreA, scoreB, quarter)
        local total = scoreA + scoreB
        if quarter <= 4 then
            if total >= 70 then return 4 end
            if total >= 56 then return 9 end
            if total >= 42 then return 18 end
            return 34
        end
        return 22
    end

    local MiamiRoster = {
        "Carter Brown", "Jayden Rivers", "Malik Thompson", "Darius King",
        "Isaiah Horton", "Andre Coleman", "Tyler Ward", "Jalen Brooks",
        "Marcus Reed", "Chris Daniels", "Rico Bennett", "Cam Johnson",
        "Devin Hayes", "Trey Williams", "KJ Franklin", "Lamar Price",
    }

    local CollegeTeams = {
        {Name="NOTRE DAME", Short="ND", Color=Color3.fromRGB(184,134,70), Players={"CJ Carr","Jeremiyah Love","Jordan Faison","Eli Raridon","Jaylen Sneed","Aneyas Williams"}},
        {Name="FLORIDA STATE", Short="FSU", Color=Color3.fromRGB(120,47,64), Players={"Luke Kromenhoek","Hykeem Williams","Roydell Williams","Lawrance Toafili","Ja'Khi Douglas","Cam Riley"}},
        {Name="FLORIDA", Short="UF", Color=Color3.fromRGB(0,65,130), Players={"DJ Lagway","Tre Wilson","Montrell Johnson","Eugene Wilson","Aidan Mizell","Jadan Baugh"}},
        {Name="GEORGIA", Short="UGA", Color=Color3.fromRGB(186,12,47), Players={"Gunner Stockton","Dillon Bell","Oscar Delp","Cash Jones","London Humphreys","CJ Allen"}},
        {Name="ALABAMA", Short="BAMA", Color=Color3.fromRGB(158,27,50), Players={"Ty Simpson","Ryan Williams","Jam Miller","Germie Bernard","Justice Haynes","Kendrick Law"}},
        {Name="LSU", Short="LSU", Color=Color3.fromRGB(70,29,124), Players={"Garrett Nussmeier","Chris Hilton","Kaleb Jackson","Kyren Lacy","Aaron Anderson","Caden Durham"}},
        {Name="CLEMSON", Short="CLEM", Color=Color3.fromRGB(245,102,0), Players={"Cade Klubnik","Antonio Williams","Phil Mafah","Tyler Brown","Adam Randall","Wes Goodwin"}},
        {Name="OHIO STATE", Short="OSU", Color=Color3.fromRGB(187,0,0), Players={"Julian Sayin","Jeremiah Smith","Carnell Tate","TreVeyon Henderson","Brandon Inniss","CJ Hicks"}},
        {Name="MICHIGAN", Short="MICH", Color=Color3.fromRGB(0,39,76), Players={"Alex Orji","Donovan Edwards","Colston Loveland","Semaj Morgan","Kalel Mullings","Will Johnson"}},
        {Name="OREGON", Short="ORE", Color=Color3.fromRGB(18,71,52), Players={"Dante Moore","Evan Stewart","Jordan James","Tez Johnson","Noah Whittington","Matayo Uiagalelei"}},
        {Name="TEXAS", Short="TEX", Color=Color3.fromRGB(191,87,0), Players={"Arch Manning","CJ Baxter","Ryan Wingo","Johntay Cook","Jaydon Blue","Anthony Hill"}},
        {Name="OKLAHOMA", Short="OU", Color=Color3.fromRGB(132,22,23), Players={"Jackson Arnold","Gavin Sawchuk","Nic Anderson","Jalil Farooq","Deion Burks","Billy Bowman"}},
        {Name="USC", Short="USC", Color=Color3.fromRGB(153,27,30), Players={"Miller Moss","Zachariah Branch","Ja'Kobi Lane","Quinten Joyner","Duce Robinson","Makai Lemon"}},
        {Name="PENN STATE", Short="PSU", Color=Color3.fromRGB(30,64,124), Players={"Drew Allar","Nick Singleton","Kaytron Allen","Trey Wallace","Tyler Warren","Abdul Carter"}},
        {Name="TENNESSEE", Short="TENN", Color=Color3.fromRGB(255,130,0), Players={"Nico Iamaleava","Dylan Sampson","Squirrel White","Bru McCoy","Mike Matthews","James Pearce"}},
        {Name="TEXAS A&M", Short="A&M", Color=Color3.fromRGB(80,0,0), Players={"Marcel Reed","Le'Veon Moss","Noah Thomas","Rueben Owens","Conner Weigman","Taurean York"}},
        {Name="WASHINGTON", Short="WASH", Color=Color3.fromRGB(51,0,111), Players={"Demon Williams","Giles Jackson","Denzel Boston","Jonah Coleman","Will Rogers","Carson Bruener"}},
        {Name="UTAH", Short="UTAH", Color=Color3.fromRGB(204,0,0), Players={"Cam Rising","Micah Bernard","Money Parks","Dorian Singer","Brant Kuithe","Lander Barton"}},
        {Name="AUBURN", Short="AUB", Color=Color3.fromRGB(12,35,64), Players={"Payton Thorne","Cam Coleman","Jarquez Hunter","KeAndre Lambert-Smith","Rivaldo Fairweather","Keldric Faulk"}},
        {Name="WISCONSIN", Short="WISC", Color=Color3.fromRGB(197,5,12), Players={"Tyler Van Dyke","Chez Mellusi","Will Pauling","Bryson Green","Tawee Walker","Hunter Wohler"}},
        {Name="IOWA", Short="IOWA", Color=Color3.fromRGB(255,205,0), Players={"Cade McNamara","Kaleb Johnson","Luke Lachey","Seth Anderson","Leshon Williams","Jay Higgins"}},
        {Name="NORTH CAROLINA", Short="UNC", Color=Color3.fromRGB(123,175,212), Players={"Max Johnson","Omarion Hampton","JJ Jones","Nate McCollum","Kobe Paysour","Power Echols"}},
        {Name="LOUISVILLE", Short="LOU", Color=Color3.fromRGB(173,0,0), Players={"Tyler Shough","Isaac Brown","Caullin Lacy","Chris Bell","Ahmari Huggins-Bruce","TJ Quinn"}},
        {Name="VIRGINIA TECH", Short="VT", Color=Color3.fromRGB(99,0,49), Players={"Kyron Drones","Bhayshul Tuten","Jaylin Lane","Da'Quan Felton","Ali Jennings","Antwaun Powell-Ryland"}},
        {Name="BAYLOR", Short="BAY", Color=Color3.fromRGB(0,82,48), Players={"Dequan Finn","Richard Reese","Monaray Baldwin","Ketron Jackson","Bryson Washington","Matt Jones"}},
        {Name="TCU", Short="TCU", Color=Color3.fromRGB(77,25,121), Players={"Josh Hoover","Savion Williams","JP Richardson","Cam Cook","Eric McAlister","Namdi Obiazor"}},
        {Name="NEBRASKA", Short="NEB", Color=Color3.fromRGB(227,25,55), Players={"Dylan Raiola","Dante Dowdell","Malachi Coleman","Jahmal Banks","Thomas Fidone","Isaac Gifford"}},
        {Name="COLORADO", Short="CU", Color=Color3.fromRGB(207,184,124), Players={"Shedeur Sanders","Travis Hunter","Jimmy Horn","Dylan Edwards","LaJohntay Wester","Shilo Sanders"}},
        {Name="STANFORD", Short="STAN", Color=Color3.fromRGB(140,21,21), Players={"Ashton Daniels","Elic Ayomanor","Tiger Bachmeier","Sedrick Irvin","Justin Lamson","David Bailey"}},
        {Name="UCLA", Short="UCLA", Color=Color3.fromRGB(45,104,196), Players={"Ethan Garbers","TJ Harden","J.Michael Sturdivant","Logan Loya","Moliki Matavao","Kain Medrano"}},
    }

    opponentOverride = opponentOverride or getgenv().MiamiFootballOpponent
    local opponent = CollegeTeams[math.random(1, #CollegeTeams)]
    if opponentOverride then
        for _, t in ipairs(CollegeTeams) do
            if t.Name == opponentOverride or t.Short == opponentOverride then opponent = t; break end
        end
    end

    local TeamImages = getgenv().MiamiFootballTeamImages or {}
    local miamiImages = TeamImages.MIAMI or TeamImages["MIAMI"] or {}
    local oppImages = TeamImages[opponent.Name] or TeamImages[opponent.Short] or {}
    opponent.Logo = opponent.Logo or oppImages.Logo
    opponent.Photo = opponent.Photo or oppImages.Photo
    local MiamiLogoAsset = getgenv().MiamiLogoAsset or miamiImages.Logo
    local MiamiPhotoAsset = getgenv().MiamiTeamPhotoAsset or miamiImages.Photo

    local gui = Instance.new("ScreenGui")
    gui.Name = "MiamiFootballMoneyGame"
    gui.ResetOnSpawn = false
    gui.IgnoreGuiInset = true
    gui.DisplayOrder = 99998
    local okParent = pcall(function() gui.Parent = CoreGui end)
    if not okParent and pg then gui.Parent = pg end
    if not gui.Parent then return function() end end

    local frame = Instance.new("Frame")
    frame.Name = "Scorebug"
    frame.AnchorPoint = Vector2.new(0.5, 0)
    frame.Position = UDim2.new(0.5, 0, 0, -190)
    frame.Size = UDim2.fromOffset(470, 258)
    frame.BackgroundColor3 = Color3.fromRGB(8, 14, 10)
    frame.BackgroundTransparency = 0.05
    frame.BorderSizePixel = 0
    frame.ClipsDescendants = true
    frame.Parent = gui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 14)
    corner.Parent = frame

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(61, 255, 138)
    stroke.Thickness = 1.8
    stroke.Transparency = 0.05
    stroke.Parent = frame

    local strokeGrad = Instance.new("UIGradient")
    strokeGrad.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(40,170,100)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(120,255,160)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(40,170,100)),
    }
    strokeGrad.Parent = stroke
    task.spawn(function()
        local r = 0
        while strokeGrad.Parent do
            r = (r + 3) % 360
            strokeGrad.Rotation = r
            task.wait(0.04)
        end
    end)

    local bg = Instance.new("ImageLabel")
    bg.Name = "FootballBg"
    bg.BackgroundTransparency = 1
    bg.Size = UDim2.fromScale(1, 1)
    bg.Image = "rbxassetid://100121801281958"
    bg.ScaleType = Enum.ScaleType.Crop
    bg.ImageTransparency = 0.36
    bg.ZIndex = 0
    bg.Parent = frame

    local shade = Instance.new("Frame")
    shade.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    shade.BackgroundTransparency = 0.16
    shade.BorderSizePixel = 0
    shade.Size = UDim2.fromScale(1, 1)
    shade.ZIndex = 1
    shade.Parent = frame

    local topBar = Instance.new("Frame")
    topBar.BackgroundColor3 = Color3.fromRGB(11, 24, 14)
    topBar.BackgroundTransparency = 0.04
    topBar.BorderSizePixel = 0
    topBar.Size = UDim2.new(1, 0, 0, 32)
    topBar.ZIndex = 2
    topBar.Parent = frame

    local title = Instance.new("TextLabel")
    title.BackgroundTransparency = 1
    title.Position = UDim2.fromOffset(12, 0)
    title.Size = UDim2.new(1, -150, 1, 0)
    title.Font = Enum.Font.GothamBlack
    title.TextSize = 13
    title.RichText = true
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.TextStrokeTransparency = 0.5
    title.ZIndex = 3
    title.Text = '<font color="#3dff8a">MIAMI</font> MONEY BOWL  •  COLLEGE SHOWDOWN'
    title.Parent = topBar

    local quarter = Instance.new("TextLabel")
    quarter.BackgroundTransparency = 1
    quarter.Position = UDim2.new(1, -132, 0, 0)
    quarter.Size = UDim2.fromOffset(120, 32)
    quarter.Font = Enum.Font.GothamBlack
    quarter.TextSize = 13
    quarter.TextXAlignment = Enum.TextXAlignment.Right
    quarter.TextColor3 = Color3.fromRGB(255, 255, 255)
    quarter.TextStrokeTransparency = 0.45
    quarter.ZIndex = 3
    quarter.Text = "Q1 15:00"
    quarter.Parent = topBar

    local miamiBox = Instance.new("Frame")
    miamiBox.Position = UDim2.fromOffset(14, 44)
    miamiBox.Size = UDim2.fromOffset(213, 54)
    miamiBox.BackgroundColor3 = Color3.fromRGB(0, 90, 55)
    miamiBox.BackgroundTransparency = 0.06
    miamiBox.BorderSizePixel = 0
    miamiBox.ZIndex = 2
    miamiBox.Parent = frame
    local mbc = Instance.new("UICorner"); mbc.CornerRadius = UDim.new(0, 9); mbc.Parent = miamiBox

    local oppBox = Instance.new("Frame")
    oppBox.Position = UDim2.new(1, -227, 0, 44)
    oppBox.Size = UDim2.fromOffset(213, 54)
    oppBox.BackgroundColor3 = opponent.Color
    oppBox.BackgroundTransparency = 0.06
    oppBox.BorderSizePixel = 0
    oppBox.ZIndex = 2
    oppBox.Parent = frame
    local obc = Instance.new("UICorner"); obc.CornerRadius = UDim.new(0, 9); obc.Parent = oppBox

    local function makeTeamPhotoBackground(parent, asset, shortName, color, side)
        local holder = Instance.new("Frame")
        holder.Name = "TeamPhotoBackdrop"
        holder.BackgroundTransparency = 1
        holder.ClipsDescendants = true
        holder.Size = UDim2.fromScale(1, 1)
        holder.ZIndex = 2
        holder.Parent = parent

        if asset and tostring(asset) ~= "" then
            local img = Instance.new("ImageLabel")
            img.BackgroundTransparency = 1
            img.Size = UDim2.fromScale(1.15, 1.15)
            img.Position = UDim2.fromScale(-0.075, -0.075)
            img.Image = tostring(asset)
            img.ScaleType = Enum.ScaleType.Crop
            img.ImageTransparency = 0.38
            img.ZIndex = 2
            img.Parent = holder
            local fade = Instance.new("UIGradient")
            fade.Rotation = (side == "Right") and 180 or 0
            fade.Transparency = NumberSequence.new{
                NumberSequenceKeypoint.new(0.00, 0.15),
                NumberSequenceKeypoint.new(0.55, 0.55),
                NumberSequenceKeypoint.new(1.00, 0.88),
            }
            fade.Parent = img
        else
            local poster = Instance.new("TextLabel")
            poster.BackgroundTransparency = 1
            poster.AnchorPoint = Vector2.new(0.5, 0.5)
            poster.Position = UDim2.fromScale((side == "Right") and 0.72 or 0.28, 0.55)
            poster.Size = UDim2.fromOffset(120, 70)
            poster.Font = Enum.Font.GothamBlack
            poster.Text = tostring(shortName)
            poster.TextSize = (#tostring(shortName) > 3) and 24 or 34
            poster.TextColor3 = Color3.fromRGB(255,255,255)
            poster.TextTransparency = 0.68
            poster.TextStrokeTransparency = 0.85
            poster.Rotation = (side == "Right") and 10 or -10
            poster.ZIndex = 2
            poster.Parent = holder
            local stripe = Instance.new("Frame")
            stripe.BackgroundColor3 = Color3.fromRGB(255,255,255)
            stripe.BackgroundTransparency = 0.82
            stripe.BorderSizePixel = 0
            stripe.AnchorPoint = Vector2.new(0.5, 0.5)
            stripe.Position = UDim2.fromScale(0.5, 0.5)
            stripe.Size = UDim2.new(1.25, 0, 0, 12)
            stripe.Rotation = (side == "Right") and -18 or 18
            stripe.ZIndex = 2
            stripe.Parent = holder
        end
        local tint = Instance.new("Frame")
        tint.BackgroundColor3 = color
        tint.BackgroundTransparency = 0.72
        tint.BorderSizePixel = 0
        tint.Size = UDim2.fromScale(1, 1)
        tint.ZIndex = 2
        tint.Parent = holder
        return holder
    end

    local function makeLogoBadge(parent, logoText, color, side, logoAsset)
        local badge = Instance.new("Frame")
        badge.Name = "TeamLogo"
        badge.Size = UDim2.fromOffset(34, 34)
        badge.Position = (side == "Right") and UDim2.new(1, -42, 0, 10) or UDim2.fromOffset(8, 10)
        badge.BackgroundColor3 = color
        badge.BackgroundTransparency = 0
        badge.BorderSizePixel = 0
        badge.ZIndex = 4
        badge.Parent = parent

        local bc = Instance.new("UICorner")
        bc.CornerRadius = UDim.new(1, 0)
        bc.Parent = badge

        local bs = Instance.new("UIStroke")
        bs.Color = Color3.fromRGB(255, 255, 255)
        bs.Thickness = 1.5
        bs.Transparency = 0.15
        bs.Parent = badge

        local grad = Instance.new("UIGradient")
        grad.Rotation = 35
        grad.Color = ColorSequence.new{
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255,255,255)),
            ColorSequenceKeypoint.new(0.18, color),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(0,0,0)),
        }
        grad.Transparency = NumberSequence.new{
            NumberSequenceKeypoint.new(0, 0.78),
            NumberSequenceKeypoint.new(1, 0.15),
        }
        grad.Parent = badge

        if logoAsset and tostring(logoAsset) ~= "" then
            local img = Instance.new("ImageLabel")
            img.BackgroundTransparency = 1
            img.Size = UDim2.fromScale(0.86, 0.86)
            img.Position = UDim2.fromScale(0.07, 0.07)
            img.Image = tostring(logoAsset)
            img.ScaleType = Enum.ScaleType.Fit
            img.ZIndex = 5
            img.Parent = badge
        else
            local txt = Instance.new("TextLabel")
            txt.BackgroundTransparency = 1
            txt.Size = UDim2.fromScale(1, 1)
            txt.Font = Enum.Font.GothamBlack
            txt.TextSize = (#tostring(logoText) > 2) and 11 or 16
            txt.Text = tostring(logoText)
            txt.TextColor3 = Color3.fromRGB(255, 255, 255)
            txt.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
            txt.TextStrokeTransparency = 0.2
            txt.ZIndex = 5
            txt.Parent = badge
        end

        return badge
    end

    local miamiPhotoBackdrop = makeTeamPhotoBackground(miamiBox, MiamiPhotoAsset, "U", Color3.fromRGB(244, 115, 33), "Left")
    local oppPhotoBackdrop = makeTeamPhotoBackground(oppBox, opponent.Photo, opponent.Short, opponent.Color, "Right")

    local miamiLogo = makeLogoBadge(miamiBox, "U", Color3.fromRGB(244, 115, 33), "Left", MiamiLogoAsset)
    local oppLogo = makeLogoBadge(oppBox, opponent.Short, opponent.Color, "Right", opponent.Logo)

    local function makeTeamLabel(parent, txt, side)
        local l = Instance.new("TextLabel")
        l.BackgroundTransparency = 1
        if side == Enum.TextXAlignment.Right then
            l.Position = UDim2.fromOffset(8, 4)
            l.Size = UDim2.new(1, -56, 0, 18)
        else
            l.Position = UDim2.fromOffset(48, 4)
            l.Size = UDim2.new(1, -56, 0, 18)
        end
        l.Font = Enum.Font.GothamBlack
        l.TextSize = 12
        l.TextXAlignment = side or Enum.TextXAlignment.Left
        l.TextColor3 = Color3.fromRGB(255, 255, 255)
        l.TextStrokeTransparency = 0.25
        l.ZIndex = 3
        l.Text = txt
        l.Parent = parent
        return l
    end

    makeTeamLabel(miamiBox, "MIAMI", Enum.TextXAlignment.Left)
    makeTeamLabel(oppBox, opponent.Name, Enum.TextXAlignment.Right)

    local function makeScoreLabel(parent, side)
        local l = Instance.new("TextLabel")
        l.BackgroundTransparency = 1
        if side == Enum.TextXAlignment.Right then
            l.Position = UDim2.fromOffset(8, 20)
            l.Size = UDim2.new(1, -56, 0, 30)
        else
            l.Position = UDim2.fromOffset(48, 20)
            l.Size = UDim2.new(1, -56, 0, 30)
        end
        l.Font = Enum.Font.GothamBlack
        l.TextSize = 29
        l.TextXAlignment = side
        l.TextColor3 = Color3.fromRGB(255, 255, 255)
        l.TextStrokeTransparency = 0.1
        l.ZIndex = 3
        l.Text = "0"
        l.Parent = parent
        return l
    end

    local miamiScoreLabel = makeScoreLabel(miamiBox, Enum.TextXAlignment.Left)
    local oppScoreLabel = makeScoreLabel(oppBox, Enum.TextXAlignment.Right)

    local play = Instance.new("TextLabel")
    play.BackgroundTransparency = 1
    play.Position = UDim2.fromOffset(14, 105)
    play.Size = UDim2.new(1, -28, 0, 35)
    play.Font = Enum.Font.GothamBold
    play.TextSize = 14
    play.TextWrapped = true
    play.RichText = true
    play.TextColor3 = Color3.fromRGB(245, 245, 245)
    play.TextStrokeTransparency = 0.34
    play.ZIndex = 2
    play.Text = '<font color="#3dff8a">KICKOFF!</font> Money generation drive is starting...'
    play.Parent = frame

    local scorer = Instance.new("TextLabel")
    scorer.BackgroundTransparency = 1
    scorer.Position = UDim2.fromOffset(14, 135)
    scorer.Size = UDim2.new(1, -28, 0, 20)
    scorer.Font = Enum.Font.GothamSemibold
    scorer.TextSize = 12
    scorer.RichText = true
    scorer.TextColor3 = Color3.fromRGB(210, 220, 210)
    scorer.TextStrokeTransparency = 0.55
    scorer.ZIndex = 2
    scorer.Text = "Scoring player will show here."
    scorer.Parent = frame

    local driveLabel = Instance.new("TextLabel")
    driveLabel.BackgroundTransparency = 1
    driveLabel.Position = UDim2.fromOffset(14, 154)
    driveLabel.Size = UDim2.new(1, -150, 0, 18)
    driveLabel.Font = Enum.Font.GothamBlack
    driveLabel.TextSize = 12
    driveLabel.RichText = true
    driveLabel.TextXAlignment = Enum.TextXAlignment.Left
    driveLabel.TextColor3 = Color3.fromRGB(235, 245, 235)
    driveLabel.TextStrokeTransparency = 0.45
    driveLabel.ZIndex = 2
    driveLabel.Text = "1ST & 10  •  BALL ON MIA 25"
    driveLabel.Parent = frame

    local redZoneLabel = Instance.new("TextLabel")
    redZoneLabel.BackgroundTransparency = 1
    redZoneLabel.Position = UDim2.new(1, -132, 0, 154)
    redZoneLabel.Size = UDim2.fromOffset(118, 18)
    redZoneLabel.Font = Enum.Font.GothamBlack
    redZoneLabel.TextSize = 12
    redZoneLabel.TextXAlignment = Enum.TextXAlignment.Right
    redZoneLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
    redZoneLabel.TextStrokeTransparency = 0.25
    redZoneLabel.ZIndex = 2
    redZoneLabel.Text = ""
    redZoneLabel.Parent = frame

    local fieldFrame = Instance.new("Frame")
    fieldFrame.Position = UDim2.fromOffset(24, 178)
    fieldFrame.Size = UDim2.new(1, -48, 0, 25)
    fieldFrame.BackgroundColor3 = Color3.fromRGB(25, 92, 42)
    fieldFrame.BackgroundTransparency = 0.08
    fieldFrame.BorderSizePixel = 0
    fieldFrame.ZIndex = 2
    fieldFrame.Parent = frame
    local ffc = Instance.new("UICorner"); ffc.CornerRadius = UDim.new(0, 5); ffc.Parent = fieldFrame
    local ffs = Instance.new("UIStroke"); ffs.Color = Color3.fromRGB(210,255,210); ffs.Transparency = 0.55; ffs.Thickness = 1; ffs.Parent = fieldFrame
    for i = 1, 9 do
        local yard = Instance.new("Frame")
        yard.BackgroundColor3 = Color3.fromRGB(235, 255, 235)
        yard.BackgroundTransparency = 0.45
        yard.BorderSizePixel = 0
        yard.Position = UDim2.new(i/10, 0, 0, 3)
        yard.Size = UDim2.new(0, 1, 1, -6)
        yard.ZIndex = 3
        yard.Parent = fieldFrame
    end

    local ballMarker = Instance.new("TextLabel")
    ballMarker.BackgroundTransparency = 1
    ballMarker.AnchorPoint = Vector2.new(0.5, 0.5)
    ballMarker.Position = UDim2.new(0.25, 0, 0.5, 0)
    ballMarker.Size = UDim2.fromOffset(26, 22)
    ballMarker.Font = Enum.Font.GothamBlack
    ballMarker.TextSize = 17
    ballMarker.Text = "🏈"
    ballMarker.TextStrokeTransparency = 0.35
    ballMarker.ZIndex = 5
    ballMarker.Parent = fieldFrame

    local momentumBack = Instance.new("Frame")
    momentumBack.Position = UDim2.fromOffset(45, 209)
    momentumBack.Size = UDim2.new(1, -90, 0, 8)
    momentumBack.BackgroundColor3 = opponent.Color
    momentumBack.BackgroundTransparency = 0.1
    momentumBack.BorderSizePixel = 0
    momentumBack.ZIndex = 2
    momentumBack.Parent = frame
    local mbk = Instance.new("UICorner"); mbk.CornerRadius = UDim.new(1, 0); mbk.Parent = momentumBack

    local momentumFill = Instance.new("Frame")
    momentumFill.Size = UDim2.new(0.5, 0, 1, 0)
    momentumFill.BackgroundColor3 = Color3.fromRGB(61, 255, 138)
    momentumFill.BorderSizePixel = 0
    momentumFill.ZIndex = 3
    momentumFill.Parent = momentumBack
    local mfc = Instance.new("UICorner"); mfc.CornerRadius = UDim.new(1, 0); mfc.Parent = momentumFill

    local lowerThird = Instance.new("Frame")
    lowerThird.Name = "LowerThird"
    lowerThird.Position = UDim2.new(0, -470, 0, 100)
    lowerThird.Size = UDim2.new(1, 0, 0, 55)
    lowerThird.BackgroundColor3 = Color3.fromRGB(6, 12, 8)
    lowerThird.BackgroundTransparency = 0.04
    lowerThird.BorderSizePixel = 0
    lowerThird.Visible = false
    lowerThird.ZIndex = 6
    lowerThird.Parent = frame
    local lts = Instance.new("UIStroke"); lts.Color = Color3.fromRGB(61,255,138); lts.Transparency = 0.25; lts.Parent = lowerThird
    local ltTitle = Instance.new("TextLabel")
    ltTitle.BackgroundTransparency = 1
    ltTitle.Position = UDim2.fromOffset(12, 4)
    ltTitle.Size = UDim2.new(1, -24, 0, 23)
    ltTitle.Font = Enum.Font.GothamBlack
    ltTitle.TextSize = 18
    ltTitle.RichText = true
    ltTitle.TextXAlignment = Enum.TextXAlignment.Left
    ltTitle.TextColor3 = Color3.fromRGB(255,255,255)
    ltTitle.TextStrokeTransparency = 0.25
    ltTitle.ZIndex = 7
    ltTitle.Parent = lowerThird
    local ltSub = Instance.new("TextLabel")
    ltSub.BackgroundTransparency = 1
    ltSub.Position = UDim2.fromOffset(12, 28)
    ltSub.Size = UDim2.new(1, -24, 0, 20)
    ltSub.Font = Enum.Font.GothamSemibold
    ltSub.TextSize = 12
    ltSub.RichText = true
    ltSub.TextXAlignment = Enum.TextXAlignment.Left
    ltSub.TextColor3 = Color3.fromRGB(220,230,220)
    ltSub.TextStrokeTransparency = 0.55
    ltSub.ZIndex = 7
    ltSub.Parent = lowerThird

    local statsCard = Instance.new("Frame")
    statsCard.AnchorPoint = Vector2.new(0.5, 0.5)
    statsCard.Position = UDim2.new(0.5, 0, 0.5, 0)
    statsCard.Size = UDim2.fromOffset(0, 0)
    statsCard.BackgroundColor3 = Color3.fromRGB(7, 13, 9)
    statsCard.BackgroundTransparency = 0.05
    statsCard.BorderSizePixel = 0
    statsCard.Visible = false
    statsCard.ZIndex = 11
    statsCard.Parent = frame
    local scc = Instance.new("UICorner"); scc.CornerRadius = UDim.new(0, 12); scc.Parent = statsCard
    local scs = Instance.new("UIStroke"); scs.Color = Color3.fromRGB(61,255,138); scs.Thickness = 1.5; scs.Parent = statsCard
    local statsText = Instance.new("TextLabel")
    statsText.BackgroundTransparency = 1
    statsText.Position = UDim2.fromOffset(12, 8)
    statsText.Size = UDim2.new(1, -24, 1, -16)
    statsText.Font = Enum.Font.GothamBold
    statsText.TextSize = 13
    statsText.RichText = true
    statsText.TextWrapped = true
    statsText.TextColor3 = Color3.fromRGB(235,245,235)
    statsText.TextStrokeTransparency = 0.45
    statsText.ZIndex = 12
    statsText.Text = ""
    statsText.Parent = statsCard

    local ticker = Instance.new("TextLabel")
    ticker.BackgroundColor3 = Color3.fromRGB(5, 8, 6)
    ticker.BackgroundTransparency = 0.16
    ticker.BorderSizePixel = 0
    ticker.Position = UDim2.new(0, 0, 1, -22)
    ticker.Size = UDim2.new(1, 0, 0, 22)
    ticker.Font = Enum.Font.GothamSemibold
    ticker.TextSize = 11
    ticker.TextXAlignment = Enum.TextXAlignment.Left
    ticker.TextColor3 = Color3.fromRGB(220, 230, 220)
    ticker.RichText = true
    ticker.ZIndex = 2
    ticker.Text = "   ESPN COLLEGE FOOTBALL  •  MIAMI MONEY DRIVE  •  LIVE"
    ticker.Parent = frame

    local ball = Instance.new("TextLabel")
    ball.BackgroundTransparency = 1
    ball.Size = UDim2.fromOffset(28, 20)
    ball.Position = UDim2.fromOffset(218, 205)
    ball.Font = Enum.Font.GothamBlack
    ball.TextSize = 18
    ball.Text = "🏈"
    ball.ZIndex = 4
    ball.Parent = frame

    local progressBack = Instance.new("Frame")
    progressBack.Position = UDim2.new(0, 45, 1, -34)
    progressBack.Size = UDim2.new(1, -90, 0, 5)
    progressBack.BackgroundColor3 = Color3.fromRGB(25, 35, 28)
    progressBack.BorderSizePixel = 0
    progressBack.ZIndex = 2
    progressBack.Parent = frame
    local pbc = Instance.new("UICorner"); pbc.CornerRadius = UDim.new(1, 0); pbc.Parent = progressBack

    local progress = Instance.new("Frame")
    progress.Size = UDim2.new(0, 0, 1, 0)
    progress.BackgroundColor3 = Color3.fromRGB(61, 255, 138)
    progress.BorderSizePixel = 0
    progress.ZIndex = 3
    progress.Parent = progressBack
    local pc = Instance.new("UICorner"); pc.CornerRadius = UDim.new(1, 0); pc.Parent = progress

    local bigPlay = Instance.new("Frame")
    bigPlay.AnchorPoint = Vector2.new(0.5, 0.5)
    bigPlay.Position = UDim2.new(0.5, 0, 0.5, 0)
    bigPlay.Size = UDim2.fromOffset(0, 0)
    bigPlay.BackgroundColor3 = Color3.fromRGB(6, 12, 8)
    bigPlay.BackgroundTransparency = 0.08
    bigPlay.BorderSizePixel = 0
    bigPlay.Visible = false
    bigPlay.ZIndex = 8
    bigPlay.Parent = frame
    local bpc = Instance.new("UICorner"); bpc.CornerRadius = UDim.new(0, 10); bpc.Parent = bigPlay
    local bps = Instance.new("UIStroke"); bps.Color = Color3.fromRGB(61,255,138); bps.Thickness = 1.5; bps.Parent = bigPlay
    local bigText = Instance.new("TextLabel")
    bigText.BackgroundTransparency = 1
    bigText.Size = UDim2.fromScale(1, 1)
    bigText.Font = Enum.Font.GothamBlack
    bigText.TextSize = 22
    bigText.RichText = true
    bigText.TextColor3 = Color3.fromRGB(255,255,255)
    bigText.TextStrokeTransparency = 0.15
    bigText.ZIndex = 9
    bigText.Text = "TOUCHDOWN"
    bigText.Parent = bigPlay

    local matchupCard = Instance.new("Frame")
    matchupCard.AnchorPoint = Vector2.new(0.5, 0.5)
    matchupCard.Position = UDim2.new(0.5, 0, 0.5, 0)
    matchupCard.Size = UDim2.fromOffset(430, 170)
    matchupCard.BackgroundColor3 = Color3.fromRGB(6, 12, 8)
    matchupCard.BackgroundTransparency = 0.03
    matchupCard.BorderSizePixel = 0
    matchupCard.ZIndex = 13
    matchupCard.Parent = frame
    local mcc = Instance.new("UICorner"); mcc.CornerRadius = UDim.new(0, 14); mcc.Parent = matchupCard
    local mcs = Instance.new("UIStroke"); mcs.Color = Color3.fromRGB(61,255,138); mcs.Thickness = 1.5; mcs.Parent = matchupCard

    local function makeMatchupPanel(asset, fallback, color, x)
        local panel = Instance.new("Frame")
        panel.Position = UDim2.fromOffset(x, 42)
        panel.Size = UDim2.fromOffset(170, 86)
        panel.BackgroundColor3 = color
        panel.BackgroundTransparency = 0.1
        panel.BorderSizePixel = 0
        panel.ClipsDescendants = true
        panel.ZIndex = 14
        panel.Parent = matchupCard
        local pc = Instance.new("UICorner"); pc.CornerRadius = UDim.new(0, 10); pc.Parent = panel
        if asset and tostring(asset) ~= "" then
            local img = Instance.new("ImageLabel")
            img.BackgroundTransparency = 1
            img.Size = UDim2.fromScale(1.12, 1.12)
            img.Position = UDim2.fromScale(-0.06, -0.06)
            img.Image = tostring(asset)
            img.ScaleType = Enum.ScaleType.Crop
            img.ImageTransparency = 0.18
            img.ZIndex = 14
            img.Parent = panel
        else
            local fake = Instance.new("TextLabel")
            fake.BackgroundTransparency = 1
            fake.Size = UDim2.fromScale(1, 1)
            fake.Font = Enum.Font.GothamBlack
            fake.Text = fallback
            fake.TextSize = (#tostring(fallback) > 3) and 32 or 44
            fake.TextColor3 = Color3.fromRGB(255,255,255)
            fake.TextStrokeTransparency = 0.35
            fake.TextTransparency = 0.1
            fake.ZIndex = 15
            fake.Parent = panel
        end
        return panel
    end

    makeMatchupPanel(MiamiPhotoAsset, "U", Color3.fromRGB(244,115,33), 18)
    makeMatchupPanel(opponent.Photo, opponent.Short, opponent.Color, 242)

    local matchupTitle = Instance.new("TextLabel")
    matchupTitle.BackgroundTransparency = 1
    matchupTitle.Position = UDim2.fromOffset(0, 8)
    matchupTitle.Size = UDim2.new(1, 0, 0, 26)
    matchupTitle.Font = Enum.Font.GothamBlack
    matchupTitle.TextSize = 18
    matchupTitle.RichText = true
    matchupTitle.TextColor3 = Color3.fromRGB(255,255,255)
    matchupTitle.TextStrokeTransparency = 0.25
    matchupTitle.ZIndex = 16
    matchupTitle.Text = '<font color="#3dff8a">MIAMI</font> VS <font color="#ffd166">' .. opponent.Name .. '</font>'
    matchupTitle.Parent = matchupCard

    local vs = Instance.new("TextLabel")
    vs.BackgroundTransparency = 1
    vs.AnchorPoint = Vector2.new(0.5, 0.5)
    vs.Position = UDim2.new(0.5, 0, 0.54, 0)
    vs.Size = UDim2.fromOffset(70, 50)
    vs.Font = Enum.Font.GothamBlack
    vs.TextSize = 24
    vs.TextColor3 = Color3.fromRGB(255,255,255)
    vs.TextStrokeTransparency = 0.15
    vs.Text = "VS"
    vs.ZIndex = 17
    vs.Parent = matchupCard

    local matchupSub = Instance.new("TextLabel")
    matchupSub.BackgroundTransparency = 1
    matchupSub.Position = UDim2.fromOffset(0, 136)
    matchupSub.Size = UDim2.new(1, 0, 0, 20)
    matchupSub.Font = Enum.Font.GothamSemibold
    matchupSub.TextSize = 12
    matchupSub.TextColor3 = Color3.fromRGB(210,220,210)
    matchupSub.TextStrokeTransparency = 0.55
    matchupSub.ZIndex = 16
    matchupSub.Text = "Pregame photos loaded  •  Money generation kickoff"
    matchupSub.Parent = matchupCard

    task.delay(2.6, function()
        if matchupCard and matchupCard.Parent then
            TweenService:Create(matchupCard, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.fromOffset(0, 0), BackgroundTransparency = 1}):Play()
            for _, d in ipairs(matchupCard:GetDescendants()) do
                if d:IsA("TextLabel") then TweenService:Create(d, TweenInfo.new(0.2), {TextTransparency = 1, TextStrokeTransparency = 1}):Play() end
                if d:IsA("ImageLabel") then TweenService:Create(d, TweenInfo.new(0.2), {ImageTransparency = 1}):Play() end
                if d:IsA("Frame") then TweenService:Create(d, TweenInfo.new(0.2), {BackgroundTransparency = 1}):Play() end
            end
            task.wait(0.3)
            if matchupCard then matchupCard:Destroy() end
        end
    end)

    local miamiScore, oppScore = 0, 0
    local q, secondsLeft = 1, 15 * 60
    local possession = "MIAMI"
    local down, distance, yardLine = 1, 10, 25
    local drivePlays, driveYards, driveStart = 0, 0, 25
    local momentum = 50
    local stats = { MiamiPass=0, MiamiRush=0, OppPass=0, OppRush=0, MiamiTO=0, OppTO=0 }
    local halftimeShown = false
    local running, finished = true, false

    local function quarterText()
        if q <= 4 then return "Q" .. q end
        return "OT" .. tostring(q - 4)
    end

    local function updateClock()
        local mins = math.floor(secondsLeft / 60)
        local secs = secondsLeft % 60
        quarter.Text = string.format("%s %02d:%02d", quarterText(), mins, secs)
    end

    local function playBroadcastSound(kind)
        local ids = getgenv().MiamiFootballSounds
        local id = ids and ids[kind]
        if not id then return end
        pcall(function()
            local s = Instance.new("Sound")
            s.SoundId = id
            s.Volume = 0.8
            s.Parent = gui
            s:Play()
            game:GetService("Debris"):AddItem(s, 5)
        end)
    end

    local function fieldName()
        if possession == "MIAMI" then
            if yardLine < 50 then return "MIA " .. tostring(math.max(1, yardLine)) end
            return opponent.Short .. " " .. tostring(math.max(1, 100 - yardLine))
        else
            if yardLine > 50 then return opponent.Short .. " " .. tostring(math.max(1, 100 - yardLine)) end
            return "MIA " .. tostring(math.max(1, yardLine))
        end
    end

    local function resetDrive(newPossession)
        possession = newPossession or ((possession == "MIAMI") and opponent.Short or "MIAMI")
        down, distance = 1, 10
        yardLine = (possession == "MIAMI") and 25 or 75
        driveStart, drivePlays, driveYards = yardLine, 0, 0
    end

    local function showLowerThird(headline, detail, team)
        lowerThird.Visible = true
        lowerThird.Position = UDim2.new(0, -470, 0, 100)
        lowerThird.BackgroundColor3 = (team == "MIAMI") and Color3.fromRGB(0, 65, 38) or opponent.Color
        ltTitle.Text = headline
        ltSub.Text = detail
        TweenService:Create(lowerThird, TweenInfo.new(0.23, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Position = UDim2.new(0, 0, 0, 100)}):Play()
        task.delay(2.2, function()
            if lowerThird.Parent and lowerThird.Visible then
                TweenService:Create(lowerThird, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Position = UDim2.new(0, 470, 0, 100)}):Play()
                task.wait(0.28)
                if lowerThird.Parent then lowerThird.Visible = false end
            end
        end)
    end

    local function flashStadium(team)
        local oldColor = stroke.Color
        local flashColor = (team == "MIAMI") and Color3.fromRGB(61,255,138) or Color3.fromRGB(255,220,120)
        for i = 1, 3 do
            if not stroke.Parent then return end
            stroke.Color = flashColor
            stroke.Thickness = 3
            task.wait(0.08)
            stroke.Color = oldColor
            stroke.Thickness = 1.8
            task.wait(0.08)
        end
    end

    local function showStatsCard(kind)
        statsCard.Visible = true
        statsCard.Size = UDim2.fromOffset(0, 0)
        local titleText = (kind == "HALF") and "HALFTIME REPORT" or "FINAL REPORT"
        statsText.Text = string.format(
            '<font color="#3dff8a"><b>%s</b></font><br/>MIAMI %d  •  %s %d<br/>MIA PASS %d  RUSH %d  TO %d<br/>%s PASS %d  RUSH %d  TO %d',
            titleText, miamiScore, opponent.Short, oppScore,
            stats.MiamiPass, stats.MiamiRush, stats.MiamiTO,
            opponent.Short, stats.OppPass, stats.OppRush, stats.OppTO
        )
        TweenService:Create(statsCard, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.fromOffset(330, 118)}):Play()
        task.delay((kind == "HALF") and 3 or 4.2, function()
            if statsCard.Parent and statsCard.Visible then
                TweenService:Create(statsCard, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.fromOffset(0, 0)}):Play()
                task.wait(0.22)
                if statsCard.Parent then statsCard.Visible = false end
            end
        end)
    end

    local function pulseScore(which)
        local lbl = (which == "MIAMI") and miamiScoreLabel or oppScoreLabel
        local box = (which == "MIAMI") and miamiBox or oppBox
        local logo = (which == "MIAMI") and miamiLogo or oppLogo
        local oldSize = lbl.TextSize
        local oldPos = box.Position
        local logoOldSize = logo and logo.Size
        local logoOldRot = logo and logo.Rotation or 0
        TweenService:Create(lbl, TweenInfo.new(0.12, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {TextSize = oldSize + 8}):Play()
        TweenService:Create(box, TweenInfo.new(0.07), {Position = oldPos + UDim2.fromOffset(math.random(-4,4), math.random(-2,2))}):Play()
        if logo then
            TweenService:Create(logo, TweenInfo.new(0.16, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = UDim2.fromOffset(42, 42),
                Rotation = logoOldRot + ((which == "MIAMI") and -8 or 8),
            }):Play()
        end
        task.delay(0.09, function()
            if box.Parent then TweenService:Create(box, TweenInfo.new(0.12), {Position = oldPos}):Play() end
        end)
        task.delay(0.18, function()
            if lbl.Parent then TweenService:Create(lbl, TweenInfo.new(0.18), {TextSize = oldSize}):Play() end
            if logo and logo.Parent and logoOldSize then
                TweenService:Create(logo, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    Size = logoOldSize,
                    Rotation = logoOldRot,
                }):Play()
            end
        end)
    end

    local function showBurst(teamName, points, playerName)
        bigPlay.Visible = true
        bigPlay.BackgroundColor3 = (teamName == "MIAMI") and Color3.fromRGB(0, 70, 40) or opponent.Color
        bigPlay.Size = UDim2.fromOffset(0, 0)
        bigText.TextTransparency = 0
        bigText.TextStrokeTransparency = 0.15
        bigText.Text = string.format("%s +%d<br/><font size=\"14\">%s</font>", teamName, points, playerName)
        TweenService:Create(bigPlay, TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.fromOffset(310, 58)}):Play()

        for i = 1, 12 do
            local c = Instance.new("TextLabel")
            c.BackgroundTransparency = 1
            c.Size = UDim2.fromOffset(18, 18)
            c.Position = UDim2.new(0.5, 0, 0.5, 0)
            c.Font = Enum.Font.GothamBlack
            c.TextSize = math.random(12, 18)
            c.Text = (math.random(1, 3) == 1) and "🏈" or "✦"
            c.TextColor3 = (teamName == "MIAMI") and Color3.fromRGB(61,255,138) or Color3.fromRGB(255,220,120)
            c.ZIndex = 10
            c.Parent = frame
            local dx, dy = math.random(-175, 175), math.random(-70, 70)
            TweenService:Create(c, TweenInfo.new(0.65, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                Position = UDim2.new(0.5, dx, 0.5, dy), TextTransparency = 1
            }):Play()
            task.delay(0.75, function() if c then c:Destroy() end end)
        end

        task.delay(0.85, function()
            if bigPlay.Parent then
                TweenService:Create(bigText, TweenInfo.new(0.18), {TextTransparency = 1, TextStrokeTransparency = 1}):Play()
                TweenService:Create(bigPlay, TweenInfo.new(0.22, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.fromOffset(0, 0)}):Play()
                task.wait(0.24)
                bigPlay.Visible = false
            end
        end)
    end

    local function setPlay(txt, playerTxt)
        play.TextTransparency = 0.35
        play.Position = UDim2.fromOffset(14, 110)
        play.Text = txt
        if playerTxt then scorer.Text = playerTxt end
        TweenService:Create(play, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            TextTransparency = 0,
            Position = UDim2.fromOffset(14, 105),
        }):Play()
    end

    local function updateAll()
        updateClock()
        miamiScoreLabel.Text = tostring(miamiScore)
        oppScoreLabel.Text = tostring(oppScore)
        local progressValue = math.clamp(((q - 1) + (1 - (secondsLeft / 900))) / 4, 0, 0.98)
        if q > 4 then progressValue = 0.98 end
        progress.Size = UDim2.new(progressValue, 0, 1, 0)
        local downName = ({"1ST", "2ND", "3RD", "4TH"})[math.clamp(down,1,4)] or "1ST"
        driveLabel.Text = string.format('%s & %d  •  BALL ON %s  •  DRIVE: %d PLAYS, %d YDS', downName, distance, fieldName(), drivePlays, driveYards)
        local redZone = (possession == "MIAMI" and yardLine >= 80) or (possession ~= "MIAMI" and yardLine <= 20)
        redZoneLabel.Text = redZone and "RED ZONE" or ""
        if redZone then
            redZoneLabel.TextColor3 = (possession == "MIAMI") and Color3.fromRGB(61,255,138) or Color3.fromRGB(255,90,90)
        end
        TweenService:Create(ballMarker, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position = UDim2.new(math.clamp(yardLine / 100, 0.03, 0.97), 0, 0.5, 0)
        }):Play()
        momentum = math.clamp(momentum, 0, 100)
        TweenService:Create(momentumFill, TweenInfo.new(0.25), {Size = UDim2.new(momentum / 100, 0, 1, 0)}):Play()
        ticker.Text = string.format("   LIVE  •  %s BALL  •  MIAMI %d - %s %d  •  %s  •  MOMENTUM %d%%", possession, miamiScore, opponent.Short, oppScore, quarterText(), momentum)
    end

    local MiamiTD = {
        "hauls in a fade for the touchdown",
        "breaks a tackle and scores from the red zone",
        "takes the jet sweep to the pylon",
        "catches a strike over the middle for six",
        "powers across the goal line",
    }
    local MiamiFG = {
        "splits the uprights from 38",
        "drills the field goal after a long drive",
        "banks in a clutch kick",
    }
    local OppTD = {
        "answers with a touchdown catch",
        "finds the edge and scores",
        "wins the jump ball in the end zone",
        "caps the drive with a short rushing TD",
    }
    local OppFG = {
        "connects on a field goal",
        "sneaks the kick inside the upright",
        "makes it from 42 yards out",
    }

    local function addPoints(team, points)
        local playerName, desc, teamLabel, scoreType
        local gain = math.random(8, 55)
        drivePlays = drivePlays + math.random(1, 3)
        driveYards = math.abs(yardLine - driveStart) + gain

        if team == "MIAMI" then
            playerName = pick(MiamiRoster)
            teamLabel = "MIAMI"
            scoreType = (points == 3) and "FIELD GOAL" or "TOUCHDOWN MIAMI"
            if points == 3 then desc = pick(MiamiFG); stats.MiamiPass = stats.MiamiPass + math.random(8, 35)
            else desc = pick(MiamiTD); if math.random(1,2)==1 then stats.MiamiPass = stats.MiamiPass + gain else stats.MiamiRush = stats.MiamiRush + gain end end
            miamiScore = miamiScore + points
            momentum = momentum + ((points == 3) and 6 or 11)
            pulseScore("MIAMI")
            setPlay(string.format('<font color="#3dff8a">%s</font> %s. Miami +%d.', playerName, desc, points),
                string.format('Last score: <font color="#3dff8a">%s</font> for Miami', playerName))
            showLowerThird('<font color="#3dff8a">'..scoreType..'</font>', string.format('%s  •  %d yd play  •  Drive: %d plays, %d yards', playerName, gain, drivePlays, driveYards), "MIAMI")
        else
            playerName = pick(opponent.Players)
            teamLabel = opponent.Short
            scoreType = (points == 3) and (opponent.Short .. " FIELD GOAL") or (opponent.Short .. " TOUCHDOWN")
            if points == 3 then desc = pick(OppFG); stats.OppPass = stats.OppPass + math.random(8, 35)
            else desc = pick(OppTD); if math.random(1,2)==1 then stats.OppPass = stats.OppPass + gain else stats.OppRush = stats.OppRush + gain end end
            oppScore = oppScore + points
            momentum = momentum - ((points == 3) and 6 or 11)
            pulseScore("OPP")
            setPlay(string.format('<font color="#ffd166">%s</font> %s for %s. +%d.', playerName, desc, opponent.Name, points),
                string.format('Last score: <font color="#ffd166">%s</font> for %s', playerName, opponent.Name))
            showLowerThird('<font color="#ffd166">'..scoreType..'</font>', string.format('%s  •  %d yd play  •  Drive: %d plays, %d yards', playerName, gain, drivePlays, driveYards), "OPP")
        end
        showBurst(teamLabel, points, playerName)
        task.spawn(function() flashStadium(team) end)
        playBroadcastSound(points == 3 and "FieldGoal" or "Touchdown")
        resetDrive((team == "MIAMI") and opponent.Short or "MIAMI")
        updateAll()
    end

    local NoScoreEvents = {
        "Miami forces a punt after a huge third-down stop.",
        "The defense bends but does not break.",
        "Drive stalls near midfield. Punt team is out.",
        "Sack on third down! Crowd is loud.",
        "Review confirms the runner was short.",
        "A holding call wipes out a big gain.",
        "Great coverage downfield, incomplete on third down.",
        "Clock keeps moving after a short run inside.",
    }

    updateAll()
    TweenService:Create(frame, TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Position = UDim2.new(0.5, 0, 0, 54)
    }):Play()

    task.spawn(function()
        local x = 207
        local dir = 1
        while running and gui.Parent do
            x = x + (dir * 9)
            if x > 412 then dir = -1 end
            if x < 12 then dir = 1 end
            TweenService:Create(ball, TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Position = UDim2.fromOffset(x, 157)}):Play()
            task.wait(0.55)
        end
    end)

    task.spawn(function()
        while running and gui.Parent do
            task.wait(math.random(135, 210) / 100)
            if not running then break end

            secondsLeft = secondsLeft - math.random(70, 150)
            if secondsLeft <= 0 then
                if q < 4 then
                    q = q + 1
                    secondsLeft = 15 * 60
                    setPlay(string.format('<font color="#3dff8a">END OF Q%d.</font> Starting %s...', q - 1, quarterText()), "New quarter. Scorebug still live.")
                    if q == 3 and not halftimeShown then halftimeShown = true; showStatsCard("HALF") end
                    updateAll()
                    task.wait(0.8)
                else
                    q = q + 1
                    secondsLeft = 10 * 60
                    setPlay('<font color="#ffd166">END OF REGULATION.</font> Money generation is still running, so we go to OT.', "Overtime started because generation is still active.")
                    updateAll()
                    task.wait(0.8)
                end
            end

            drivePlays = drivePlays + 1
            local gain = math.random(-3, 24)
            if math.random(1, 100) <= 6 then
                if possession == "MIAMI" then stats.MiamiTO = stats.MiamiTO + 1 else stats.OppTO = stats.OppTO + 1 end
                setPlay('<font color="#ff7777">TURNOVER!</font> Defense flips the field.', "Possession changed after a takeaway.")
                resetDrive((possession == "MIAMI") and opponent.Short or "MIAMI")
                updateAll()
                task.wait(0.7)
            end
            if possession == "MIAMI" then
                yardLine = math.clamp(yardLine + math.max(gain, 0), 1, 99)
                if gain > 0 then stats.MiamiRush = stats.MiamiRush + math.floor(gain * 0.45); stats.MiamiPass = stats.MiamiPass + math.floor(gain * 0.55) end
            else
                yardLine = math.clamp(yardLine - math.max(gain, 0), 1, 99)
                if gain > 0 then stats.OppRush = stats.OppRush + math.floor(gain * 0.45); stats.OppPass = stats.OppPass + math.floor(gain * 0.55) end
            end
            if gain >= distance then down, distance = 1, 10 else down = down + 1; distance = math.max(1, distance - math.max(gain, 0)) end
            if down > 4 then
                setPlay('Turnover on downs near ' .. fieldName() .. '.', "No score on the drive.")
                resetDrive((possession == "MIAMI") and opponent.Short or "MIAMI")
                updateAll()
                task.wait(0.6)
            end

            local scoreChance = clampScoreChance(miamiScore, oppScore, q)
            if (possession == "MIAMI" and yardLine >= 80) or (possession ~= "MIAMI" and yardLine <= 20) then scoreChance = scoreChance + 18 end
            local r = math.random(1, 100)
            if r <= scoreChance then
                local team = (math.random(1, 100) <= 54) and "MIAMI" or "OPP"
                possession = (team == "MIAMI") and "MIAMI" or opponent.Short

                local roll = math.random(1, 100)
                local points = 7
                if roll <= 38 then points = 3
                elseif roll <= 88 then points = 7
                elseif roll <= 96 then points = 6
                else points = 8 end

                addPoints(team, points)
            else
                setPlay(pick(NoScoreEvents), "No score on the drive.")
                if math.random(1, 100) <= 45 then resetDrive((possession == "MIAMI") and opponent.Short or "MIAMI") end
                updateAll()
            end
        end
    end)

    local function makeRealisticFinal()
        local finalPairs = {
            {24,17}, {27,20}, {28,24}, {31,23}, {31,27}, {34,28},
            {35,31}, {23,20}, {30,24}, {21,17}, {38,31}, {26,20},
        }
        local pair = pick(finalPairs)
        local targetMiami, targetOpp = pair[1], pair[2]
        if miamiScore > targetMiami then targetMiami = miamiScore end
        if oppScore > targetOpp then targetOpp = oppScore end
        if targetMiami <= targetOpp then targetMiami = targetOpp + pick({1,3,4,7}) end
        miamiScore, oppScore = targetMiami, targetOpp
    end

    local function finishGame(success)
        if finished then return end
        finished = true
        running = false

        q = math.min(q, 4)
        secondsLeft = 0

        if success ~= false then
            makeRealisticFinal()
            local mvp = pick(MiamiRoster)
            setPlay(string.format('<font color="#3dff8a">FINAL!</font> Miami wins %d-%d over %s — money generation complete.', miamiScore, oppScore, opponent.Name),
                string.format('Player of the game: <font color="#3dff8a">%s</font>', mvp))
        else
            setPlay(string.format('<font color="#ff7777">GAME CALLED.</font> Money generation stopped. Miami %d - %s %d.', miamiScore, opponent.Name, oppScore),
                "Generation stopped before the final whistle.")
        end

        progress.Size = UDim2.new(1, 0, 1, 0)
        updateClock()
        miamiScoreLabel.Text = tostring(miamiScore)
        oppScoreLabel.Text = tostring(oppScore)
        ticker.Text = string.format("   FINAL  •  MIAMI %d  -  %s %d  •  %s", miamiScore, opponent.Short, oppScore, success ~= false and "MONEY COMPLETE" or "STOPPED")
        pulseScore("MIAMI")
        pulseScore("OPP")
        showBurst("FINAL", 0, "MIAMI MONEY BOWL")
        playBroadcastSound("Final")
        showStatsCard("FINAL")

        task.wait(4.75)
        if gui and gui.Parent then
            TweenService:Create(frame, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Position = UDim2.new(0.5, 0, 0, -200)}):Play()
            for _, d in ipairs(gui:GetDescendants()) do
                if d:IsA("TextLabel") then TweenService:Create(d, TweenInfo.new(0.35), {TextTransparency = 1, TextStrokeTransparency = 1}):Play() end
                if d:IsA("Frame") then TweenService:Create(d, TweenInfo.new(0.35), {BackgroundTransparency = 1}):Play() end
                if d:IsA("ImageLabel") then TweenService:Create(d, TweenInfo.new(0.35), {ImageTransparency = 1}):Play() end
                if d:IsA("UIStroke") then TweenService:Create(d, TweenInfo.new(0.35), {Transparency = 1}):Play() end
            end
            task.wait(0.45)
            gui:Destroy()
        end
    end

    return finishGame
end

local function GetGoodCleaner()
    if not workspace:FindFirstChild("1# Map") then return nil end
    local CounterInstance
    for _, V in pairs(workspace["1# Map"]:GetChildren()) do
        if V:FindFirstChild("CounterM") then CounterInstance = V; break end
    end
    if not CounterInstance then return nil end
    for _, v in pairs(CounterInstance:GetChildren()) do
        local cp = v:FindFirstChild("CashPrompt", true)
        local gp = v:FindFirstChild("GrabPrompt", true)
        if cp and cp.Enabled and cp.ObjectText == "Count Bread" and gp and not gp.Enabled then
            return v
        end
    end
end

local function CleanAllFilthyMoney()
    local Player = LocalPlayer
    if not Player:FindFirstChild("stored") or not Player.stored:FindFirstChild("FilthyStack") or Player.stored.FilthyStack.Value == 0 then
        sendNotification("Money","You don't have any filthy cash!"); return
    end
    if not Player.Character or not Player.Character:FindFirstChild("HumanoidRootPart") then return end
    local Cleaner = GetGoodCleaner()
    if not Cleaner then sendNotification("Money","Couldn't find a good cleaner!"); return end
    pcall(function() Config:Teleport(Cleaner.WorldPivot or (Cleaner.CFrame or Cleaner:GetPivot())) end)
    task.wait(0.4)
    pcall(function() fireproximityprompt(Cleaner:FindFirstChild("CashPrompt", true)) end)
    local onObj = Cleaner:FindFirstChild("On", true)
    if onObj then repeat task.wait() until onObj.Color == Color3.fromRGB(74,156,69) else task.wait(0.5) end
    task.wait(0.5)
    pcall(function() fireproximityprompt(Cleaner:FindFirstChild("CashPrompt", true)) end)
    task.wait(0.25)
    pcall(function() Config:Teleport(Cleaner.WorldPivot or (Cleaner.CFrame or Cleaner:GetPivot())) end)
    task.wait(0.4)
    repeat task.wait() until Player.Backpack:FindFirstChild("MoneyReady")
    Player.Character.Humanoid:EquipTool(Player.Backpack["MoneyReady"])
    repeat task.wait(1) pcall(function() fireproximityprompt(Cleaner:FindFirstChild("GrabPrompt", true)) end) until not Player.Character:FindFirstChild("MoneyReady")
    repeat task.wait() until Player.Backpack:FindFirstChild("BagOfMoney")
    pcall(function() Config:Teleport(CFrame.new(-1217.30, 253.88, -3635.04)) end)
    task.wait(0.4)
    if Player.Backpack:FindFirstChild("BagOfMoney") then Player.Character.Humanoid:EquipTool(Player.Backpack["BagOfMoney"]) end
    task.wait(1)
    pcall(function()
        local atm = workspace:FindFirstChild("ATMMoney")
        local prompt = atm and atm:FindFirstChild("Prompt", true)
        if prompt then fireproximityprompt(prompt) end
    end)
end

local function InfMoneyHoldCupz()
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local hrp = char:WaitForChild("HumanoidRootPart")
    local origin = hrp.CFrame
    local sell = workspace:FindFirstChild("IceFruit Sell")
    if not sell then sendNotification("Inf Money","IceFruit Sell not found."); return end
    local prompt = sell:FindFirstChild("ProximityPrompt"); if not prompt then return end
    prompt.HoldDuration = 0
    prompt.MaxActivationDistance = math.huge
    Config:Teleport(sell.CFrame); task.wait(1)
    for _ = 1, 250 do task.spawn(function() pcall(function() fireproximityprompt(prompt, 1) end) end) end
    task.wait(5)
    Config:Teleport(origin); task.wait(0.25)
    getgenv().SwimMethod = false
end

local function SetupInfiniteMoney()
    for _, item in ipairs({"Ice-Fruit Bag","Ice-Fruit Cupz","FijiWater","FreshWater"}) do
        task.spawn(function() pcall(function() ReplicatedStorage:WaitForChild("ExoticShopRemote"):InvokeServer(item) end) end)
    end
end

local function GenerateMaxIllegalMoney()
    local Player = LocalPlayer
    local function GetFruitCup()
        for _, container in ipairs({Player.Backpack, Player.Character}) do
            if container then
                for _, tool in pairs(container:GetChildren()) do
                    if tool:IsA("Tool") and tool.Name == "Ice-Fruit Cupz" then
                        local cupPart = tool:FindFirstChild("IceFruit Cup")
                        if cupPart and cupPart:FindFirstChild("IceFruit PunchMedium")
                           and cupPart["IceFruit PunchMedium"].Transparency ~= 1 then
                            return true, tool
                        end
                    end
                end
            end
        end
        return false, nil
    end

    local Found, Cup = GetFruitCup()
    local oldCFrame = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") and Player.Character.HumanoidRootPart.CFrame
    if Cup and Found and oldCFrame and workspace:FindFirstChild("IceFruit Sell") then
        if Cup.Parent == Player.Backpack then Player.Character.Humanoid:EquipTool(Cup); task.wait(1) end
        Config:Teleport(workspace["IceFruit Sell"].CFrame); task.wait(0.5)
        for _ = 1, 4000 do task.spawn(function() pcall(function() fireproximityprompt(workspace["IceFruit Sell"].ProximityPrompt) end) end) end
        Config:Teleport(oldCFrame); task.wait(8)
        return
    end

    local items = {"FijiWater","FreshWater","Ice-Fruit Bag","Ice-Fruit Cupz"}
    local stove
    if workspace:FindFirstChild("CookingPots") then
        for _, value in pairs(workspace.CookingPots:GetChildren()) do
            if value:IsA("Model") then
                local prompt = value:FindFirstChildWhichIsA("ProximityPrompt", true)
                if prompt and prompt.ActionText == "Turn On" and prompt.Enabled then stove = value; break end
            end
        end
    end

    for _, item in ipairs(items) do
        if not Player.Backpack:FindFirstChild(item) then
            pcall(function() ReplicatedStorage:WaitForChild("ExoticShopRemote"):InvokeServer(item) end)
            task.wait(1)
        end
    end
    for _, item in ipairs(items) do
        if not Player.Backpack:FindFirstChild(item) then
            sendNotification("Money", "Need more cash / missing items."); return
        end
    end
    if not stove then sendNotification("Money", "Could not find a cooking stove."); return end

    Config:Teleport(stove.CookPart.CFrame); task.wait(1)
    pcall(function() StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.Backpack, false) end)
    Player.Character.HumanoidRootPart.Anchored = true; task.wait(1.5)
    pcall(function() fireproximityprompt(stove:FindFirstChildWhichIsA("ProximityPrompt", true)) end)
    task.wait(2)
    for _, item in ipairs({"FijiWater","FreshWater","Ice-Fruit Bag"}) do
        if Player.Backpack:FindFirstChild(item) then
            Player.Character.Humanoid:EquipTool(Player.Backpack[item]); task.wait(1)
            pcall(function() fireproximityprompt(stove:FindFirstChildWhichIsA("ProximityPrompt", true)) end)
            task.wait(3)
        end
    end
    pcall(function() repeat task.wait() until stove.CookPart.Steam.LoadUI.Enabled == false end)
    if not Player.Character:FindFirstChild("Ice-Fruit Cupz") and Player.Backpack:FindFirstChild("Ice-Fruit Cupz") then
        Player.Character.Humanoid:EquipTool(Player.Backpack["Ice-Fruit Cupz"]); task.wait(1)
    end
    task.wait(1)
    pcall(function() fireproximityprompt(stove:FindFirstChildWhichIsA("ProximityPrompt", true)) end)
    task.wait(3)
    Player.Character.HumanoidRootPart.Anchored = false
    if workspace:FindFirstChild("IceFruit Sell") then
        Config:Teleport(workspace["IceFruit Sell"].CFrame); task.wait(1)
        Player.Character.HumanoidRootPart.Anchored = true; task.wait(1.5)
        if not Player.Character:FindFirstChild("Ice-Fruit Cupz") and Player.Backpack:FindFirstChild("Ice-Fruit Cupz") then
            Player.Character.Humanoid:EquipTool(Player.Backpack["Ice-Fruit Cupz"]); task.wait(1)
        end
        workspace["IceFruit Sell"].ProximityPrompt.HoldDuration = 0
        for _ = 1, 4000 do task.spawn(function() pcall(function() fireproximityprompt(workspace["IceFruit Sell"].ProximityPrompt) end) end) end
    end
    Player.Character.HumanoidRootPart.Anchored = false
    pcall(function() StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.Backpack, true) end)
end

getgenv().MoneyDropEnabled = getgenv().MoneyDropEnabled or false
local function DropMoneyAmount(amount)
    local BankRemote = ReplicatedStorage:FindFirstChild('BankProcessRemote', true)
    if not BankRemote then return false end
    local function send(...) if BankRemote.InvokeServer then return BankRemote:InvokeServer(...) elseif BankRemote.FireServer then return BankRemote:FireServer(...) end end
    for _, args in ipairs({{"Drop",tostring(amount)},{"Drop",tonumber(amount) or amount}}) do
        local ok, r = pcall(send, table.unpack(args))
        if ok and r ~= false then return true end
    end
end
task.spawn(function()
    while task.wait(0.3) do
        if getgenv().MoneyDropEnabled then pcall(function() DropMoneyAmount(10000) end) end
    end
end)

------------------------------------------------------------
--  DUPE SYSTEM COOLDOWN & PROCESSES (From ddd.txt)
------------------------------------------------------------
getgenv().AutoDupeCurrentItem = getgenv().AutoDupeCurrentItem or false
local DupeToolName  = nil
local DupeCooldown  = false

local function getPing()
    local ok, r = pcall(function()
        return tonumber(string.match(tostring(LocalPlayer:GetNetworkPing()), '%d+'))
    end)
    if ok and r then return r end
    return 100
end

local function duplicateCurrentItem()
    if DupeCooldown then sendNotification("Dupe", "Cooldown! Please wait."); return end
    DupeCooldown = true

    local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local Backpack  = LocalPlayer:WaitForChild('Backpack')
    local Tool = Character:FindFirstChildOfClass('Tool')
    if not Tool then sendNotification("Dupe","Hold a tool first!"); DupeCooldown=false; return end

    Tool.Parent = Backpack
    task.wait(0.5)

    local ToolName = Tool.Name
    local ToolId
    local delay = 0.25 + ((math.clamp(getPing(), 0, 300) / 300) * 0.03)

    local Connection
    if ReplicatedStorage:FindFirstChild("MarketItems") then
        Connection = ReplicatedStorage.MarketItems.ChildAdded:Connect(function(item)
            if item.Name == ToolName then
                local owner = item:WaitForChild('owner', 2)
                if owner and owner.Value == LocalPlayer.Name then
                    ToolId = item:GetAttribute('SpecialId') or item:GetAttribute('ToolId')
                end
            end
        end)
    end

    pcall(function() ReplicatedStorage.ListWeaponRemote:FireServer(ToolName, 676767) end)
    task.wait(delay)
    pcall(function() ReplicatedStorage.BackpackRemote:InvokeServer('Store', ToolName) end)
    task.wait(3)
    if ToolId then pcall(function() ReplicatedStorage.BuyItemRemote:FireServer(ToolName, 'Remove', ToolId) end) end
    pcall(function() ReplicatedStorage.BackpackRemote:InvokeServer('Grab', ToolName) end)
    task.wait(1)

    local duped = Backpack:FindFirstChild(ToolName)
    if duped and LocalPlayer.Character then duped.Parent = LocalPlayer.Character end
    if Connection then Connection:Disconnect() end
    task.wait(2)
    DupeCooldown = false
end

task.spawn(function()
    while task.wait(0.1) do
        if getgenv().AutoDupeCurrentItem and DupeToolName and DupeToolName ~= "nil" and not DupeCooldown then
            duplicateCurrentItem()
        end
    end
end)

------------------------------------------------------------
--  VEHICLE MODIFICATIONS & BOOSTS LOGIC (From ddd.txt)
------------------------------------------------------------
local CarFly = { Enabled = false, Speed = 150 }
local carFlyConnection = nil

local function StopCarFly()
    CarFly.Enabled = false
    if carFlyConnection then carFlyConnection:Disconnect(); carFlyConnection = nil end
    for _, v in pairs(workspace:GetDescendants()) do
        if v.Name == "SleepyHubCarVelocity" or v.Name == "SleepyHubCarGyro" then v:Destroy() end
    end
end

local function StartCarFly()
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChildWhichIsA("Humanoid")
    if not hum then return end
    local seat = hum.SeatPart
    if not seat or not seat:IsA("VehicleSeat") then
        sendNotification("Car Fly","Sit in a vehicle first!"); return
    end

    local root = (seat.Parent and seat.Parent.PrimaryPart) or seat
    StopCarFly()
    CarFly.Enabled = true

    local bv = Instance.new("BodyVelocity")
    bv.Name = "SleepyHubCarVelocity"
    bv.MaxForce = Vector3.new(1e9,1e9,1e9)
    bv.Parent = root

    local bg = Instance.new("BodyGyro")
    bg.Name = "SleepyHubCarGyro"
    bg.MaxTorque = Vector3.new(1e9,1e9,1e9)
    bg.P = 50000; bg.D = 1000
    bg.CFrame = root.CFrame
    bg.Parent = root

    carFlyConnection = RunService.RenderStepped:Connect(function()
        if not CarFly.Enabled or not seat.Parent then StopCarFly() return end
        local cam = workspace.CurrentCamera
        local moveDir = Vector3.zero
        local look = cam.CFrame.LookVector
        local right = cam.CFrame.RightVector
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + look end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - look end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - right end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + right end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0,1,0) end
        if moveDir.Magnitude > 0 then
            bv.Velocity = moveDir.Unit * CarFly.Speed
            bg.CFrame  = CFrame.new(root.Position, root.Position + moveDir)
        else
            bv.Velocity = Vector3.zero
        end
    end)
end

local function GetNearestCar(hrp)
    local CivCars = workspace:FindFirstChild("CivCars")
    if not CivCars then return nil end
    local nearestCar, nearestDist = nil, math.huge
    for _, car in ipairs(CivCars:GetChildren()) do
        if car:IsA("Model") then
            local seat = car:FindFirstChild("DriveSeat") or car:FindFirstChildWhichIsA("VehicleSeat")
            if seat and not seat.Occupant then
                if not car.PrimaryPart then
                    pcall(function() car.PrimaryPart = car:FindFirstChildWhichIsA("BasePart") end)
                end
                if car.PrimaryPart then
                    local d = (car.PrimaryPart.Position - hrp.Position).Magnitude
                    if d < nearestDist then nearestDist, nearestCar = d, car end
                end
            end
        end
    end
    return nearestCar
end

local VehicleModifications = {
    SpeedEnabled = false, SpeedValue = 10/1000,
    BreakEnabled = false, BreakValue = 50/1000,
    InstantStop  = false,
}

task.spawn(function()
    while task.wait() do
        local char = LocalPlayer.Character
        local hum  = char and char:FindFirstChildWhichIsA("Humanoid")
        local seat = hum and hum.SeatPart
        if seat and seat:IsA("VehicleSeat") then
            if VehicleModifications.SpeedEnabled and UserInputService:IsKeyDown(Enum.KeyCode.W) then
                seat.AssemblyLinearVelocity = seat.AssemblyLinearVelocity *
                    Vector3.new(1+VehicleModifications.SpeedValue, 1, 1+VehicleModifications.SpeedValue)
            end
            if VehicleModifications.BreakEnabled and UserInputService:IsKeyDown(Enum.KeyCode.S) then
                seat.AssemblyLinearVelocity = seat.AssemblyLinearVelocity *
                    Vector3.new(1-VehicleModifications.BreakValue, 1, 1-VehicleModifications.BreakValue)
            end
            if VehicleModifications.InstantStop and UserInputService:IsKeyDown(Enum.KeyCode.V) then
                seat.AssemblyLinearVelocity  = Vector3.zero
                seat.AssemblyAngularVelocity = Vector3.zero
            end
        end
    end
end)

------------------------------------------------------------
--  AUTOFARM FUNCTIONAL SYSTEMS LOGIC (From ddd.txt)
------------------------------------------------------------
local AutoGrabMoney       = false
local AutoCleanMoney      = false
local StudioMoneyNotifyEnabled = false
local StudioMoneyNotifyConnections = {}
local dumpsterIsRunning   = false
local dumpsterTask        = nil
local constructionRunning = false
local constructionThread  = nil
local AutoStealLootbags   = false
local AutoPickupBags      = false
local DisableBloodEffects = false
local BypassLockedCars    = false
local NoKnockback         = false
local FasterRespawn       = false
local RespawnWhereYouDied = false
local KeepToolsOnDeath    = false
local AutoFOnLowHealth    = false
local NoCameraBob         = false

local PawnRemote          = ReplicatedStorage:FindFirstChild("PawnRemote")
local Backpack            = LocalPlayer:WaitForChild("Backpack", 5) or LocalPlayer:FindFirstChild("Backpack")

local function fireRemoteForAllBackpackItems()
    if not Backpack then return end
    for _, item in pairs(Backpack:GetChildren()) do
        pcall(function() if PawnRemote then PawnRemote:FireServer(item.Name) end end)
    end
end
if Backpack then
    Backpack.ChildAdded:Connect(function(child)
        if dumpsterIsRunning then pcall(function() if PawnRemote then PawnRemote:FireServer(child.Name) end end) end
    end)
end

local function startDumpsterAutofarm()
    if dumpsterTask then return end
    dumpsterIsRunning = true
    dumpsterTask = task.spawn(function()
        while dumpsterIsRunning do
            local dumpsters = {}
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Part") and v.Name == "DumpsterPromt" then
                    local p = v:FindFirstChildWhichIsA("ProximityPrompt")
                    if p then table.insert(dumpsters, { part = v, prompt = p }) end
                end
            end
            for _, e in ipairs(dumpsters) do
                if not dumpsterIsRunning then break end
                if e.part and e.prompt and e.part:IsDescendantOf(workspace) then
                    Config:Teleport(CFrame.new(e.part.Position + Vector3.new(0,3,0)))
                    task.wait(0.6)
                    pcall(function() fireproximityprompt(e.prompt) end)
                    task.wait(1)
                end
            end
            task.wait(2)
        end
        dumpsterTask = nil
    end)
    fireRemoteForAllBackpackItems()
end
local function stopDumpsterAutofarm() dumpsterIsRunning = false end

local function startConstructionAutofarm()
    if constructionThread then return end
    constructionRunning = true
    constructionThread = task.spawn(function()
        while constructionRunning do
            task.wait(1)
            pcall(function()
                if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then return end
                if not LocalPlayer.Character:FindFirstChild("Humanoid") or LocalPlayer.Character:FindFirstChild("Humanoid").Health == 0 then return end
                if not LocalPlayer:GetAttribute("WorkingJob") then
                    Config:Teleport(CFrame.new(-1729, 371, -1171))
                    task.wait(0.4)
                    fireproximityprompt(workspace.ConstructionStuff["Start Job"].Prompt)
                    repeat task.wait() until LocalPlayer:GetAttribute("WorkingJob") or not constructionRunning
                end
                if not LocalPlayer.Backpack:FindFirstChild("PlyWood") and not LocalPlayer.Character:FindFirstChild("PlyWood") then
                    Config:Teleport(CFrame.new(-1728, 371, -1178))
                    repeat task.wait() fireproximityprompt(workspace.ConstructionStuff["Grab Wood"].Prompt) until LocalPlayer.Backpack:FindFirstChild("PlyWood") or LocalPlayer.Character:FindFirstChild("PlyWood") or not constructionRunning
                end
                repeat task.wait() until LocalPlayer.Backpack:FindFirstChild("PlyWood") or LocalPlayer.Character:FindFirstChild("PlyWood") or not constructionRunning
                if LocalPlayer.Backpack:FindFirstChild("PlyWood") then
                    LocalPlayer.Character.Humanoid:EquipTool(LocalPlayer.Backpack:FindFirstChild("PlyWood"))
                end
                local targetWood
                for _, v in pairs(workspace.ConstructionStuff:GetDescendants()) do
                    if v:IsA("ProximityPrompt") and v.ActionText == "Wall" then targetWood = v; break end
                end
                if targetWood and targetWood.Parent then
                    Config:Teleport(targetWood.Parent.CFrame + Vector3.new(0,3,0)); task.wait(0.4)
                    fireproximityprompt(targetWood); task.wait(1)
                end
            end)
        end
        constructionThread = nil
    end)
end
local function stopConstructionAutofarm()
    constructionRunning = false
    if constructionThread then task.cancel(constructionThread); constructionThread = nil end
end

-- Prompts instant interaction logic
local promptConnections  = {}
local promptAddedConnection
local function setupPrompt(p, enableInstant)
    if not p or not p:IsA("ProximityPrompt") then return end
    if enableInstant then
        p.HoldDuration        = 0
        p.RequiresLineOfSight = false
        promptConnections[p]  = true
    else
        p.HoldDuration        = 1
        p.RequiresLineOfSight = true
        promptConnections[p]  = nil
    end
end

local function refreshPrompts(enableInstant)
    for prompt, conn in pairs(promptConnections) do
        if typeof(conn) == "RBXScriptConnection" then conn:Disconnect() end
        promptConnections[prompt] = nil
    end
    for _, p in ipairs(workspace:GetDescendants()) do
        if p:IsA("ProximityPrompt") then setupPrompt(p, enableInstant) end
    end
    if promptAddedConnection then promptAddedConnection:Disconnect(); promptAddedConnection = nil end
    if enableInstant then
        promptAddedConnection = workspace.DescendantAdded:Connect(function(d)
            if d:IsA("ProximityPrompt") then setupPrompt(d, true) end
        end)
    end
end

local function watchStudioPayPrompt(studioPayName)
    local sp = workspace:FindFirstChild("StudioPay"); if not sp then return end
    local mf = sp:FindFirstChild("Money"); if not mf then return end
    local part = mf:FindFirstChild(studioPayName); if not part then return end
    local prompt = part:FindFirstChild("Prompt"); if not prompt or not prompt:IsA("ProximityPrompt") then return end
    local c = prompt:GetPropertyChangedSignal("Enabled"):Connect(function()
        if not StudioMoneyNotifyEnabled then return end
        sendNotification(prompt.Enabled and "Studio Money Restocked" or "Studio Money Stolen",
            prompt.Enabled and "Money in studio restocked." or "1 money in studio just got stolen!")
    end)
    table.insert(StudioMoneyNotifyConnections, c)
end
local function cleanupStudioMoneyNotify()
    for _, c in ipairs(StudioMoneyNotifyConnections) do if c and c.Disconnect then c:Disconnect() end end
    StudioMoneyNotifyConnections = {}
end

-- Background action loops (AFK, interaction, bloods)
ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt, selfPlayer)
    if not (prompt and selfPlayer == LocalPlayer and BypassLockedCars) then return end
    local check = prompt.Parent
    while check and check.Parent do
        local seat = check:FindFirstChild("DriveSeat")
        if seat and seat:IsA("VehicleSeat") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            pcall(function() seat:Sit(LocalPlayer.Character.Humanoid) end); break
        end
        check = check.Parent
    end
end)

task.spawn(function()
    while task.wait(0.1) do
        if FasterRespawn and LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") and LocalPlayer.Character.Humanoid:GetState() == Enum.HumanoidStateType.Dead then
            local lc = ReplicatedStorage:FindFirstChild("LoadCharacter")
            if lc and lc:IsA("RemoteEvent") then pcall(function() lc:FireServer() end) end
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if NoCameraBob then
        local char = LocalPlayer.Character; local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum and hum.CameraOffset.Magnitude > 0 then hum.CameraOffset = Vector3.zero end
    end
end)

task.spawn(function()
    while task.wait(0.1) do
        if not AutoFOnLowHealth then break end
        local char = LocalPlayer.Character; local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum and hum.Health > 0 and hum.Health < 10 then
            pcall(function()
                local V = game:GetService("VirtualInputManager")
                V:SendKeyEvent(true, Enum.KeyCode.F, false, nil); task.wait(0.05)
                V:SendKeyEvent(false, Enum.KeyCode.F, false, nil)
            end)
        end
    end
end)

local speedConnection, currentSpeedHumanoid
local function forceSpeed() if currentSpeedHumanoid and currentSpeedHumanoid.Parent then currentSpeedHumanoid.WalkSpeed = 25 end end

local function updateSpeedBoostState(s)
    if s then
        currentSpeedHumanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if not currentSpeedHumanoid then local ch=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait(); currentSpeedHumanoid=ch:WaitForChild("Humanoid") end
        if speedConnection then speedConnection:Disconnect() end
        speedConnection = RunService.Heartbeat:Connect(forceSpeed)
    else
        if speedConnection then speedConnection:Disconnect(); speedConnection=nil end
        if currentSpeedHumanoid then currentSpeedHumanoid.WalkSpeed = 16 end
    end
end

local function updateJumpBoostState(s)
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local hum = char:FindFirstChildOfClass("Humanoid"); if not hum then return end
    hum.JumpPower = s and ((hum.JumpPower or 50)+50) or 50
end

local InfJumpEnabled = false
UserInputService.JumpRequest:Connect(function()
    if InfJumpEnabled then
        local char = LocalPlayer.Character; local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

local noclipConnection
local function enableNoclip()
    if noclipConnection then return end
    noclipConnection = RunService.Stepped:Connect(function()
        if not NoclipEnabled then return end
        local ch = LocalPlayer.Character
        if ch then for _, p in pairs(ch:GetDescendants()) do if p:IsA("BasePart") then p.CanCollide = false end end end
    end)
end
local function disableNoclip()
    if noclipConnection then noclipConnection:Disconnect(); noclipConnection=nil end
    local ch = LocalPlayer.Character
    if ch then for _, p in pairs(ch:GetDescendants()) do if p:IsA("BasePart") then p.CanCollide = true end end end
end

-- Teleport location data setup
local teleportLocations = {
    ["🚗 Car Dealer"]    = CFrame.new(-409.63, 253.41, -1230.14),
    ["🧺 New Laundry"]   = CFrame.new(-1216.24, 253.88, -3969.89),
    ["🍴 New Deli"]      = CFrame.new(-1388.31, 268.14, -3897.86),
    ["💰 New Seller"]    = CFrame.new(-965.27, 260.19, -4244.83),
    ["🏦 New Bank"]      = CFrame.new(-1217.30, 253.88, -3635.04),
    ["🏪 Market"]        = CFrame.new(-405.17, 334.31, -562.63),
    ["🏢 New Penthouse"] = CFrame.new(-1488.06, 476.30, -3747.01),
    ["🎒 Backpack"]      = CFrame.new(-725.64, 253.92, -684.30),
    ["🎙 Studio"]        = CFrame.new(93408.45, 14484.90, 570.14),
    ["💦 Dripstore"]     = CFrame.new(67462.32, 10489.21, 549.60),
    ["🍃 Exotic"]        = CFrame.new(-1519.52, 272.15, -983.73),
    ["🏬 RPT"]           = CFrame.new(-1744.10, 236.95, -596.03),
    ["🏠 Random House"]  = CFrame.new(-1228.93, 261.04, -3758.50),
    ["🔫 Gunshop"]       = CFrame.new(92960.12, 122098.50, 17253.95),
    ["🛒 Dollar General"]= CFrame.new(-413.66, 253.82, -1055.57),
    ["🏥 Hospital"]      = CFrame.new(-1587.46, 254.27, 18.42),
    ["🏰 Mansion"]       = CFrame.new(-789.10, 253.57, 1380.76),
}
local QUICK_TP_KEYS = {
    "🚗 Car Dealer", "🔫 Gunshop", "🎙 Studio", "🍃 Exotic", "🏬 RPT",
    "🎒 Backpack", "🏢 New Penthouse", "💰 New Seller", "🍴 New Deli",
}
local QUICK_TP_LOOKUP = {}
for _, k in ipairs(QUICK_TP_KEYS) do QUICK_TP_LOOKUP[k] = true end

local teleportNames = {}
for n in pairs(teleportLocations) do
    if not QUICK_TP_LOOKUP[n] then table.insert(teleportNames, n) end
end
table.sort(teleportNames)

local function TeleportToCookPot()
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local pos = hrp and hrp.Position or Vector3.zero
    local pot, best = nil, math.huge
    local function check(p) if p and p:IsA("BasePart") then local d=(p.Position-pos).Magnitude; if d<best then best,pot=d,p end end end
    if workspace:FindFirstChild("CookingPots") then
        for _, v in pairs(workspace.CookingPots:GetChildren()) do
            if v:IsA("Model") then check(v:FindFirstChild("CookPart") or v.PrimaryPart or v:FindFirstChildWhichIsA("BasePart")) end
        end
    end
    if pot then Config:Teleport(pot.CFrame + Vector3.new(0,1.5,0)); sendNotification("Teleport","Teleported to nearest Cook Pot!")
    else Config:Teleport(CFrame.new(-198.89, 283.85, -1170.45)); sendNotification("Teleport","Fallback CookPot location.") end
end

-- Money statistic value helpers
local function valueToNumber(o) if not o then return nil end local ok,v=pcall(function() return o.Value end); if ok and tonumber(v) then return tonumber(v) end end
local function findMoneyValue(c, names, recursive)
    if not c then return nil end
    local lk={}; for _, n in ipairs(names) do lk[string.lower(n)] = true end
    for _, ch in ipairs(c:GetChildren()) do if lk[string.lower(ch.Name)] then local v=valueToNumber(ch); if v~=nil then return v end end end
    if recursive then for _, ch in ipairs(c:GetDescendants()) do if lk[string.lower(ch.Name)] then local v=valueToNumber(ch); if v~=nil then return v end end end end
end
local function getMyMoneyValues()
    local cN={"Money","Cash","Wallet","Bank","Coins"}
    local dN={"FilthyStack","FilthyMoney","DirtyMoney","DirtyCash","IllegalMoney","Dirty","Filthy"}
    local stored = LocalPlayer:FindFirstChild("stored") or LocalPlayer:FindFirstChild("Stored")
    local leader = LocalPlayer:FindFirstChild("leaderstats") or LocalPlayer:FindFirstChild("Leaderstats") or LocalPlayer:FindFirstChild("stats")
    local clean = findMoneyValue(stored,{"Money"},false) or findMoneyValue(leader,cN,false) or findMoneyValue(LocalPlayer,cN,false) or findMoneyValue(LocalPlayer,cN,true)
    local dirty = findMoneyValue(stored,{"FilthyStack"},false) or findMoneyValue(stored,dN,false) or findMoneyValue(LocalPlayer,dN,false) or findMoneyValue(LocalPlayer,dN,true)
    return clean, dirty
end
local function getBankValue2()
    local stored = LocalPlayer:FindFirstChild("stored")
    local leader = LocalPlayer:FindFirstChild("leaderstats") or LocalPlayer:FindFirstChild("stats")
    return findMoneyValue(stored,{"Bank"},false) or findMoneyValue(leader,{"Bank"},false) or findMoneyValue(LocalPlayer,{"Bank"},true)
end

local function formatMoney(amount)
    local formatted = tostring(math.floor(tonumber(amount) or 0))
    while true do
        local newFormatted, count = formatted:gsub("^(-?%d+)(%d%d%d)", "%1,%2")
        formatted = newFormatted
        if count == 0 then break end
    end
    return "$" .. formatted
end

-- Dynamic Looting Task Handlers
local function findHouseModels()
    local found = {}
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("Model") or v:IsA("Folder") then
            local n = v.Name:lower()
            if n:find("house") and not n:find("pent") and not n:find("ware") then
                if v:FindFirstChildWhichIsA("ProximityPrompt", true) then table.insert(found, v) end
            end
        end
    end
    table.sort(found, function(a,b) return a.Name:lower() < b.Name:lower() end)
    return found
end

local function lootHouse(hf)
    if not hf then return end
    for _, item in pairs(hf:GetDescendants()) do
        if item:IsA("ProximityPrompt") and item.Enabled then
            local part = item.Parent
            if part and part:IsA("BasePart") then
                Config:Teleport(part.CFrame + Vector3.new(0,3,0)); task.wait(0.2)
                pcall(function() fireproximityprompt(item) end); task.wait(0.25)
            end
        end
    end
end

task.spawn(function()
    while task.wait(0.5) do
        if getgenv().AutoLootStudio then
            local sf = workspace:FindFirstChild("StudioPay") and workspace.StudioPay:FindFirstChild("Money")
            if sf then for _, m in pairs(sf:GetChildren()) do for _, part in pairs(m:GetChildren()) do
                if part:IsA("BasePart") then
                    local p = part:FindFirstChild("Prompt")
                    if p and p:IsA("ProximityPrompt") and p.Enabled then
                        Config:Teleport(part.CFrame+Vector3.new(0,3,0)); task.wait(0.2)
                        pcall(function() fireproximityprompt(p) end); task.wait(0.1)
                    end
                end
            end end end
        end
    end
end)

task.spawn(function()
    while task.wait(1) do
        if getgenv().AutoLootHouse1 or getgenv().AutoLootHouse2 then
            local h = findHouseModels()
            if getgenv().AutoLootHouse1 and h[1] then pcall(function() lootHouse(h[1]) end) end
            if getgenv().AutoLootHouse2 and h[2] then pcall(function() lootHouse(h[2]) end) end
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if getgenv().AutoLootTrash then
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Part") and (v.Name:lower():find("trash") or v.Name:lower():find("dumpster")) then
                    local p = v:FindFirstChildWhichIsA("ProximityPrompt", true)
                    if p and p.Enabled then
                        Config:Teleport(v.CFrame+Vector3.new(0,3,0)); task.wait(0.3)
                        pcall(function() fireproximityprompt(p) end); task.wait(0.5)
                    end
                end
            end
        end
    end
end)

task.spawn(function()
    while task.wait(1) do
        if getgenv().AutoSellLoot then
            pcall(function()
                local pr = ReplicatedStorage:FindFirstChild("PawnRemote")
                if pr then for _, item in pairs(LocalPlayer.Backpack:GetChildren()) do pr:FireServer(item.Name) end end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if getgenv().AutoRobBank then
            pcall(function()
                for _, v in pairs(workspace:GetDescendants()) do
                    if v:IsA("ProximityPrompt") and v.Enabled and (v.ActionText:lower():find("rob") or v.ActionText:lower():find("crack") or v.ActionText:lower():find("drill")) then
                        Config:Teleport(v.Parent.CFrame+Vector3.new(0,3,0)); task.wait(0.2)
                        fireproximityprompt(v); task.wait(0.3)
                    end
                end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if getgenv().AutoRobStore then
            pcall(function()
                local sel = Toggles.TargetStoreSelect and Toggles.TargetStoreSelect.Value or "Store1"
                local sm = workspace:FindFirstChild(sel) or (workspace:FindFirstChild("Stores") and workspace.Stores:FindFirstChild(sel))
                if sm then
                    for _, v in pairs(sm:GetDescendants()) do
                        if v:IsA("ProximityPrompt") and v.Enabled and (v.ActionText:lower():find("rob") or v.ActionText:lower():find("steal") or v.ActionText:lower():find("register")) then
                            Config:Teleport(v.Parent.CFrame+Vector3.new(0,3,0)); task.wait(0.2)
                            fireproximityprompt(v); task.wait(0.3)
                        end
                    end
                end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(1) do
        if AutoStealLootbags then
            local st = workspace:FindFirstChild("Storage")
            if st then
                for _, item in ipairs(st:GetChildren()) do
                    if not AutoStealLootbags then break end
                    local pr = item:FindFirstChild("stealprompt", true)
                    if pr and pr:IsA("ProximityPrompt") and pr.Enabled then
                        local ch = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                        local root = ch:FindFirstChild("HumanoidRootPart"); if not root then break end
                        local old = root.CFrame
                        getgenv().SwimMethod = true; task.wait(0.3)
                        local tp = pr.Parent:IsA("BasePart") and pr.Parent or item:FindFirstChildWhichIsA("BasePart", true)
                        if not tp then break end
                        Config:Teleport(tp.CFrame+Vector3.new(0,2,0)); task.wait(0.3)
                        fireproximityprompt(pr); task.wait(0.3)
                        Config:Teleport(old); task.wait(0.5)
                        getgenv().SwimMethod = false
                    end
                end
            end
        end
    end
end)

task.spawn(function()
    while task.wait(1) do
        if AutoGrabMoney then
            local d = workspace:FindFirstChild("Dollas")
            if d then for _, item in ipairs(d:GetChildren()) do
                if not AutoGrabMoney then break end
                if (item.Name=="DeadMoney" or item.Name=="Money") and item:IsA("BasePart") then
                    local pr = item:FindFirstChildWhichIsA("ProximityPrompt", true)
                    if pr and pr.Enabled then
                        local ch = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                        local root = ch:FindFirstChild("HumanoidRootPart"); if not root then break end
                        local old = root.CFrame
                        getgenv().SwimMethod = true; task.wait(0.3)
                        Config:Teleport(item.CFrame+Vector3.new(0,2,0)); task.wait(0.3)
                        fireproximityprompt(pr); task.wait(0.3)
                        Config:Teleport(old); task.wait(0.5); getgenv().SwimMethod = false
                    end
                end
            end end
        end
        if AutoPickupBags and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local st = workspace:FindFirstChild("Storage")
            if st then
                for _, v in next, st:GetChildren() do
                    if v:IsA("MeshPart") then
                        if v:FindFirstChild("PlayerName") and v.PlayerName.Value == LocalPlayer.Name then break end
                        if (v.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude < 5 then
                            local p = v:FindFirstChild("stealprompt")
                            if p and p:IsA("ProximityPrompt") then pcall(function() fireproximityprompt(p) end) end
                        end
                    end
                end
            end
        end
        if DisableBloodEffects and LocalPlayer:FindFirstChild("PlayerGui") and LocalPlayer.PlayerGui:FindFirstChild("BloodGui") then
            LocalPlayer.PlayerGui.BloodGui.Enabled = false
        end
    end
end)

task.spawn(function()
    local VU = game:GetService("VirtualUser")
    while true do
        task.wait(1200)
        if getgenv().AntiAFKEnabled then pcall(function() VU:CaptureController(); VU:ClickButton2(Vector2.new()) end) end
    end
end)

------------------------------------------------------------
--  SAFE/DUPE MODULE LOGIC & DRIVERS (From ddd.txt)
------------------------------------------------------------
local dupeBlacklist = {
    ["Fists"]=true, ["Fist"]=true, ["Phone"]=true, ["Car Keys"]=true,
    ["Car keys"]=true, ["Bandage"]=true, ["762s"]=true, ["556s"]=true,
    ["Extended"]=true, ["T shirt"]=true, ["Combat"]=true, ["Weight"]=true,
    ["Shiesty"]=true, ["Lemonade"]=true,
}

local function getSafeFolder()
    local hds = workspace:FindFirstChild("HomeDoorSS")
    local hd  = hds and hds:FindFirstChild("HomeDoor")
    return hd and hd:FindFirstChild("Safe")
end

local function GetSafeItems()
    local items = {}
    local invData = LocalPlayer:FindFirstChild("InvData")
    if invData then for _, c in ipairs(invData:GetChildren()) do table.insert(items, c.Name) end end
    table.sort(items); return items
end

local function GetAllBackpackItems()
    local items = {}
    local char = LocalPlayer.Character
    local bp   = LocalPlayer:FindFirstChild("Backpack")
    if char then for _, t in pairs(char:GetChildren()) do
        if t:IsA("Tool") and not dupeBlacklist[t.Name] and not table.find(items, t.Name) then table.insert(items, t.Name) end
    end end
    if bp then for _, t in pairs(bp:GetChildren()) do
        if t:IsA("Tool") and not dupeBlacklist[t.Name] and not table.find(items, t.Name) then table.insert(items, t.Name) end
    end end
    table.sort(items); return items
end

local function GetLockedTools()
    local tools = {}
    if LocalPlayer:FindFirstChild("Backpack") then
        for _, t in pairs(LocalPlayer.Backpack:GetChildren()) do
            if t:IsA("Tool") and not dupeBlacklist[t.Name] then table.insert(tools, t.Name) end
        end
    end
    return tools
end

local function FirePromptsInArea(range, nameFilter)
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return end
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("ProximityPrompt") then
            local pPos = (v.Parent:IsA("Model") and v.Parent:GetPivot().Position)
                      or (v.Parent:IsA("BasePart") and v.Parent.Position)
            if pPos and (pPos - root.Position).Magnitude < range then
                if not nameFilter
                or (v.Parent.Name:lower():find(nameFilter:lower()) or v.Name:lower():find(nameFilter:lower())) then
                    pcall(function() fireproximityprompt(v) end)
                end
            end
        end
    end
end

local function TakeFromSafe(itemName)
    local invRemote = ReplicatedStorage:FindFirstChild("Inventory")
    local SafeFolder = getSafeFolder()
    if not SafeFolder then sendNotification("Safe","Safe not found in this server."); return false end
    if not invRemote then sendNotification("Safe","InventoryRemote missing."); return false end
    local safePart = SafeFolder:FindFirstChild("Union") or SafeFolder:FindFirstChildWhichIsA("BasePart")
    if not safePart then return false end
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return false end
    local originalCF = root.CFrame
    pcall(function()
        Config:Teleport(safePart.CFrame * CFrame.new(0, 2, 0))
        task.wait(0.3)
        invRemote:FireServer("Change", itemName, "Inv", SafeFolder)
        task.wait(0.5)
        Config:Teleport(originalCF)
        task.wait(0.5)
    end)
    local got = LocalPlayer.Backpack:FindFirstChild(itemName)
              or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild(itemName))
    sendNotification("Safe", got and ("Took " .. itemName) or ("Failed: " .. itemName))
    return got ~= nil
end

local function StoreHoldingItem()
    local invRemote = ReplicatedStorage:FindFirstChild("Inventory")
    local backpackRemoteSafe = ReplicatedStorage:FindFirstChild("BackpackRemote")
    local char = LocalPlayer.Character; if not char then return end
    local tool = char:FindFirstChildOfClass("Tool")
    if not tool then sendNotification("Safe","Hold an item first!"); return end
    local SafeFolder = getSafeFolder()
    if not SafeFolder then sendNotification("Safe","Safe not found."); return end
    local safePart = SafeFolder:FindFirstChild("Union") or SafeFolder:FindFirstChildWhichIsA("BasePart")
    local root = char:FindFirstChild("HumanoidRootPart")
    if not safePart or not root then return end
    local originalCF = root.CFrame
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then hum:UnequipTools() end
    task.wait(0.2)
    Config:Teleport(safePart.CFrame * CFrame.new(0, 2, 0))
    task.wait(0.3)
    pcall(function()
        if backpackRemoteSafe then backpackRemoteSafe:InvokeServer("Store", tool.Name) end
        if invRemote then invRemote:FireServer("Change", tool.Name, "Backpack", SafeFolder) end
    end)
    task.wait(0.5)
    Config:Teleport(originalCF)
    sendNotification("Safe","Stored " .. tool.Name)
end

local function SafeDupeOnce(itemName, returnCFrame)
    local invRemote = ReplicatedStorage:FindFirstChild("Inventory")
    local backpackRemoteSafe = ReplicatedStorage:FindFirstChild("BackpackRemote")
    local SafeFolder = getSafeFolder()
    if not SafeFolder then return false end
    local safePart = SafeFolder:FindFirstChild("Union") or SafeFolder:FindFirstChildWhichIsA("BasePart")
    if not safePart then return false end
    pcall(function()
        Config:Teleport(safePart.CFrame * CFrame.new(0, 2, 0))
        task.wait(0.3)
        task.spawn(function() if backpackRemoteSafe then backpackRemoteSafe:InvokeServer("Store", itemName) end end)
        task.spawn(function() if invRemote then invRemote:FireServer("Change", itemName, "Backpack", SafeFolder) end end)
        task.wait(0.6)
        Config:Teleport(returnCFrame)
        task.wait(1.2)
        if backpackRemoteSafe then backpackRemoteSafe:InvokeServer("Grab", itemName) end
        task.wait(0.5)
    end)
    return true
end

local autoDupeActive = false
local dupeSpeed      = 15
local dupeCounter    = 0
local selectedDupeItem = nil

local function StartSafeDupe(statusLabelFn)
    local backpackRemoteSafe = ReplicatedStorage:FindFirstChild("BackpackRemote")
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then sendNotification("Dupe","No character!"); return end
    local origLoc = root.CFrame

    autoDupeActive = true
    sendNotification("Dupe","Starting safe dupe...")

    task.spawn(function()
        statusLabelFn("RENTING...")
        Config:Teleport(CFrame.new(-1443, 256, 2137)); task.wait(0.7); FirePromptsInArea(15)
        statusLabelFn("OPENING SAFE...")
        Config:Teleport(CFrame.new(-1473, 256, 2134)); task.wait(0.7); FirePromptsInArea(15, "Safe")
        statusLabelFn("RETURNING...")
        Config:Teleport(origLoc); task.wait(0.5)

        local targets
        if selectedDupeItem then
            targets = { selectedDupeItem }
            pcall(function()
                if backpackRemoteSafe then backpackRemoteSafe:InvokeServer("Grab", selectedDupeItem) end
                task.wait(0.5)
                local hum = char and char:FindFirstChildOfClass("Humanoid")
                if hum then hum:UnequipTools() end
            end)
        else
            targets = GetLockedTools()
            if #targets == 0 then
                statusLabelFn("NO TOOLS")
                autoDupeActive = false
                sendNotification("Dupe","No tools in backpack.")
                return
            end
        end

        for i = 1, dupeSpeed do
            if not autoDupeActive then break end
            local item = targets[((i - 1) % #targets) + 1]
            statusLabelFn(string.format("DUPE %d/%d: %s", i, dupeSpeed, tostring(item):upper()))
            SafeDupeOnce(item, origLoc)
            dupeCounter = dupeCounter + 1
            task.wait(0.2)
        end

        autoDupeActive = false
        statusLabelFn("DONE ✓  (total duped: " .. dupeCounter .. ")")
        sendNotification("Dupe", dupeSpeed .. " cycles done.")
    end)
end

local function StopSafeDupe(statusLabelFn)
    autoDupeActive = false
    statusLabelFn("STOPPED")
    sendNotification("Dupe","Stopped.")
end

local function SafeTP(statusLabelFn)
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then sendNotification("Safe TP","No character!"); return end
    sendNotification("Safe TP","Teleporting to safe...")
    task.spawn(function()
        statusLabelFn("RENTING...")
        Config:Teleport(CFrame.new(-1443, 256, 2137)); task.wait(0.7); FirePromptsInArea(15)
        statusLabelFn("TP TO SAFE...")
        Config:Teleport(CFrame.new(-1473, 256, 2134)); task.wait(0.7); FirePromptsInArea(15, "Safe")
        statusLabelFn("SAFE OPENED ✓")
        sendNotification("Safe TP","Safe opened, you're there.")
        task.wait(2); statusLabelFn("IDLE")
    end)
end

local autoDropEnabled = false
local function ToggleAutoDrop(state, statusLabelFn)
    local dropRemote = ReplicatedStorage:FindFirstChild("DropItemRemote")
    autoDropEnabled = state
    sendNotification("Auto Drop", state and "Started" or "Stopped")
    if state then
        task.spawn(function()
            statusLabelFn("DROPPING...")
            while autoDropEnabled do
                local bp = LocalPlayer:FindFirstChild("Backpack")
                local ch = LocalPlayer.Character
                if not bp or not ch then task.wait(0.5);  end
                local found = false
                for _, item in ipairs(bp:GetChildren()) do
                    if not autoDropEnabled then break end
                    if item:IsA("Tool") and not dupeBlacklist[item.Name] then
                        found = true
                        local hum = ch:FindFirstChildOfClass("Humanoid")
                        if hum then
                            hum:EquipTool(item)
                            local dt = tick()
                            repeat
                                if dropRemote then
                                    pcall(function() dropRemote:FireServer(item.Name) end)
                                    pcall(function() dropRemote:FireServer(item) end)
                                    pcall(function() dropRemote:FireServer() end)
                                end
                                task.wait(0.2)
                            until not item:IsDescendantOf(ch) or not autoDropEnabled or (tick() - dt > 3)
                            task.wait(0.1)
                        end
                    end
                end
                if not found then statusLabelFn("WAITING..."); task.wait(1) end
                task.wait(0.1)
            end
            statusLabelFn("IDLE")
        end)
    end
end

local function loadSafeTabFunctions()
    -- ===== SAFE (LEFT) =====
    local SafeSection = Tabs["Safe/Dupe"]:AddLeftGroupbox("Safe")

    local safeItems = GetSafeItems()
    if #safeItems == 0 then safeItems = { "(empty - press Refresh)" } end
    local selectedSafeItem = safeItems[1]

    local safeDropdown = SafeSection:AddDropdown("SafeSelectedItem", {
        Text = "Select Safe Item",
        Values = safeItems,
        Default = selectedSafeItem,
        Callback = function(v) selectedSafeItem = normalizeDropdownValue(v) end,
    })

    SafeSection:AddButton("🔄 Refresh Safe List", function()
        local items = GetSafeItems()
        if #items == 0 then items = { "(empty)" } end
        if safeDropdown then
            pcall(function() safeDropdown:SetValues(items) end)
        end
        selectedSafeItem = items[1]
        sendNotification("Safe", "Refreshed: " .. #items .. " items")
    end)

    SafeSection:AddButton("📦 Take Selected", function()
        if not selectedSafeItem or selectedSafeItem:find("empty") then
            sendNotification("Safe","Select a valid item first!"); return
        end
        task.spawn(function() TakeFromSafe(selectedSafeItem) end)
    end)

    SafeSection:AddButton("📦 Take All", function()
        local items = GetSafeItems()
        if #items == 0 then sendNotification("Safe","Safe is empty."); return end
        task.spawn(function()
            sendNotification("Safe","Taking all "..#items.." items...")
            local taken = 0
            for _, n in ipairs(items) do
                if TakeFromSafe(n) then taken = taken + 1 end
                task.wait(0.5)
            end
            sendNotification("Safe", "Took " .. taken .. "/" .. #items .. " items.")
        end)
    end)

    SafeSection:AddButton("💼 Store Holding Item", function() task.spawn(StoreHoldingItem) end)

    -- ===== SAFE DUPE (LEFT) =====
    local DupeSection = Tabs["Safe/Dupe"]:AddLeftGroupbox("Safe Dupe")

    local dupeItems = GetAllBackpackItems()
    table.insert(dupeItems, 1, "(All locked tools)")

    local dupeDropdown = DupeSection:AddDropdown("DupeSelectedItem", {
        Text = "Item to Dupe",
        Values = dupeItems,
        Default = dupeItems[1],
        Callback = function(v)
            local pick = normalizeDropdownValue(v)
            selectedDupeItem = (pick == "(All locked tools)") and nil or pick
        end,
    })

    DupeSection:AddButton("🔄 Refresh Backpack", function()
        local items = GetAllBackpackItems()
        table.insert(items, 1, "(All locked tools)")
        if dupeDropdown then
            pcall(function() dupeDropdown:SetValues(items) end)
        end
        selectedDupeItem = nil
        sendNotification("Dupe","Backpack refreshed: " .. (#items - 1) .. " tools")
    end)

    DupeSection:AddSlider("DupeCycles", {
        Text = "Dupe Cycles", Min = 1, Max = 50, Default = 15, Rounding = 0,
        Callback = function(v) dupeSpeed = math.floor(v) end
    })

    local dupeStatusLabel
    local function setDupeStatus(t) pcall(function() dupeStatusLabel:SetText("Status: " .. t) end) end

    DupeSection:AddButton("▶ Start Safe Dupe", function()
        if autoDupeActive then StopSafeDupe(setDupeStatus)
        else StartSafeDupe(setDupeStatus) end
    end)

    DupeSection:AddButton("■ Stop Safe Dupe", function() StopSafeDupe(setDupeStatus) end)
    DupeSection:AddButton("⚡ Safe TP (open safe)", function() SafeTP(setDupeStatus) end)
    DupeSection:AddToggle("SafeAutoDrop", {
        Text = "Auto Drop Tools",
        Default = false,
        Callback = function(s) ToggleAutoDrop(s, setDupeStatus) end
    })

    -- ===== INFO / STATUS (RIGHT) =====
    local SafeInfo = Tabs["Safe/Dupe"]:AddRightGroupbox("Info / Status")
    local safeStatusLabel  = SafeInfo:AddLabel("Safe Status: Ready")
    local itemCountLabel   = SafeInfo:AddLabel("Items in Safe: ?")
    local dupeCounterLabel = SafeInfo:AddLabel("Total Duped: 0")
    dupeStatusLabel        = SafeInfo:AddLabel("Status: IDLE")

    task.spawn(function()
        while task.wait(1) do
            local n = #GetSafeItems()
            pcall(function() itemCountLabel:SetText("Items in Safe: " .. n) end)
            local SafeFolder = getSafeFolder()
            pcall(function() safeStatusLabel:SetText("Safe Status: " .. (SafeFolder and "Found ✅" or "Not Found ❌")) end)
            pcall(function() dupeCounterLabel:SetText("Total Duped: " .. dupeCounter) end)
        end
    end)

    -- ===== INSTRUCTIONS (RIGHT) =====
    local HelpSection = Tabs["Safe/Dupe"]:AddRightGroupbox("📖 Instructions")

    HelpSection:AddButton("ℹ️ How To Use Safe", function()
        sendNotification("Safe Help",
            "1) Click '🔄 Refresh Safe List' to load items\n"
         .. "2) Pick an item from the dropdown\n"
         .. "3) Press '📦 Take Selected' (or '📦 Take All')\n"
         .. "4) To deposit: hold a tool → press '💼 Store Holding Item'")
    end)

    HelpSection:AddButton("ℹ️ How To Use Safe Dupe", function()
        sendNotification("Safe Dupe Help",
            "1) Hold or have the item in your backpack\n"
         .. "2) Press '🔄 Refresh Backpack'\n"
         .. "3) Pick an item from the dropdown (or leave '(All locked tools)' to dupe everything)\n"
         .. "4) Set Dupe Cycles (1-50)\n"
         .. "5) Press '▶ Start Safe Dupe'\n"
         .. "6) Watch the Status label and wait for 'DONE ✓'")
    end)

    HelpSection:AddButton("ℹ️ How To Use Safe TP", function()
        sendNotification("Safe TP Help",
            "Teleports you to your house, rents it,\n"
             .. "and opens the safe so you can manage items.\n"
             .. "Must be done at least once before duping.")
    end)

    HelpSection:AddButton("ℹ️ How To Use Auto Drop", function()
        sendNotification("Auto Drop Help",
            "Equips and drops every tool in your\n"
         .. "backpack one by one (skipping fists/phone/etc.)\n"
         .. "Toggle it ON to start, OFF to stop.\n"
         .. "Great for offloading duped guns to a friend.")
    end)

    HelpSection:AddButton("⚠️ IMPORTANT NOTES", function()
        sendNotification("Important",
            "• Only works in Da Hood-style games\n"
         .. "• Need to OWN a house (or rent one)\n"
         .. "• Don't switch items while duping\n"
         .. "• Higher cycles = more dupes but more detection risk\n"
         .. "• If a dupe fails, try Safe TP first then retry")
    end)
end

------------------------------------------------------------
--  PLAYER TAB (Fully Merged & Configured from ddd.txt)
------------------------------------------------------------

-- Left Side Groups
local BlueGunModsSection = Tabs.Player:AddLeftGroupbox("Weapon Modifications")

local function bindToggle(displayName, key, flag)
    BlueGunModsSection:AddToggle(flag, {
        Text = displayName,
        Default = false,
        Callback = function(s)
            WeaponMods[key] = s
            if key == "InfiniteDamage" then WeaponMods.DamageAmplified = s end
            pcall(applyAllWeaponMods)
        end
    })
end

bindToggle("Infinite Ammo",    "InfiniteAmmo",      "BlueWM_InfiniteAmmo")
bindToggle("Infinite Clips",   "InfiniteClips",     "BlueWM_InfiniteClips")
bindToggle("Infinite Damage",  "InfiniteDamage",    "BlueWM_InfiniteDamage")
bindToggle("Instant Reload",   "InstantReload",     "BlueWM_InstantReload")
bindToggle("Instant Equip",    "InstantEquip",      "BlueWM_InstantEquip")
bindToggle("80k Bullets",      "Bullets80k",        "BlueWM_80kBullets")
BlueGunModsSection:AddLabel("⚠ 80k bullet guns cannot be passed to others")
bindToggle("Fully Automatic",  "Automatic",         "BlueWM_Automatic")
bindToggle("Disable Jamming",  "DisableJamming",    "BlueWM_DisableJamming")
bindToggle("Modify Recoil Value", "ModifyRecoilValue", "BlueWM_ModifyRecoil")
bindToggle("Modify Spread Value", "ModifySpreadValue", "BlueWM_ModifySpread")
bindToggle("Modify Fire Rate",  "ModifyFireRate",    "BlueWM_ModifyFireRate")
BlueGunModsSection:AddButton("Force 80k Bullets", MianiForce80kBullets)


local MianiEmoteSection = Tabs.Player:AddLeftGroupbox("Emotes")
MianiEmoteSection:AddDropdown("MianiSelectedEmote", {
    Text = "Select Emote",
    Values = MianiEmoteNames,
    Default = "Take The L",
    Callback = function(v) 
        if v and MianiEmotes[v] then MianiSelectedEmoteName = v end 
    end
})
MianiEmoteSection:AddButton("Play Selected Emote", function()
    local id = MianiEmotes[MianiSelectedEmoteName]
    if id then MianiPlayEmoteById(id, MianiSelectedEmoteName) end
end)
MianiEmoteSection:AddButton("Stop Emote", function() 
    MianiStopAllTracks()
    sendNotification("Emotes","Stopped emote") 
end)


local MianiEmoteSettingsSection = Tabs.Player:AddLeftGroupbox("Emote Settings")
MianiEmoteSettingsSection:AddToggle("MianiEmoteLoop", {
    Text = "Loop Emote",
    Default = true,
    Callback = function(s)
        MianiEmoteSettings.Loop = s
        if MianiCurrentEmoteTrack then pcall(function() MianiCurrentEmoteTrack.Looped = s end) end
    end
})


local GonModsSection = Tabs.Player:AddLeftGroupbox("Gon Mods")
local fireBallsEnabled = false
local function setBulletVisuals(t)
    if typeof(t) ~= "table" or not rawget(t, "BulletSpeed") then return end
    rawset(t, "BulletSpeed", 150)
    rawset(t, "DropGravity", 0.3)
    rawset(t, "BulletParticleEnaled", true)
    rawset(t, "BulletParticleColor", Color3.new(1, 0, 0))
    rawset(t, "BulletParticleSize", NumberSequence.new({
        NumberSequenceKeypoint.new(0, 9e9),
        NumberSequenceKeypoint.new(1, 1)
    }))
end

GonModsSection:AddToggle("GonRainbowBullet", {
    Text = "🔥 Fire Balls",
    Default = false,
    Callback = function(state)
        fireBallsEnabled = state
        if state then
            for _, v in getgc(true) do
                if typeof(v) == "table" then
                    pcall(function()
                        setBulletVisuals(v)
                        for _, k in ipairs({7, 8}) do
                            setBulletVisuals(rawget(v, k))
                        end
                    end)
                end
            end
            sendNotification("Gon Mods", "Fire Balls enabled!")
        else
            sendNotification("Gon Mods", "Fire Balls disabled (Re-equip gun to revert)!")
        end
    end
})


-- Right Side Groups
local BlueGunSettingsSection = Tabs.Player:AddRightGroupbox("Weapon Modification Settings")
BlueGunSettingsSection:AddLabel("100% is default percentage.")
BlueGunSettingsSection:AddSlider("BlueWM_FireRatePercent", {
    Text = "Fire-Rate Percentage",
    Min = 1, Max = 100, Default = 50, Rounding = 0,
    Callback = function(v) WeaponMods.FireRateSpeed = v end
})
BlueGunSettingsSection:AddSlider("BlueWM_SpreadPercent", {
    Text = "Spread Percentage",
    Min = 1, Max = 100, Default = 50, Rounding = 0,
    Callback = function(v) WeaponMods.SpreadPercentage = v end
})
BlueGunSettingsSection:AddSlider("BlueWM_RecoilPercent", {
    Text = "Recoil Percentage",
    Min = 1, Max = 100, Default = 50, Rounding = 0,
    Callback = function(v) WeaponMods.RecoilPercentage = v end
})
BlueGunSettingsSection:AddSlider("BlueWM_ReloadSpeed", {
    Text = "Reload Speed",
    Min = 0.01, Max = 1, Default = 0.2, Rounding = 2,
    Callback = function(v) WeaponMods.ReloadSpeed = v; pcall(applyAllWeaponMods) end
})
BlueGunSettingsSection:AddSlider("BlueWM_EquipSpeed", {
    Text = "Equip Speed",
    Min = 0.01, Max = 1, Default = 0.2, Rounding = 2,
    Callback = function(v) WeaponMods.EquipSpeed = v; pcall(applyAllWeaponMods) end
})


local BlueGunColorSection = Tabs.Player:AddRightGroupbox("Gun Color")
BlueGunColorSection:AddToggle("BlueWM_GunColor", {
    Text = "Enable Gun Color",
    Default = false,
    Callback = function(s)
        GunChams = s; if not s then MianiRestoreGunColor() end
    end
})
BlueGunColorSection:AddToggle("BlueWM_RainbowGun", {
    Text = "Rainbow Gun Color",
    Default = false,
    Callback = function(s) RainbowGun = s end
})
BlueGunColorSection:AddDropdown("BlueWM_GunColorSelect", {
    Text = "Gun Color",
    Values = {"Green","Purple","Red","Blue","Pink","Cyan","Yellow","White"},
    Default = "Green",
    Callback = function(v)
        local colors = {
            Green=Color3.fromRGB(0,200,0), Purple=Color3.fromRGB(143,0,255), Red=Color3.fromRGB(255,0,0),
            Blue=Color3.fromRGB(0,120,255), Pink=Color3.fromRGB(255,80,180), Cyan=Color3.fromRGB(0,255,255),
            Yellow=Color3.fromRGB(255,255,0), White=Color3.fromRGB(255,255,255),
        }
        GunChamsColor = colors[tostring(v)] or Color3.fromRGB(0,200,0)
    end
})


------------------------------------------------------------
--  MAIN TAB (Fully Restored & Matched to ddd.txt)
------------------------------------------------------------

-- Left Columns
local TargetGroup = Tabs.Main:AddLeftGroupbox("Target")

TargetPlayerNames = getTargetPlayerList()
TargetUtilities.SelectedPlayer = TargetPlayerNames[1] or ""

local TargetDropdownObject = TargetGroup:AddDropdown("MainTargetPlayer", {
    Text = "Select Player",
    Values = TargetPlayerNames,
    Default = TargetUtilities.SelectedPlayer,
    Callback = function(v)
        TargetUtilities.SelectedPlayer = normalizeDropdownValue(v)
    end
})

TargetGroup:AddButton("Refresh Player List", function()
    TargetPlayerNames = getTargetPlayerList()
    if TargetDropdownObject then
        pcall(function() TargetDropdownObject:SetValues(TargetPlayerNames) end)
    end
    sendNotification("Target", "Player list refreshed: " .. #TargetPlayerNames)
end)

TargetGroup:AddButton("🤝 Pass to Nearest Player", function()
    pcall(function() ReplicatedStorage.DropRemote:FireServer("Drop"); sendNotification("Pass", "Passed to nearest player.") end)
end)

TargetGroup:AddToggle("BoxHubAutoPassTo", {
    Text = "🤝 Auto Pass to Nearest (loop)",
    Default = false,
    Callback = function(v)
        getgenv().BoxHubAutoPassTo = v; notifyToggle("Auto Pass to Nearest", v)
        if v and not getgenv()._BoxHubAutoPassToRunning then
            getgenv()._BoxHubAutoPassToRunning = true
            task.spawn(function()
                while getgenv().BoxHubAutoPassTo do
                    pcall(function() ReplicatedStorage.DropRemote:FireServer("Drop") end)
                    task.wait(0.1)
                end
                getgenv()._BoxHubAutoPassToRunning = false
            end)
        end
    end
})

TargetGroup:AddToggle("BoxHubAutoDropGuns", {
    Text = "🔫 Auto Drop Guns",
    Default = false,
    Callback = function(v)
        getgenv().BoxHubAutoDropGuns = v; notifyToggle("Auto Drop Guns", v)
        if v and not getgenv()._BoxHubAutoDropGunsRunning then
            getgenv()._BoxHubAutoDropGunsRunning = true
            task.spawn(function()
                while getgenv().BoxHubAutoDropGuns do
                    pcall(function()
                        local lp = LocalPlayer
                        local char = lp.Character
                        local bp = lp:FindFirstChild("Backpack")
                        if char and bp then
                            for _, Gun in ipairs(bp:GetChildren()) do
                                if Gun:IsA("Tool") and Gun:FindFirstChild("GunScript_Server") then
                                    local hum = char:FindFirstChildOfClass("Humanoid")
                                    if hum then hum:EquipTool(Gun) end
                                    local d = ReplicatedStorage:FindFirstChild("DropItemRemote")
                                    if d then d:FireServer(Gun) end
                                    break
                                end
                            end
                        end
                    end)
                    task.wait(math.random(10,13)/10)
                end
                getgenv()._BoxHubAutoDropGunsRunning = false
            end)
        end
    end
})

TargetGroup:AddToggle("TargetSpectatePlayer", {
    Text = "Spectate Player",
    Default = false,
    Callback = function(s) TargetUtilities.SpectatePlayer = s; notifyToggle("Spectate Player", s) end
})

TargetGroup:AddToggle("TargetBringNearest", {
    Text = "Bring Nearest Player",
    Default = false,
    Callback = function(s) TargetUtilities.BringingNearestPlayer = s; notifyToggle("Bring Nearest Player", s) end
})

TargetGroup:AddToggle("TargetBringPlayer", {
    Text = "Bring Player",
    Default = false,
    Callback = function(s) TargetUtilities.BringingPlayer = s; notifyToggle("Bring Player", s) end
})

TargetGroup:AddToggle("TargetAutoRagdollGun", {
    Text = "Auto Ragdoll Player - Gun",
    Default = false,
    Callback = function(s) TargetUtilities.AutoRagdoll = s; notifyToggle("Auto Ragdoll", s) end
})

TargetGroup:AddButton("Teleport To Player", function()
    local t = getSelectedTargetPlayer()
    if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") then
        Config:Teleport(t.Character.HumanoidRootPart.CFrame * CFrame.new(3,0,0))
    else
        sendNotification("Target", "No valid target selected.")
    end
end)

TargetGroup:AddButton("God Player - Hold Gun", function()
    local t = getSelectedTargetPlayer()
    if t then targetGunRemote(t.Name, "HumanoidRootPart", math.huge) else sendNotification("Target", "No valid target selected.") end
end)


local DupeGroup = Tabs.Main:AddLeftGroupbox("Dupe")
DupeGroup:AddButton("How To Dupe (Instructions)", function()
    sendNotification("How To Dupe", "Hold the gun/tool → click Dupe (Single).\nFor Auto Dupe: hold the item, toggle Auto Dupe ON, do not switch items.")
end)

DupeGroup:AddButton("Dupe (Single)", function()
    local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
    if tool then
        DupeToolName = tool.Name; sendNotification("Dupe", "Duping: " .. tool.Name); duplicateCurrentItem()
    else
        sendNotification("Dupe", "Hold a tool first!")
    end
end)

DupeGroup:AddToggle("AutoDupeCurrentItem", {
    Text = "Start Auto Dupe",
    Default = false,
    Callback = function(state)
        getgenv().AutoDupeCurrentItem = state
        if state then
            local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
            if tool then
                DupeToolName = tool.Name; sendNotification("Auto Dupe", "Running: " .. DupeToolName)
            else
                getgenv().AutoDupeCurrentItem = false; sendNotification("Auto Dupe", "Hold a tool first!")
            end
        else
            sendNotification("Auto Dupe", "Stopped.")
        end
    end
})


local MoneyGroup = Tabs.Main:AddLeftGroupbox("Money")
getgenv().MiamiFootballOpponent = getgenv().MiamiFootballOpponent or "Random"
MoneyGroup:AddDropdown("MiamiFootballOpponent", {
    Text = "Football Opponent",
    Values = {"Random","NOTRE DAME","FLORIDA STATE","FLORIDA","GEORGIA","ALABAMA","LSU","CLEMSON","OHIO STATE","MICHIGAN","OREGON","TEXAS","OKLAHOMA","USC","PENN STATE","TENNESSEE","TEXAS A&M","WASHINGTON","UTAH","AUBURN","WISCONSIN","IOWA","NORTH CAROLINA","LOUISVILLE","VIRGINIA TECH","BAYLOR","TCU","NEBRASKA","COLORADO","STANFORD","UCLA"},
    Default = "Random",
    Callback = function(v)
        local pick = normalizeDropdownValue(v)
        getgenv().MiamiFootballOpponent = (pick == "Random") and nil or pick
        sendNotification("Football", "Opponent: " .. tostring(pick))
    end
})

MoneyGroup:AddButton("Manual [Inf Money]", function()
    sendNotification("Inf Money", "Triggered.")
    local finishFootballGame = StartMiamiFootballGameOverlay()
    task.spawn(function()
        local ok, err = pcall(InfMoneyHoldCupz)
        if finishFootballGame then finishFootballGame(ok) end
        if not ok then warn(err); sendNotification("Inf Money", "Stopped: " .. tostring(err)) end
    end)
end)

MoneyGroup:AddButton("Generate Max Illegal Money", function()
    sendNotification("Illegal Money", "Generating max...")
    local finishFootballGame = StartMiamiFootballGameOverlay()
    task.spawn(function()
        local ok, err = pcall(GenerateMaxIllegalMoney)
        if finishFootballGame then finishFootballGame(ok) end
        if not ok then warn(err); sendNotification("Illegal Money", "Stopped: " .. tostring(err)) end
    end)
end)

MoneyGroup:AddButton("Teleport [COOK POT]", function() TeleportToCookPot() end)
MoneyGroup:AddButton("Buy Ice-Fruit [ITEMS]", function() sendNotification("Buy Items", "Buying Ice-Fruit items..."); SetupInfiniteMoney() end)
MoneyGroup:AddButton("Instructions: Inf Money", function() sendNotification("WARNING", "Need ≥ $3,000 cash first. Cook Ice-Fruit, hold cup, then press Manual Inf Money.") end)
MoneyGroup:AddButton("Clean All Filthy Money", function() CleanAllFilthyMoney() end)
MoneyGroup:AddToggle("AutoDrop10k", {
    Text = "Auto Drop 10k",
    Default = false,
    Callback = function(s) getgenv().MoneyDropEnabled = s end
})


-- Right Columns
local QuickOutfitGroup = Tabs.Main:AddRightGroupbox("Quick Outfits")
QuickOutfitGroup:AddButton("Spiderman", function()
    applyOutfit("Spiderman", {{"Buy","Shirts","Spiderman"},{"Wear","Shirts","Spiderman"},{"Buy","Pants","Spiderman"},{"Wear","Pants","Spiderman"},{"Buy","Shiestys","RedShiesty"},{"Wear","Shiestys","RedShiesty"}})
end)
QuickOutfitGroup:AddButton("All Black", function()
    applyOutfit("All Black", {{"Buy","Shirts","Black"},{"Wear","Shirts","Black"},{"Buy","Pants","Black"},{"Wear","Pants","Black"},{"Buy","Shiestys","BlackShiesty"},{"Wear","Shiestys","BlackShiesty"}})
end)
QuickOutfitGroup:AddButton("Blu Moncler Drip", function()
    applyOutfit("Blu Moncler Drip", {{"Buy","Shirts","Blu Moncler"},{"Wear","Shirts","Blu Moncler"},{"Buy","Pants","Amiri Blue Jeans"},{"Wear","Pants","Amiri Blue Jeans"},{"Buy","Shiestys","Shiesty"},{"Wear","Shiestys","Shiesty"}})
end)
QuickOutfitGroup:AddButton("Amiri Star Drip", function()
    applyOutfit("Amiri Star Drip", {{"Buy","Shirts","Amiri Star Hoodie"},{"Wear","Shirts","Amiri Star Hoodie"},{"Buy","Pants","Black Amiri Jeans B22"},{"Wear","Pants","Black Amiri Jeans B22"},{"Buy","Shiestys","Shiesty"},{"Wear","Shiestys","Shiesty"}})
end)
QuickOutfitGroup:AddButton("Red Sp5der Drip", function()
    applyOutfit("Red Sp5der Drip", {{"Buy","Shirts","Red Sp5der Sweats"},{"Wear","Shirts","Red Sp5der Sweats"},{"Buy","Pants","Red Sp5der Sweats"},{"Wear","Pants","Red Sp5der Sweats"},{"Buy","Shiestys","RedShiesty"},{"Wear","Shiestys","RedShiesty"}})
end)
QuickOutfitGroup:AddButton("Green Sp5der Drip", function()
    applyOutfit("Green Sp5der Drip", {{"Buy","Shirts","Green Sp5der Sweats"},{"Wear","Shirts","Green Sp5der Hoodie"},{"Buy","Pants","Green Sp5der Sweats"},{"Wear","Pants","Green Sp5der Sweats"},{"Buy","Shiestys","Shiesty"},{"Wear","Shiestys","Shiesty"}})
end)
QuickOutfitGroup:AddButton("Blue Sp5der Drip", function()
    applyOutfit("Blue Sp5der Drip", {{"Buy","Shirts","Blue Sp5der Hoodie"},{"Wear","Shirts","Blue Sp5der Hoodie"},{"Buy","Pants","Blue Sp5der Sweats"},{"Wear","Pants","Blue Sp5der Sweats"},{"Buy","Shiestys","Shiesty"},{"Wear","Shiestys","Shiesty"}})
end)
QuickOutfitGroup:AddButton("Red Zipped Drip", function()
    applyOutfit("Red Zipped Drip", {{"Buy","Shirts","Red Zipped Jacket"},{"Wear","Shirts","Red Zipped Jacket"},{"Buy","Pants","White AF1 BJeans"},{"Wear","Pants","White AF1 BJeans"}})
end)

getgenv().outfitData = getgenv().outfitData or {
    ["Blue Spider"] = { Hats={"MRHoodieBRR"}, Shirts={"Blue Sp5der Hoodie"}, Pants={"Blue Sp5der Sweats"}, Shiestys={"Shiesty"} },
    ["Spider Man"]  = { Hats=nil,              Shirts={"Spiderman"},          Pants={"Spiderman"},          Shiestys={"RedShiesty"} },
    ["Red Spider"]  = { Hats={"MRHoodieBRR"}, Shirts={"Red Sp5der Sweats"},  Pants={"Red Sp5der Sweats"},  Shiestys={"Shiesty"} },
}
getgenv().selectedOutfit = getgenv().selectedOutfit or "Blue Spider"
QuickOutfitGroup:AddDropdown("BoxHubOutfitDropdown", {
    Text = "Outfit Preset",
    Values = {"Blue Spider", "Spider Man", "Red Spider"},
    Default = "Blue Spider",
    Multi = false,
    Callback = function(o) getgenv().selectedOutfit = o; sendNotification("Outfit", "Selected: " .. tostring(o)) end
})
QuickOutfitGroup:AddButton("Equip Selected Outfit", function()
    local outfit = getgenv().outfitData[getgenv().selectedOutfit]
    if not outfit then sendNotification("Outfit", "Outfit not found."); return end
    pcall(function() ReplicatedStorage.ClothShopRemote:FireServer("Reset Data") end)
    task.wait(0.2)
    for category, items in pairs(outfit) do
        if items then
            for _, item in ipairs(items) do
                pcall(function()
                    if LocalPlayer[category] and not LocalPlayer[category]:FindFirstChild(item) then
                        ReplicatedStorage.ClothShopRemote:FireServer("Buy", category, item); task.wait(0.1)
                    end
                    ReplicatedStorage.ClothShopRemote:FireServer("Wear", category, item); task.wait(0.2)
                end)
            end
        end
    end
    sendNotification("Outfit", "Equipped " .. getgenv().selectedOutfit .. "!")
end)


local BankATMGroup = Tabs.Main:AddRightGroupbox("Bank / ATM")
local function getBankValue()
    local ok, v = pcall(function() return LocalPlayer.stored.Bank.Value end)
    if ok then return v end; return 0
end
local bankAmountLabel = BankATMGroup:AddLabel("Bank Amount: $" .. tostring(getBankValue()))
pcall(function()
    local stored = LocalPlayer:WaitForChild("stored", 5)
    if stored and stored:FindFirstChild("Bank") then
        stored.Bank:GetPropertyChangedSignal("Value"):Connect(function()
            pcall(function() bankAmountLabel:SetText("Bank Amount: $" .. tostring(stored.Bank.Value)) end)
        end)
    end
end)

BankATMGroup:AddInput("BoxHubBankWithdraw", {
    Text = "Withdraw Amount",
    Default = "",
    Placeholder = "Max 90,000",
    Numeric = true,
    Finished = true,
    Callback = function(v)
        local a = tonumber(v)
        if a and a > 0 and a <= 90000 then
            pcall(function() ReplicatedStorage.BankAction:FireServer("with", a) end)
            sendNotification("Withdraw", "Withdrew $" .. a)
        end
    end
})

BankATMGroup:AddInput("BoxHubBankDeposit", {
    Text = "Deposit Amount",
    Default = "",
    Placeholder = "Max 30,000",
    Numeric = true,
    Finished = true,
    Callback = function(v)
        local a = tonumber(v)
        if a and a > 0 and a <= 30000 then
            pcall(function() ReplicatedStorage.BankAction:FireServer("depo", a) end)
            sendNotification("Deposit", "Deposited $" .. a)
        end
    end
})

BankATMGroup:AddToggle("BoxHubDropMax", {
    Text = "Drop Max ($10k loop)",
    Default = false,
    Callback = function(v)
        getgenv().BoxHubDropMax = v
        if v then
            task.spawn(function()
                while getgenv().BoxHubDropMax do
                    pcall(function() ReplicatedStorage:WaitForChild("BankProcessRemote"):InvokeServer("Drop", 10000) end)
                    task.wait(0.5)
                end
            end)
        end
    end
})

BankATMGroup:AddToggle("BoxHubAutoDeposit", {
    Text = "Auto Deposit 30K",
    Default = false,
    Callback = function(v)
        getgenv().BoxHubAutoDeposit = v
        if v then
            task.spawn(function()
                local BA = ReplicatedStorage:WaitForChild("BankAction")
                while getgenv().BoxHubAutoDeposit do
                    pcall(function() BA:FireServer("depo", 30000) end)
                    task.wait(3)
                end
            end)
        end
    end
})

BankATMGroup:AddToggle("BoxHubAutoWithdraw", {
    Text = "Auto Withdraw 90K",
    Default = false,
    Callback = function(v)
        getgenv().BoxHubAutoWithdraw = v
        if v then
            task.spawn(function()
                local BA = ReplicatedStorage:WaitForChild("BankAction")
                while getgenv().BoxHubAutoWithdraw do
                    pcall(function() BA:FireServer("with", 90000) end)
                    task.wait(3)
                end
            end)
        end
    end
})


local VehicleGroup = Tabs.Main:AddRightGroupbox("Vehicle Settings")
VehicleGroup:AddToggle("CarFlyEnabled", {
    Text = "Car Fly",
    Default = false,
    Callback = function(s) if s then StartCarFly() else StopCarFly() end end
})
VehicleGroup:AddSlider("CarFlySpeed", {
    Text = "Car Fly Speed",
    Min = 50, Max = 300, Default = 150, Rounding = 0,
    Callback = function(v) CarFly.Speed = v end
})
VehicleGroup:AddToggle("VehSpeedEnabled", {
    Text = "Vehicle Speed Boost",
    Default = false,
    Callback = function(s) VehicleModifications.SpeedEnabled = s end
})
VehicleGroup:AddSlider("VehSpeedValue", {
    Text = "Speed Multiplier",
    Min = 1, Max = 25, Default = 5, Rounding = 0,
    Callback = function(v) VehicleModifications.SpeedValue = v / 1000 end
})
VehicleGroup:AddToggle("VehBreakEnabled", {
    Text = "Vehicle Brakes",
    Default = false,
    Callback = function(s) VehicleModifications.BreakEnabled = s end
})
VehicleGroup:AddSlider("VehBreakValue", {
    Text = "Brake Multiplier",
    Min = 10, Max = 300, Default = 50, Rounding = 0,
    Callback = function(v) VehicleModifications.BreakValue = v / 1000 end
})
VehicleGroup:AddToggle("VehInstantStop", {
    Text = "Instant Stop (V)",
    Default = false,
    Callback = function(s) VehicleModifications.InstantStop = s end
})
VehicleGroup:AddButton("Bring Nearest Car", function()
    local char = LocalPlayer.Character; if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart"); local hum = char:FindFirstChildWhichIsA("Humanoid")
    if not hrp or not hum then return end
    local car = GetNearestCar(hrp)
    if not car then sendNotification("Bring Car", "No empty cars found nearby!") return end
    local seat = car:FindFirstChild("DriveSeat") or car:FindFirstChildWhichIsA("VehicleSeat"); if not seat then return end
    if not car.PrimaryPart then pcall(function() car.PrimaryPart = car:FindFirstChildWhichIsA("BasePart") end) end
    if car.PrimaryPart then
        car:SetPrimaryPartCFrame(hrp.CFrame * CFrame.new(0,0,-6) * CFrame.Angles(0,math.rad(180),0))
    else
        seat.CFrame = hrp.CFrame * CFrame.new(0,0,-6) * CFrame.Angles(0,math.rad(180),0)
    end
    task.wait(0.2); seat:Sit(hum)
    sendNotification("Bring Car", "Car brought successfully!")
end)


------------------------------------------------------------
--  AUTOFARM FUNCTION SECTOR (Encapsulated to resolve local register limit 200)
------------------------------------------------------------
local function loadAutofarmTabElements()
    -- Quick TPs (RIGHT)
    local FarmTpSection = Tabs.Autofarm:AddRightGroupbox("Quick TPs")
    local farmSelectedTp = teleportNames[1]
    FarmTpSection:AddDropdown("FarmTpLocation", {
        Text = "Location",
        Values = teleportNames,
        Default = farmSelectedTp,
        Callback = function(v) farmSelectedTp = normalizeDropdownValue(v) end
    })
    FarmTpSection:AddButton("Teleport", function()
        local cf = farmSelectedTp and teleportLocations[farmSelectedTp]
        if cf then Config:Teleport(cf); sendNotification("TP","Teleported to "..tostring(farmSelectedTp)) end
    end)
    FarmTpSection:AddButton("Dynamic Cook Pot", function() TeleportToCookPot() end)

    -- Dynamic Quick TP buttons
    for _, key in ipairs(QUICK_TP_KEYS) do
        local cf = teleportLocations[key]
        FarmTpSection:AddButton(key, function()
            if cf then
                Config:Teleport(cf)
                sendNotification("TP", "Teleported to " .. key, 2)
            else
                sendNotification("TP", "Location not found: " .. key)
            end
        end)
    end


    -- My Money Statistic Tracker (LEFT)
    local MyMoneySection = Tabs.Autofarm:AddLeftGroupbox("My Money")
    local myClean = MyMoneySection:AddLabel("Clean Money: None")
    local myBank  = MyMoneySection:AddLabel("Bank: None")
    local myDirty = MyMoneySection:AddLabel("Dirty Money: None")
    local myMade  = MyMoneySection:AddLabel("Money Made: $0")
    local myLost  = MyMoneySection:AddLabel("Money Lost: $0")

    getgenv().MoneyMadeTotal = getgenv().MoneyMadeTotal or 0
    getgenv().MoneyLostTotal = getgenv().MoneyLostTotal or 0
    local lastClean = nil

    task.spawn(function()
        while task.wait(0.3) do
            local c, d = getMyMoneyValues()
            local b = getBankValue2()
            if lastClean ~= nil and c ~= nil then
                local delta = c - lastClean
                if delta > 0 then getgenv().MoneyMadeTotal = getgenv().MoneyMadeTotal + delta
                elseif delta < 0 then getgenv().MoneyLostTotal = getgenv().MoneyLostTotal + (-delta) end
            end
            lastClean = c
            pcall(function()
                myClean:SetText("Clean Money: "..(c~=nil and formatMoney(c) or "None"))
                myBank:SetText("Bank: "..(b~=nil and formatMoney(b) or "None"))
                myDirty:SetText("Dirty Money: "..(d~=nil and formatMoney(d) or "None"))
                myMade:SetText("Money Made: "..formatMoney(getgenv().MoneyMadeTotal))
                myLost:SetText("Money Lost: "..formatMoney(getgenv().MoneyLostTotal))
            end)
        end
    end)


    -- Autofarms (LEFT)
    local AutofarmsSection = Tabs.Autofarm:AddLeftGroupbox("Autofarms")
    local dumpsterFlag = false
    AutofarmsSection:AddButton("Dumpster Autofarm", function()
        if not dumpsterFlag then 
            dumpsterFlag=true
            startDumpsterAutofarm()
            sendNotification("Autofarm","Dumpster started.")
        else 
            dumpsterFlag=false
            stopDumpsterAutofarm()
            sendNotification("Autofarm","Dumpster stopped.") 
        end
    end)
    AutofarmsSection:AddButton("Studio Autofarm", function()
        getgenv().StudioPayFarm = not getgenv().StudioPayFarm
        sendNotification("Autofarm","Studio "..(getgenv().StudioPayFarm and "started." or "stopped."))
    end)
    local constFlag = false
    AutofarmsSection:AddButton("Construction Autofarm", function()
        if not constFlag then 
            constFlag=true
            startConstructionAutofarm()
            sendNotification("Autofarm","Construction started.")
        else 
            constFlag=false
            stopConstructionAutofarm()
            sendNotification("Autofarm","Construction stopped.") 
        end
    end)


    -- Local Player (LEFT)
    local PlayerLocalSection = Tabs.Autofarm:AddLeftGroupbox("Local Player")
    PlayerLocalSection:AddToggle("AF_AntiFallDamage", {
        Text = "Anti Fall Damage",
        Default = false,
        Callback = function(s)
            if s and LocalPlayer.Character then local f=LocalPlayer.Character:FindFirstChild("FallDamageRagdoll"); if f then f:Destroy() end end
        end
    })
    PlayerLocalSection:AddToggle("AF_AntiJumpCooldown", {
        Text = "Anti Jump Cooldown",
        Default = false,
        Callback = function(s)
            local jd = LocalPlayer.PlayerGui and LocalPlayer.PlayerGui:FindFirstChild("JumpDebounce")
            local ls = jd and jd:FindFirstChildOfClass("LocalScript"); if ls then ls.Enabled = not s end
        end
    })
    PlayerLocalSection:AddToggle("AF_InfiniteSleep", {
        Text = "Infinite Sleep",
        Default = false,
        Callback = function(s)
            local sg = LocalPlayer.PlayerGui and LocalPlayer.PlayerGui:FindFirstChild("SleepGui")
            local ss = sg and sg:FindFirstChild("Frame") and sg.Frame:FindFirstChild("sleep") and sg.Frame.sleep:FindFirstChild("SleepBar") and sg.Frame.sleep.SleepBar:FindFirstChild("sleepScript")
            if ss then ss.Enabled = not s end
        end
    })
    PlayerLocalSection:AddToggle("AF_InfiniteHunger", {
        Text = "Infinite Hunger",
        Default = false,
        Callback = function(s)
            local hg = LocalPlayer.PlayerGui and LocalPlayer.PlayerGui:FindFirstChild("Hunger")
            local hs = hg and hg:FindFirstChild("Frame") and hg.Frame:FindFirstChild("Frame") and hg.Frame.Frame:FindFirstChild("Frame") and hg.Frame.Frame.Frame:FindFirstChild("HungerBarScript")
            if hs then hs.Enabled = not s end
        end
    })
    PlayerLocalSection:AddToggle("AF_InfiniteStamina", {
        Text = "Infinite Stamina",
        Default = false,
        Callback = function(s)
            local rg = LocalPlayer.PlayerGui and LocalPlayer.PlayerGui:FindFirstChild("Run")
            local ss = rg and rg:FindFirstChild("Frame") and rg.Frame:FindFirstChild("Frame") and rg.Frame.Frame:FindFirstChild("Frame") and rg.Frame.Frame.Frame:FindFirstChild("StaminaBarScript")
            if ss then ss.Enabled = not s end
        end
    })
    PlayerLocalSection:AddToggle("AF_NoRentPay", {
        Text = "No Rent Pay",
        Default = false,
        Callback = function(s)
            local rg = StarterGui:FindFirstChild("RentGui"); local rs = rg and rg:FindFirstChildOfClass("LocalScript")
            if rs then rs.Enabled = not s end
        end
    })
    PlayerLocalSection:AddToggle("InstantInteraction", {
        Text = "Instant Interaction",
        Default = false,
        Callback = function(s) refreshPrompts(s) end
    })
    PlayerLocalSection:AddToggle("StudioMoneyNotify", {
        Text = "Studio Money Notify",
        Default = false,
        Callback = function(s)
            StudioMoneyNotifyEnabled = s; cleanupStudioMoneyNotify()
            if s then for _, n in ipairs({"StudioPay1","StudioPay2","StudioPay3"}) do watchStudioPayPrompt(n) end end
        end
    })
    PlayerLocalSection:AddToggle("AutoStealLootbags", {
        Text = "Auto Steal Lootbags",
        Default = false,
        Callback = function(s) AutoStealLootbags=s end
    })
    PlayerLocalSection:AddToggle("AutoPickupBags", {
        Text = "Auto Pickup Bags",
        Default = false,
        Callback = function(s) AutoPickupBags=s end
    })
    PlayerLocalSection:AddToggle("AF_DisableBloodEffects", {
        Text = "Disable Blood Effects",
        Default = false,
        Callback = function(s) DisableBloodEffects=s end
    })
    PlayerLocalSection:AddToggle("AF_BypassLockedCars", {
        Text = "Bypass Locked Cars",
        Default = false,
        Callback = function(s) BypassLockedCars=s end
    })
    PlayerLocalSection:AddToggle("AF_NoKnockback", {
        Text = "No Knockback",
        Default = false,
        Callback = function(s) NoKnockback=s end
    })
    PlayerLocalSection:AddToggle("AutoDepositPickedCash", {
        Text = "Auto Deposit Picked Cash",
        Default = false,
        Callback = function(s) AutoCleanMoney=s end
    })
    PlayerLocalSection:AddToggle("AutoStealDroppedCash", {
        Text = "Auto Steal Dropped Cash",
        Default = false,
        Callback = function(s) AutoGrabMoney=s end
    })


    -- Movement (RIGHT)
    local MovementSection = Tabs.Autofarm:AddRightGroupbox("Movement")
    MovementSection:AddToggle("AF_NoCameraBob", {
        Text = "Disable Camera Bobbing",
        Default = false,
        Callback = function(s) NoCameraBob=s end
    })
    MovementSection:AddToggle("AF_SpeedBoost", {
        Text = "Speed Boost",
        Default = false,
        Callback = function(s) updateSpeedBoostState(s) end
    })
    MovementSection:AddToggle("AF_JumpBoost", {
        Text = "Jump Boost",
        Default = false,
        Callback = function(s) updateJumpBoostState(s) end
    })
    MovementSection:AddToggle("AF_InfiniteJump", {
        Text = "Infinite Jump",
        Default = false,
        Callback = function(s) InfJumpEnabled=s end
    })
    MovementSection:AddToggle("AF_Noclip", {
        Text = "Noclip",
        Default = false,
        Callback = function(s) 
            NoclipEnabled=s; if s then enableNoclip() else disableNoclip() end 
        end
    })


    -- Miscellaneous (LEFT)
    local MiscSection = Tabs.Autofarm:AddLeftGroupbox("Miscellaneous")
    MiscSection:AddToggle("AF_FasterRespawn", {
        Text = "Faster Respawn",
        Default = false,
        Callback = function(s) FasterRespawn=s end
    })
    MiscSection:AddToggle("AF_RespawnWhereDied", {
        Text = "Respawn Where You Died",
        Default = false,
        Callback = function(s) RespawnWhereYouDied=s end
    })
    MiscSection:AddToggle("AF_KeepToolsOnDeath", {
        Text = "Keep Tools On Death",
        Default = false,
        Callback = function(s) KeepToolsOnDeath=s end
    })
    MiscSection:AddToggle("AF_InstantRevive", {
        Text = "Instant Revive (auto F on low HP)",
        Default = false,
        Callback = function(s) AutoFOnLowHealth=s end
    })
    MiscSection:AddButton("🚑 Get Help - $2000", function()
        if not workspace:FindFirstChild("HospitalPart") then
            sendNotification("Get Help", "HospitalPart not found.")
            return
        end
        local char = LocalPlayer.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = workspace.HospitalPart.CFrame
            task.wait(0.05)
            local tries = 0
            repeat
                task.wait(0.05)
                pcall(function() fireproximityprompt(workspace.HospitalPart.Prompt, 1) end)
                tries = tries + 1
            until LocalPlayer:GetAttribute("LastACPos") == nil or tries > 200
            sendNotification("Get Help", "Done!")
        end
    end)
    local autoGetHelpEnabled = false
    MiscSection:AddToggle("AutoGetHelp", {
        Text = "Auto Get Help",
        Default = false,
        Callback = function(s)
            autoGetHelpEnabled = s
        end
    })

    task.spawn(function()
        while true do
            task.wait(0.5)
            if autoGetHelpEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
                local hum = LocalPlayer.Character.Humanoid
                if hum.Health > 0 and hum.Health < 15 and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    if workspace:FindFirstChild("HospitalPart") then
                        local hrp = LocalPlayer.Character.HumanoidRootPart
                        hrp.CFrame = workspace.HospitalPart.CFrame
                        task.wait(0.05)
                        local tries = 0
                        repeat
                            task.wait(0.05)
                            pcall(function() fireproximityprompt(workspace.HospitalPart.Prompt, 1) end)
                            tries = tries + 1
                        until LocalPlayer:GetAttribute("LastACPos") == nil or tries > 20 or not autoGetHelpEnabled
                    end
                end
            end
        end
    end)

    MiscSection:AddToggle("AF_AntiAFK", {
        Text = "Anti-AFK (20 min)",
        Default = false,
        Callback = function(s) getgenv().AntiAFKEnabled=s end
    })


    -- Looting (RIGHT)
    local LootSection = Tabs.Autofarm:AddRightGroupbox("Looting / Auto Loot")
    LootSection:AddToggle("AutoLootStudio", {
        Text = "Auto Loot Studio",
        Default = false,
        Callback = function(s) getgenv().AutoLootStudio=s end
    })
    LootSection:AddToggle("AutoLootHouse1", {
        Text = "Auto Loot House [1]",
        Default = false,
        Callback = function(s) getgenv().AutoLootHouse1=s end
    })
    LootSection:AddToggle("AutoLootHouse2", {
        Text = "Auto Loot House [2]",
        Default = false,
        Callback = function(s) getgenv().AutoLootHouse2=s end
    })
    LootSection:AddToggle("AutoLootTrash", {
        Text = "Auto Loot Trash",
        Default = false,
        Callback = function(s) getgenv().AutoLootTrash=s end
    })
    LootSection:AddToggle("AutoSellLoot", {
        Text = "Auto Sell Loot",
        Default = false,
        Callback = function(s) getgenv().AutoSellLoot=s end
    })


    -- Heist Tools (LEFT)
    local HeistSection = Tabs.Autofarm:AddLeftGroupbox("Heist Tools")
    HeistSection:AddDropdown("HeistToolSelect", {
        Text = "Heist Tool",
        Values = {"C4","Drill","Thermite","Crowbar"},
        Default = "C4",
        Callback = function(v) getgenv().HeistToolSelect=v end
    })
    HeistSection:AddToggle("AutoRobBank", {
        Text = "Auto Rob Bank",
        Default = false,
        Callback = function(s) getgenv().AutoRobBank=s end
    })
    HeistSection:AddDropdown("TargetStoreSelect", {
        Text = "Target Store",
        Values = {"Store1","Store2","Store3","DollarGeneral","McDonalds"},
        Default = "Store1",
        Callback = function(v) getgenv().TargetStoreSelect=v end
    })
    HeistSection:AddToggle("AutoRobStore", {
        Text = "Auto Rob Store",
        Default = false,
        Callback = function(s) getgenv().AutoRobStore=s end
    })
end

------------------------------------------------------------
--  VISUALS TAB SUPPORT & LOGIC (From ddd.txt)
------------------------------------------------------------
local VisualColorPresets = {
    ["White"]=Color3.fromRGB(255,255,255), ["Black"]=Color3.fromRGB(0,0,0),
    ["Red"]=Color3.fromRGB(255,0,0),       ["Green"]=Color3.fromRGB(0,255,0),
    ["Blue"]=Color3.fromRGB(0,120,255),    ["Purple"]=Color3.fromRGB(143,0,255),
    ["Pink"]=Color3.fromRGB(255,80,180),   ["Cyan"]=Color3.fromRGB(0,255,255),
    ["Yellow"]=Color3.fromRGB(255,255,0),  ["Orange"]=Color3.fromRGB(255,140,0),
    ["Lime"]=Color3.fromRGB(130,255,0),    ["Gray"]=Color3.fromRGB(120,120,120),
}
local VisualColorNames = {}
for n in pairs(VisualColorPresets) do table.insert(VisualColorNames, n) end
table.sort(VisualColorNames)
local function getVisualColor(name, fb) return VisualColorPresets[tostring(name or "")] or fb or Color3.fromRGB(255,255,255) end

local WorldVisuals = {
    SaturationEnabled=false, SaturationValue=1,
    StretchEnabled=false,    StretchValue=0.7,
    FogColorEnabled=false,   FogColor=Color3.fromRGB(255,255,255),
    AmbientEnabled=false,    AmbientColor=Color3.fromRGB(255,255,255),
    FieldOfViewEnabled=false,FieldOfViewValue=70,
    Fullbright=false,
}

local ESPFlags = {
    Enabled=false, RenderDistance=500, TeamColor=true,
    Boxes=false, BoxType="Corner", BoxColor=Color3.fromRGB(255,0,0),
    BoxFill=false, BoxFillColor=Color3.fromRGB(255,255,255),
    Healthbar=false, HealthbarNumber=false, HealthbarThickness=2,
    Name=false, Distance=false, Weapon=false,
    TextSize=13, TextColor=Color3.fromRGB(255,255,255),
    Snaplines=false, SnaplineColor=Color3.fromRGB(255,0,0),
    Chams=false, ChamsColor=Color3.fromRGB(255,0,0),
    Skeleton=false, SkeletonColor=Color3.fromRGB(255,255,255),
    LookTracers=false, LookTracerColor=Color3.fromRGB(0,255,255),
}

local VisualColorCorrection = Lighting:FindFirstChild("MiamiColorCorrection")
if not VisualColorCorrection then
    VisualColorCorrection = Instance.new("ColorCorrectionEffect")
    VisualColorCorrection.Name = "MiamiColorCorrection"
    VisualColorCorrection.Parent = Lighting
end
local OriginalWorld = {
    Brightness=Lighting.Brightness, ClockTime=Lighting.ClockTime, FogEnd=Lighting.FogEnd,
    FogColor=Lighting.FogColor, GlobalShadows=Lighting.GlobalShadows,
    OutdoorAmbient=Lighting.OutdoorAmbient, Ambient=Lighting.Ambient,
    FieldOfView=workspace.CurrentCamera and workspace.CurrentCamera.FieldOfView or 70,
    Saturation=VisualColorCorrection.Saturation, TintColor=VisualColorCorrection.TintColor,
}
local WorldReset = {Fog=false, Fov=false, Fullbright=false, Ambient=false, Saturation=false}

RunService:BindToRenderStep("MiamiWorldVisuals", Enum.RenderPriority.Camera.Value, function()
    if not (WorldVisuals.StretchEnabled or WorldVisuals.FieldOfViewEnabled
         or WorldVisuals.SaturationEnabled or WorldVisuals.AmbientEnabled
         or WorldVisuals.FogColorEnabled or WorldVisuals.Fullbright)
       and WorldReset.Fov and WorldReset.Saturation and WorldReset.Ambient
       and WorldReset.Fog and WorldReset.Fullbright then
        return
    end
    local cam = workspace.CurrentCamera; if not cam then return end
    if WorldVisuals.StretchEnabled then
        cam.CFrame = cam.CFrame * CFrame.new(0,0,0,1,0,0,0,WorldVisuals.StretchValue,0,0,0,1)
    end
    if WorldVisuals.FieldOfViewEnabled then WorldReset.Fov=false; cam.FieldOfView = WorldVisuals.FieldOfViewValue
    elseif not WorldReset.Fov then WorldReset.Fov=true; cam.FieldOfView = OriginalWorld.FieldOfView or 70 end

    if WorldVisuals.SaturationEnabled then WorldReset.Saturation=false; VisualColorCorrection.Saturation = WorldVisuals.SaturationValue
    elseif not WorldReset.Saturation then WorldReset.Saturation=true; VisualColorCorrection.Saturation = OriginalWorld.Saturation or 0 end

    if WorldVisuals.AmbientEnabled then
        WorldReset.Ambient=false
        VisualColorCorrection.TintColor = WorldVisuals.AmbientColor
        Lighting.Ambient = WorldVisuals.AmbientColor
        Lighting.OutdoorAmbient = WorldVisuals.AmbientColor
    elseif not WorldReset.Ambient then
        WorldReset.Ambient=true
        VisualColorCorrection.TintColor = OriginalWorld.TintColor or Color3.new(1,1,1)
        Lighting.Ambient = OriginalWorld.Ambient
        Lighting.OutdoorAmbient = OriginalWorld.OutdoorAmbient
    end

    if WorldVisuals.FogColorEnabled then WorldReset.Fog=false; Lighting.FogColor = WorldVisuals.FogColor
    elseif not WorldReset.Fog then WorldReset.Fog=true; Lighting.FogColor = OriginalWorld.FogColor end

    if WorldVisuals.Fullbright then
        WorldReset.Fullbright=false
        Lighting.Brightness=2; Lighting.ClockTime=14; Lighting.FogEnd=100000
        Lighting.GlobalShadows=false; Lighting.OutdoorAmbient=Color3.fromRGB(128,128,128)
    elseif not WorldReset.Fullbright then
        WorldReset.Fullbright=true
        Lighting.Brightness=OriginalWorld.Brightness; Lighting.ClockTime=OriginalWorld.ClockTime
        Lighting.FogEnd=OriginalWorld.FogEnd; Lighting.GlobalShadows=OriginalWorld.GlobalShadows
        Lighting.OutdoorAmbient=OriginalWorld.OutdoorAmbient
    end
end)

-- ESP drawing
local function createDrawing(className, props)
    if not Drawing or type(Drawing.new) ~= "function" then return {Visible=false, Remove=function() end} end
    local ok, obj = pcall(Drawing.new, className)
    if not ok or not obj then return {Visible=false, Remove=function() end} end
    for k, v in pairs(props or {}) do pcall(function() obj[k] = v end) end
    return obj
end
local ESPObjects = {}
local HighlightObjects = {}
local SkeletonPairs = {
    {"Head","UpperTorso"}, {"UpperTorso","LowerTorso"},
    {"UpperTorso","LeftUpperArm"}, {"LeftUpperArm","LeftLowerArm"}, {"LeftLowerArm","LeftHand"},
    {"UpperTorso","RightUpperArm"}, {"RightUpperArm","RightLowerArm"}, {"RightLowerArm","RightHand"},
    {"LowerTorso","LeftUpperLeg"}, {"LeftUpperLeg","LeftLowerLeg"}, {"LeftLowerLeg","LeftFoot"},
    {"LowerTorso","RightUpperLeg"}, {"RightUpperLeg","RightLowerLeg"}, {"RightLowerLeg","RightFoot"},
}
local function createESPObject(player)
    if player == LocalPlayer or ESPObjects[player] then return end
    local box = {}
    for _, n in ipairs({"Top","Bottom","Left","Right","TopLeft","TopRight","BottomLeft","BottomRight"}) do
        box[n] = createDrawing("Line",{Visible=false, Thickness=1, Color=ESPFlags.BoxColor})
    end
    local skeleton = {}
    for i = 1, #SkeletonPairs do skeleton[i] = createDrawing("Line",{Visible=false, Thickness=1, Color=ESPFlags.SkeletonColor}) end
    ESPObjects[player] = {
        Box=box,
        Fill          = createDrawing("Square",{Visible=false, Filled=true, Transparency=0.85, Color=ESPFlags.BoxFillColor}),
        HealthOutline = createDrawing("Square",{Visible=false, Filled=false, Color=Color3.fromRGB(0,0,0), Thickness=1}),
        HealthFill    = createDrawing("Square",{Visible=false, Filled=true, Color=Color3.fromRGB(0,255,0)}),
        HealthText    = createDrawing("Text",{Visible=false, Center=true, Outline=true, Size=ESPFlags.TextSize, Color=Color3.fromRGB(255,255,255)}),
        Name          = createDrawing("Text",{Visible=false, Center=true, Outline=true, Size=ESPFlags.TextSize, Color=ESPFlags.TextColor}),
        Distance      = createDrawing("Text",{Visible=false, Center=true, Outline=true, Size=ESPFlags.TextSize, Color=ESPFlags.TextColor}),
        Weapon        = createDrawing("Text",{Visible=false, Center=true, Outline=true, Size=ESPFlags.TextSize, Color=ESPFlags.TextColor}),
        Snapline      = createDrawing("Line",{Visible=false, Thickness=1, Color=ESPFlags.SnaplineColor}),
        LookTracer    = createDrawing("Line",{Visible=false, Thickness=1, Color=ESPFlags.LookTracerColor}),
        Skeleton      = skeleton,
    }
end
local function hideESPObject(obj)
    if not obj then return end
    if obj.Box then for _, l in pairs(obj.Box) do l.Visible=false end end
    if obj.Skeleton then for _, l in ipairs(obj.Skeleton) do l.Visible=false end end
    for _, k in ipairs({"Fill","HealthOutline","HealthFill","HealthText","Name","Distance","Weapon","Snapline","LookTracer"}) do
        if obj[k] then obj[k].Visible=false end
    end
end
local function removeESPObject(player)
    local obj = ESPObjects[player]
    if obj then
        if obj.Box then for _, l in pairs(obj.Box) do pcall(function() l:Remove() end) end end
        if obj.Skeleton then for _, l in ipairs(obj.Skeleton) do pcall(function() l:Remove() end) end end
        for _, k in ipairs({"Fill","HealthOutline","HealthFill","HealthText","Name","Distance","Weapon","Snapline","LookTracer"}) do
            if obj[k] then pcall(function() obj[k]:Remove() end) end
        end
        ESPObjects[player] = nil
    end
    if HighlightObjects[player] then pcall(function() HighlightObjects[player]:Destroy() end); HighlightObjects[player]=nil end
end
local function getESPColor(player, def)
    if ESPFlags.TeamColor and player.Team and LocalPlayer.Team and player.Team == LocalPlayer.Team then
        return Color3.fromRGB(0,120,255)
    end
    return def
end
local function updateLine(line, from, to, color)
    if not line then return end
    line.From=from; line.To=to; line.Color=color; line.Visible=true
end
for _, p in ipairs(Players:GetPlayers()) do createESPObject(p) end
Players.PlayerAdded:Connect(createESPObject)
Players.PlayerRemoving:Connect(removeESPObject)

local _espHiddenOnce = false
local function hideESPForPlayer(player)
    hideESPObject(ESPObjects[player])
    if HighlightObjects[player] then
        HighlightObjects[player].Enabled = false
    end
end

local function updateESPForPlayer(player, cam)
    if player == LocalPlayer then
        hideESPForPlayer(player)
        return
    end

    createESPObject(player)
    local obj = ESPObjects[player]
    local char = player.Character
    local hum  = char and char:FindFirstChildOfClass("Humanoid")
    local root = char and char:FindFirstChild("HumanoidRootPart")

    if not (obj and char and hum and root and hum.Health > 0) then
        hideESPForPlayer(player)
        return
    end

    local localRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local distance = localRoot and (root.Position - localRoot.Position).Magnitude or (root.Position - cam.CFrame.Position).Magnitude
    if distance > ESPFlags.RenderDistance then
        hideESPForPlayer(player)
        return
    end

    local rootPos, onScreen = cam:WorldToViewportPoint(root.Position)
    if not onScreen or rootPos.Z <= 0 then
        hideESPForPlayer(player)
        return
    end

    local size = char:GetExtentsSize()
    local top = cam:WorldToViewportPoint((root.CFrame * CFrame.new(0, size.Y/2, 0)).Position)
    local bottom = cam:WorldToViewportPoint((root.CFrame * CFrame.new(0, -size.Y/2, 0)).Position)
    if top.Z <= 0 or bottom.Z <= 0 then
        hideESPForPlayer(player)
        return
    end

    local height = math.max(math.abs(bottom.Y - top.Y), 2)
    local width  = math.max(height * 0.58, 2)
    local x, y   = top.X - width/2, top.Y
    local boxColor = getESPColor(player, ESPFlags.BoxColor)
    local textColor = ESPFlags.TextColor

    if ESPFlags.BoxFill then
        obj.Fill.Position = Vector2.new(x, y)
        obj.Fill.Size = Vector2.new(width, height)
        obj.Fill.Color = ESPFlags.BoxFillColor
        obj.Fill.Transparency = 0.85
        obj.Fill.Visible = true
    else
        obj.Fill.Visible = false
    end

    if ESPFlags.Boxes then
        local tl, tr = Vector2.new(x, y), Vector2.new(x + width, y)
        local bl, br = Vector2.new(x, y + height), Vector2.new(x + width, y + height)
        local corner = math.min(width, height) * 0.28
        if ESPFlags.BoxType == "Full" then
            updateLine(obj.Box.Top, tl, tr, boxColor)
            updateLine(obj.Box.Bottom, bl, br, boxColor)
            updateLine(obj.Box.Left, tl, bl, boxColor)
            updateLine(obj.Box.Right, tr, br, boxColor)
            obj.Box.TopLeft.Visible = false
            obj.Box.TopRight.Visible = false
            obj.Box.BottomLeft.Visible = false
            obj.Box.BottomRight.Visible = false
        else
            obj.Box.Top.Visible = false
            obj.Box.Bottom.Visible = false
            obj.Box.Left.Visible = false
            obj.Box.Right.Visible = false
            updateLine(obj.Box.TopLeft, tl, tl + Vector2.new(corner, 0), boxColor)
            updateLine(obj.Box.Left, tl, tl + Vector2.new(0, corner), boxColor)
            updateLine(obj.Box.TopRight, tr, tr - Vector2.new(corner, 0), boxColor)
            updateLine(obj.Box.Right, tr, tr + Vector2.new(0, corner), boxColor)
            updateLine(obj.Box.BottomLeft, bl, bl + Vector2.new(corner, 0), boxColor)
            updateLine(obj.Box.Bottom, bl, bl - Vector2.new(0, corner), boxColor)
            updateLine(obj.Box.BottomRight, br, br - Vector2.new(corner, 0), boxColor)
            updateLine(obj.Box.Top, br, br - Vector2.new(0, corner), boxColor)
        end
    else
        for _, l in pairs(obj.Box) do
            l.Visible = false
        end
    end

    if ESPFlags.Healthbar then
        local pct = math.clamp(hum.Health / math.max(hum.MaxHealth, 1), 0, 1)
        local bh = height * pct
        obj.HealthOutline.Position = Vector2.new(x - 7, y)
        obj.HealthOutline.Size = Vector2.new(ESPFlags.HealthbarThickness + 2, height)
        obj.HealthOutline.Visible = true
        obj.HealthFill.Position = Vector2.new(x - 6, y + height - bh)
        obj.HealthFill.Size = Vector2.new(ESPFlags.HealthbarThickness, bh)
        obj.HealthFill.Color = Color3.fromRGB(255 - (255 * pct), 255 * pct, 0)
        obj.HealthFill.Visible = true
        if ESPFlags.HealthbarNumber then
            obj.HealthText.Text = tostring(math.floor(hum.Health))
            obj.HealthText.Position = Vector2.new(x - 12, y + height - bh)
            obj.HealthText.Size = ESPFlags.TextSize
            obj.HealthText.Visible = true
        else
            obj.HealthText.Visible = false
        end
    else
        obj.HealthOutline.Visible = false
        obj.HealthFill.Visible = false
        obj.HealthText.Visible = false
    end

    local bottomOffset = 2
    if ESPFlags.Name then
        obj.Name.Text = player.DisplayName ~= player.Name and (player.DisplayName .. " (@" .. player.Name .. ")") or player.Name
        obj.Name.Position = Vector2.new(x + width/2, y - ESPFlags.TextSize - 2)
        obj.Name.Size = ESPFlags.TextSize
        obj.Name.Color = textColor
        obj.Name.Visible = true
    else
        obj.Name.Visible = false
    end

    if ESPFlags.Distance then
        obj.Distance.Text = tostring(math.floor(distance)) .. "m"
        obj.Distance.Position = Vector2.new(x + width/2, y + height + bottomOffset)
        obj.Distance.Size = ESPFlags.TextSize
        obj.Distance.Color = textColor
        obj.Distance.Visible = true
        bottomOffset = bottomOffset + ESPFlags.TextSize + 1
    else
        obj.Distance.Visible = false
    end

    if ESPFlags.Weapon then
        local tool = char:FindFirstChildOfClass("Tool")
        obj.Weapon.Text = tool and ("[" .. tool.Name .. "]") or "[None]"
        obj.Weapon.Position = Vector2.new(x + width/2, y + height + bottomOffset)
        obj.Weapon.Size = ESPFlags.TextSize
        obj.Weapon.Color = textColor
        obj.Weapon.Visible = true
    else
        obj.Weapon.Visible = false
    end

    if ESPFlags.Snaplines then
        obj.Snapline.From = Vector2.new(cam.ViewportSize.X/2, cam.ViewportSize.Y)
        obj.Snapline.To = Vector2.new(rootPos.X, rootPos.Y)
        obj.Snapline.Color = getESPColor(player, ESPFlags.SnaplineColor)
        obj.Snapline.Visible = true
    else
        obj.Snapline.Visible = false
    end

    if ESPFlags.LookTracers and char:FindFirstChild("Head") then
        local head = char.Head
        local p1, o1 = cam:WorldToViewportPoint(head.Position)
        local p2, o2 = cam:WorldToViewportPoint(head.Position + head.CFrame.LookVector * 10)
        if o1 and o2 and p1.Z > 0 and p2.Z > 0 then
            obj.LookTracer.From = Vector2.new(p1.X, p1.Y)
            obj.LookTracer.To = Vector2.new(p2.X, p2.Y)
            obj.LookTracer.Color = ESPFlags.LookTracerColor
            obj.LookTracer.Visible = true
        else
            obj.LookTracer.Visible = false
        end
    else
        obj.LookTracer.Visible = false
    end

    if ESPFlags.Skeleton then
        for i, pair in ipairs(SkeletonPairs) do
            local a, b = char:FindFirstChild(pair[1]), char:FindFirstChild(pair[2])
            if a and b and obj.Skeleton[i] then
                local pa, oa = cam:WorldToViewportPoint(a.Position)
                local pb, ob = cam:WorldToViewportPoint(b.Position)
                if oa and ob and pa.Z > 0 and pb.Z > 0 then
                    obj.Skeleton[i].From = Vector2.new(pa.X, pa.Y)
                    obj.Skeleton[i].To = Vector2.new(pb.X, pb.Y)
                    obj.Skeleton[i].Color = ESPFlags.SkeletonColor
                    obj.Skeleton[i].Visible = true
                else
                    obj.Skeleton[i].Visible = false
                end
            elseif obj.Skeleton[i] then
                obj.Skeleton[i].Visible = false
            end
        end
    else
        for _, l in ipairs(obj.Skeleton) do
            l.Visible = false
        end
    end

    if ESPFlags.Chams then
        local h = HighlightObjects[player]
        if not h or not h.Parent then
            h = Instance.new("Highlight")
            h.Name = "MiamiESPHighlight"
            h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            h.Parent = CoreGui
            HighlightObjects[player] = h
        end
        h.Adornee = char
        h.FillColor = getESPColor(player, ESPFlags.ChamsColor)
        h.OutlineColor = Color3.fromRGB(255, 255, 255)
        h.FillTransparency = 0.55
        h.OutlineTransparency = 0.1
        h.Enabled = true
    elseif HighlightObjects[player] then
        HighlightObjects[player].Enabled = false
    end
end

RunService.RenderStepped:Connect(function()
    if not ESPFlags.Enabled then
        if not _espHiddenOnce then
            _espHiddenOnce = true
            for _, p in ipairs(Players:GetPlayers()) do
                hideESPForPlayer(p)
            end
        end
        return
    end

    _espHiddenOnce = false
    local cam = workspace.CurrentCamera
    if not cam then return end

    for _, p in ipairs(Players:GetPlayers()) do
        local ok, err = pcall(updateESPForPlayer, p, cam)
        if not ok then
            warn("[Miami ESP] Failed to update player ESP for", p and p.Name or "unknown", err)
            hideESPForPlayer(p)
        end
    end
end)

-- Sky helpers
local function clearAllSkies()
    pcall(function() for _, s in ipairs(Lighting:GetChildren()) do if s:IsA("Sky") then s:Destroy() end end end)
end
local function applyOregonSky(assetId)
    clearAllSkies()
    pcall(function()
        local s = Instance.new("Sky")
        s.Name="OH_PresetSky"
        s.SkyboxBk=assetId; s.SkyboxDn=assetId; s.SkyboxFt=assetId
        s.SkyboxLf=assetId; s.SkyboxRt=assetId; s.SkyboxUp=assetId
        s.CelestialBodiesShown=false
        s.Parent=Lighting
    end)
end
local function applyOregonSky6(bk,dn,ft,lf,rt,up)
    clearAllSkies()
    pcall(function()
        local s = Instance.new("Sky")
        s.Name="OH_PresetSky"
        s.SkyboxBk=bk; s.SkyboxDn=dn; s.SkyboxFt=ft; s.SkyboxLf=lf; s.SkyboxRt=rt; s.SkyboxUp=up
        s.CelestialBodiesShown=false
        s.Parent=Lighting
    end)
end

------------------------------------------------------------
--  VISUALS TAB (Fully Restored & Loaded From ddd.txt)
------------------------------------------------------------
local function loadVisualsTabElements()
    -- ===== World Visuals (LEFT) =====
    local WorldSection = Tabs.Visuals:AddLeftGroupbox("World Visuals")
    WorldSection:AddToggle("WorldFullbright", {Text="Fullbright", Default=false, Callback=function(v) WorldVisuals.Fullbright=v end})
    WorldSection:AddToggle("WorldSaturation", {Text="Enable Saturation", Default=false, Callback=function(v) WorldVisuals.SaturationEnabled=v end})
    WorldSection:AddSlider("WorldSaturationValue", {Text="Saturation Value", Min=0, Max=200, Default=100, Rounding=0, Callback=function(v) WorldVisuals.SaturationValue=v/100 end})
    WorldSection:AddToggle("WorldFOV", {Text="Enable FOV", Default=false, Callback=function(v) WorldVisuals.FieldOfViewEnabled=v end})
    WorldSection:AddSlider("WorldFOVValue", {Text="FOV Value", Min=30, Max=120, Default=70, Rounding=0, Callback=function(v) WorldVisuals.FieldOfViewValue=v end})
    WorldSection:AddToggle("WorldStretch", {Text="Enable Stretch", Default=false, Callback=function(v) WorldVisuals.StretchEnabled=v end})
    WorldSection:AddSlider("WorldStretchValue", {Text="Stretch Value", Min=0.1, Max=2, Default=0.7, Rounding = 2, Callback=function(v) WorldVisuals.StretchValue=v end})
    WorldSection:AddToggle("WorldFog", {Text="Enable Fog Color", Default=false, Callback=function(v) WorldVisuals.FogColorEnabled=v end})
    WorldSection:AddDropdown("WorldFogColor", {
        Text="Fog Color", Values=VisualColorNames, Default="White",
        Callback=function(v) WorldVisuals.FogColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(255,255,255)) end
    })
    WorldSection:AddToggle("WorldAmbient", {Text="Enable Ambient Tint", Default=false, Callback=function(v) WorldVisuals.AmbientEnabled=v end})
    WorldSection:AddDropdown("WorldAmbientColor", {
        Text="Ambient Color", Values=VisualColorNames, Default="White",
        Callback=function(v) WorldVisuals.AmbientColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(255,255,255)) end
    })

    -- ===== Custom Sky (LEFT) =====
    local SkySection = Tabs.Visuals:AddLeftGroupbox("Custom Sky")
    SkySection:AddButton("✨ Enable Custom Sky", function() applyOregonSky("rbxassetid://104326479029386"); sendNotification("Sky","Custom sky enabled") end)
    SkySection:AddButton("🟢 Oregon Sky 1",   function() applyOregonSky("rbxassetid://122991440398431"); sendNotification("Sky","Oregon Sky 1") end)
    SkySection:AddButton("🟢 Oregon Sky 2",   function() applyOregonSky("rbxassetid://72244120320660");  sendNotification("Sky","Oregon Sky 2") end)
    SkySection:AddButton("🏈 Isiah Horton Sky", function() applyOregonSky("rbxassetid://111540684553492"); sendNotification("Sky","Isiah Horton") end)
    SkySection:AddButton("🏈 Ryan Williams Sky",function() applyOregonSky("rbxassetid://99177624929587");  sendNotification("Sky","Ryan Williams") end)
    SkySection:AddButton("🏈 Mali Toney Sky",   function() applyOregonSky("rbxassetid://107159461930095"); sendNotification("Sky","Mali Toney") end)
    SkySection:AddButton("🏈 Ryan Sky",         function() applyOregonSky("rbxassetid://89289726744867");  sendNotification("Sky","Ryan") end)
    SkySection:AddButton("🏈 Mali Sky",         function() applyOregonSky("rbxassetid://117286814143954"); sendNotification("Sky","Mali") end)
    SkySection:AddButton("🏈 Carnell Tate Sky 1", function() applyOregonSky("rbxassetid://78558147022384"); sendNotification("Sky","Carnell Tate 1") end)
    SkySection:AddButton("🏈 Carnell Tate Sky 2", function() applyOregonSky("rbxassetid://85733377226918"); sendNotification("Sky","Carnell Tate 2") end)
    SkySection:AddButton("🏈 Carnell Tate Sky 3", function() applyOregonSky("rbxassetid://95476054052430"); sendNotification("Sky","Carnell Tate 3") end)
    SkySection:AddButton("🏈 Marvin Harrison Sky", function() applyOregonSky("rbxassetid://83521944099069"); sendNotification("Sky","Marvin Harrison") end)
    SkySection:AddButton("🏈 USC Sky", function() applyOregonSky("rbxassetid://83445363298181"); sendNotification("Sky","USC") end)
    SkySection:AddButton("🏈 Indiana Sky 1", function() applyOregonSky("rbxassetid://134633514021384"); sendNotification("Sky","Indiana 1") end)
    SkySection:AddButton("🏈 Indiana Sky 2", function() applyOregonSky("rbxassetid://108824206727603"); sendNotification("Sky","Indiana 2") end)
    SkySection:AddButton("♻️ Reset Skybox",     function() clearAllSkies(); sendNotification("Sky","Reset") end)

    -- ===== Player ESP (RIGHT) =====
    local ESPMainSection = Tabs.Visuals:AddRightGroupbox("Player ESP")
    ESPMainSection:AddToggle("ESPEnabled", {Text="ESP Enabled", Default=false, Callback=function(v) ESPFlags.Enabled=v end})
    ESPMainSection:AddSlider("ESPRenderDistance", {Text="Render Distance", Min=50, Max=5000, Default=500, Rounding=0, Callback=function(v) ESPFlags.RenderDistance=v end})
    ESPMainSection:AddToggle("ESPTeamColor", {Text="Team Color", Default=true, Callback=function(v) ESPFlags.TeamColor=v end})
    ESPMainSection:AddToggle("ESPBoxes", {Text="Boxes", Default=false, Callback=function(v) ESPFlags.Boxes=v end})
    ESPMainSection:AddDropdown("ESPBoxType", {Text="Box Type", Values={"Corner","Full"}, Default="Corner", Callback=function(v) ESPFlags.BoxType = tostring(normalizeDropdownValue(v)) end})
    ESPMainSection:AddDropdown("ESPBoxColor", {Text="Box Color", Values=VisualColorNames, Default="Red", Callback=function(v) ESPFlags.BoxColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(255,0,0)) end})
    ESPMainSection:AddToggle("ESPBoxFill", {Text="Box Fill", Default=false, Callback=function(v) ESPFlags.BoxFill=v end})
    ESPMainSection:AddDropdown("ESPBoxFillColor", {Text="Box Fill Color", Values=VisualColorNames, Default="White", Callback=function(v) ESPFlags.BoxFillColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(255,255,255)) end})
    ESPMainSection:AddToggle("ESPHealthbar", {Text="Healthbar", Default=false, Callback=function(v) ESPFlags.Healthbar=v end})
    ESPMainSection:AddToggle("ESPHealthbarNumber", {Text="Healthbar Number", Default=false, Callback=function(v) ESPFlags.HealthbarNumber=v end})
    ESPMainSection:AddSlider("ESPHealthbarThickness", {Text="Healthbar Thickness", Min=1, Max=10, Default=2, Rounding=0, Callback=function(v) ESPFlags.HealthbarThickness=v end})
    ESPMainSection:AddToggle("ESPChams", {Text="Chams", Default=false, Callback=function(v) ESPFlags.Chams=v end})
    ESPMainSection:AddDropdown("ESPChamsColor", {Text="Chams Color", Values=VisualColorNames, Default="Red", Callback=function(v) ESPFlags.ChamsColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(255,0,0)) end})

    -- ===== ESP Text / Lines (RIGHT) =====
    local ESPTextSection = Tabs.Visuals:AddRightGroupbox("ESP Text / Lines")
    ESPTextSection:AddToggle("ESPName", {Text="Name", Default=false, Callback=function(v) ESPFlags.Name=v end})
    ESPTextSection:AddToggle("ESPDistance", {Text="Distance", Default=false, Callback=function(v) ESPFlags.Distance=v end})
    ESPTextSection:AddToggle("ESPWeapon", {Text="Weapon", Default=false, Callback=function(v) ESPFlags.Weapon=v end})
    ESPTextSection:AddSlider("ESPTextSize", {Text="Text Size", Min=8, Max=20, Default=13, Rounding=0, Callback=function(v) ESPFlags.TextSize=v end})
    ESPTextSection:AddDropdown("ESPTextColor", {Text="Text Color", Values=VisualColorNames, Default="White", Callback=function(v) ESPFlags.TextColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(255,255,255)) end})
    ESPTextSection:AddToggle("ESPSnaplines", {Text="Snaplines", Default=false, Callback=function(v) ESPFlags.Snaplines=v end})
    ESPTextSection:AddDropdown("ESPSnaplineColor", {Text="Snapline Color", Values=VisualColorNames, Default="Red", Callback=function(v) ESPFlags.SnaplineColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(255,0,0)) end})
    ESPTextSection:AddToggle("ESPSkeleton", {Text="Skeleton", Default=false, Callback=function(v) ESPFlags.Skeleton=v end})
    ESPTextSection:AddDropdown("ESPSkeletonColor", {Text="Skeleton Color", Values=VisualColorNames, Default="White", Callback=function(v) ESPFlags.SkeletonColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(255,255,255)) end})
    ESPTextSection:AddToggle("ESPLookTracers", {Text="Look Tracers", Default=false, Callback=function(v) ESPFlags.LookTracers=v end})
    ESPTextSection:AddDropdown("ESPLookTracerColor", {Text="Look Tracer Color", Values=VisualColorNames, Default="Cyan", Callback=function(v) ESPFlags.LookTracerColor = getVisualColor(normalizeDropdownValue(v), Color3.fromRGB(0,255,255)) end})
end

-- Load Modular Elements to fully bypass limit 200
pcall(loadAutofarmTabElements)
pcall(loadVisualsTabElements)
pcall(loadSafeTabFunctions)

-- ==========================================
-- SETTINGS / UI SETTINGS TAB
-- ==========================================
local MenuGroup = Tabs.Settings:AddLeftGroupbox("Menu Settings")

MenuGroup:AddLabel("Game: THA BRONX 3🐍", true)
MenuGroup:AddLabel("Developed By EL FULLY", true)
MenuGroup:AddDivider()

MenuGroup:AddLabel("Menu bind")
    :AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Menu keybind" })

MenuGroup:AddButton("Unload UI", function()
    Library:Unload()
end)

Library:OnUnload(function()
    print("Unloaded!")
end)

MenuGroup:AddButton('Rejoin Server', function()
	game:GetService('TeleportService'):TeleportToPlaceInstance(game.PlaceId, game.JobId)
	Library:Notify('Rejoining Server!', 10)
end)

MenuGroup:AddButton('Server Hop', function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/BENZZY420/SERVERHOP/refs/heads/main/SERVERHOP"))()
end)

MenuGroup:AddButton({
    Text = 'Copy Discord',
    Func = function()
       setclipboard("https://discord.gg/xC8rTZ2nE")
       Library:Notify("Copied Discord to clipboard", 10)
    end,
})

local executorName = identifyexecutor and identifyexecutor() or "Unknown"
MenuGroup:AddLabel("Executor: "..executorName, true)

Library.ToggleKeybind = Options.MenuKeybind

-- ==========================================
-- MIAMI THEME OVERRIDE
-- ==========================================
ThemeManager:SetLibrary(Library)

-- Define the custom Miami theme
ThemeManager.BuiltInThemes["Miami"] = {
    1,
    {
        FontColor = "f5f5f5",
        MainColor = "1c1c1c", -- Dark grey panel color
        BackgroundColor = "141414", -- Dark grey background canvas
        AccentColor = "ff8c00", -- Orange accent
        OutlineColor = "323232" -- Dark grey outline
    }
}

-- Set as active theme
ThemeManager:ApplyTheme("Miami")

SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })

ThemeManager:SetFolder("MiamiTheme")
SaveManager:SetFolder("MiamiTheme/specific-game")
SaveManager:SetSubFolder("specific-place")

SaveManager:BuildConfigSection(Tabs.Settings)
ThemeManager:ApplyToGroupbox(MenuGroup)
