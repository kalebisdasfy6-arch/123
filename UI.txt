local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/sametexe001/sametlibs/refs/heads/main/Thugsense/Library.lua"))()

-- Inject the purple theme colors BEFORE creating the window
if Library.Theme then
    Library.Theme.Accent = Color3.fromRGB(143, 0, 255)
    Library.Theme.Background = Color3.fromRGB(15, 15, 15)
    Library.Theme.Secondary = Color3.fromRGB(22, 22, 22)
    Library.Theme.Stroke = Color3.fromRGB(30, 30, 30)
    Library.Theme.Text = Color3.fromRGB(240, 240, 240)
end

local Window = Library:Window({
    Name = 'miani football  |  [v2.6]  |  License: <font color="#FFAA00">Lifetime</font>  |  <font color="#55FF00">Premium User</font>',
    Size = UDim2.new(0, 500, 0, 600),
    FadeSpeed = 0.0
})

local Watermark = Library:Watermark("miani football")
local KeybindList = Library:KeybindList()
Watermark:SetVisibility(false)
KeybindList:SetVisibility(false)

local MainTab = Window:Page({Name = "Main", Columns = 2, Subtabs = false})
local FarmingTab = Window:Page({Name = "Farming", Columns = 2, Subtabs = false})
local PlayerTab = Window:Page({Name = "Player", Columns = 2, Subtabs = false})
local VisualsTab = Window:Page({Name = "Visuals", Columns = 2, Subtabs = false})
local SettingsTab = Library:CreateSettingsPage(Window, Watermark, KeybindList)

for _, page in ipairs(Window.Pages) do
    page.Turn = function(self, Bool)
        if self.Active == Bool then return end
        self.Active = Bool
        if Bool then 
            self.Elements["Page"].Instance.Visible = true
            self.Elements["Text"].Instance.TextColor3 = Library.Theme.Accent
            self.Elements["Text"].Instance.TextTransparency = 0
            self.Elements["Hide"].Instance.Visible = true
        else
            self.Elements["Page"].Instance.Visible = false
            self.Elements["Text"].Instance.TextColor3 = Library.Theme.Text
            self.Elements["Text"].Instance.TextTransparency = 0.5
            self.Elements["Hide"].Instance.Visible = false
        end
    end
end

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local flags = {}

local dumpsterIsRunning = false
local dumpsterTask = nil
local constructionRunning = false
local constructionThread = nil
local StudioMoneyNotifyEnabled = false
local StudioMoneyNotifyConnections = {}
local AutoStealLootbags = false
local AutoPickupBags = false
local AutoCleanMoney = false
local AutoGrabMoney = false
local promptConnections = {}
local promptAddedConnection = nil

local function sendNotification(title, text)
    pcall(function() game:GetService("StarterGui"):SetCore("SendNotification", { Title = title, Text = text, Duration = 5 }) end)
end
local function notifyToggle(name, enabled) sendNotification(name, name .. (enabled and " enabled" or " disabled")) end



local function SafeTeleportTo(targetCFrame)
    Config:Teleport(targetCFrame)
end

local function startDumpsterAutofarm()
    if dumpsterTask then return end
    dumpsterIsRunning = true
    dumpsterTask = task.spawn(function()
        while dumpsterIsRunning do
            local dumpsters = {}
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Part") and v.Name == "DumpsterPromt" then
                    local prompt = v:FindFirstChildWhichIsA("ProximityPrompt")
                    if prompt then table.insert(dumpsters, { part = v, prompt = prompt }) end
                end
            end
            for _, entry in ipairs(dumpsters) do
                if not dumpsterIsRunning then continue end
                if entry.part and entry.prompt and entry.part:IsDescendantOf(workspace) then
                    SafeTeleportTo(CFrame.new(entry.part.Position + Vector3.new(0, 3, 0)))
                    task.wait(0.6)
                    pcall(function() fireproximityprompt(entry.prompt) end)
                    task.wait(1)
                end
            end
            task.wait(2)
        end
        dumpsterTask = nil
    end)
    fireRemoteForAllBackpackItems()
end

local function stopDumpsterAutofarm()
    dumpsterIsRunning = false
end

local function interactPrompt(prompt)
    if fireproximityprompt then fireproximityprompt(prompt)
    else prompt:InputHoldBegin(); prompt:InputHoldEnd() end
end

local function startConstructionAutofarm()
    if constructionThread then return end
    constructionRunning = true
    constructionThread = task.spawn(function()
        while constructionRunning do
            task.wait(1)
            pcall(function()
                if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then return end
                if not LocalPlayer.Character:FindFirstChild("Humanoid") or LocalPlayer.Character:FindFirstChild("Humanoid").Health == 0 then return end

                if not LocalPlayer:GetAttribute("WorkingJob") then
                    Config:Teleport(CFrame.new(-1729, 371, -1171))
                    task.wait(0.4)
                    fireproximityprompt(workspace.ConstructionStuff["Start Job"].Prompt)
                    repeat task.wait() until LocalPlayer:GetAttribute("WorkingJob") or not constructionRunning
                end

                if not LocalPlayer.Backpack:FindFirstChild("PlyWood") and not LocalPlayer.Character:FindFirstChild("PlyWood") then
                    Config:Teleport(CFrame.new(-1728, 371, -1178))
                    repeat task.wait() fireproximityprompt(workspace.ConstructionStuff["Grab Wood"].Prompt) until LocalPlayer.Backpack:FindFirstChild("PlyWood") or LocalPlayer.Character:FindFirstChild("PlyWood") or not constructionRunning
                end

                repeat task.wait() until LocalPlayer.Backpack:FindFirstChild("PlyWood") or LocalPlayer.Character:FindFirstChild("PlyWood") or not constructionRunning

                if LocalPlayer.Backpack:FindFirstChild("PlyWood") then
                    LocalPlayer.Character.Humanoid:EquipTool(LocalPlayer.Backpack:FindFirstChild("PlyWood"))
                end

                local targetWood = nil
                for _, v in pairs(workspace.ConstructionStuff:GetDescendants()) do
                    if v:IsA("ProximityPrompt") and v.ActionText == "Wall" then
                        targetWood = v
                        break
                    end
                end

                if targetWood and targetWood.Parent then
                    Config:Teleport(targetWood.Parent.CFrame + Vector3.new(0, 3, 0))
                    task.wait(0.4)
                    fireproximityprompt(targetWood)
                    task.wait(1)
                end
            end)
        end
        constructionThread = nil
    end)
end

local function stopConstructionAutofarm()
    constructionRunning = false
    if constructionThread then task.cancel(constructionThread); constructionThread = nil end
end

local function refreshPrompts(enableInstant)
    for _, prompt in ipairs(workspace:GetDescendants()) do
        if prompt:IsA("ProximityPrompt") then setupPrompt(prompt, enableInstant) end
    end
    if promptAddedConnection then promptAddedConnection:Disconnect(); promptAddedConnection = nil end
    if enableInstant then
        promptAddedConnection = workspace.DescendantAdded:Connect(function(descendant)
            if descendant:IsA("ProximityPrompt") then setupPrompt(descendant, true) end
        end)
    end
end

local function watchStudioPayPrompt(studioPayName)
    local studioPay = workspace:FindFirstChild("StudioPay")
    if not studioPay then return end
    local moneyFolder = studioPay:FindFirstChild("Money")
    if not moneyFolder then return end
    local part = moneyFolder:FindFirstChild(studioPayName)
    if not part then return end
    local prompt = part:FindFirstChild("Prompt")
    if not prompt or not prompt:IsA("ProximityPrompt") then return end
    local conn = prompt:GetPropertyChangedSignal("Enabled"):Connect(function()
        if not StudioMoneyNotifyEnabled then return end
        if prompt.Enabled then sendNotification("Studio Money Restocked", "Money in studio restocked.")
        else sendNotification("Studio Money Stolen", "1 money in studio just got stolen!") end
    end)
    table.insert(StudioMoneyNotifyConnections, conn)
end

local function cleanupStudioMoneyNotify()
    for _, conn in ipairs(StudioMoneyNotifyConnections) do if conn and conn.Disconnect then conn:Disconnect() end end
    StudioMoneyNotifyConnections = {}
end

local function findHouseModels()
    local found = {}
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("Model") or v:IsA("Folder") then
            local n = v.Name:lower()
            if n:find("house") and not n:find("pent") and not n:find("ware") then
                if v:FindFirstChildWhichIsA("ProximityPrompt", true) then table.insert(found, v) end
            end
        end
    end
    table.sort(found, function(a, b) return a.Name:lower() < b.Name:lower() end)
    return found
end

local function lootHouse(houseFolder)
    if not houseFolder then return end
    for _, item in pairs(houseFolder:GetDescendants()) do
        if item:IsA("ProximityPrompt") and item.Enabled then
            local part = item.Parent
            if part and part:IsA("BasePart") then
                Config:Teleport(part.CFrame + Vector3.new(0, 3, 0)); task.wait(0.2)
                pcall(function() fireproximityprompt(item) end); task.wait(0.25)
            end
        end
    end
end

local function GetPlayerMoneyAmount()
    local pl = Players.LocalPlayer
    if not pl then return nil end
    local leader = pl:FindFirstChild("leaderstats") or pl:FindFirstChild("Leaderstats") or pl:FindFirstChild("stats")
    if leader then
        for _, name in ipairs({"Money","Cash","Bank","Coins","Wallet"}) do
            local val = leader:FindFirstChild(name)
            if val and type(val.Value) == "number" then return val.Value end
        end
    end
    if pl.GetAttribute then
        local a = pl:GetAttribute("Money") or pl:GetAttribute("Cash") or pl:GetAttribute("Coins")
        if a and tonumber(a) then return tonumber(a) end
    end
    return nil
end

local function autoDepositAmount(amount)
    pcall(function()
        local BankAction = game:GetService("ReplicatedStorage"):FindFirstChild("BankAction")
        if not BankAction then return end
        if amount and tonumber(amount) and tonumber(amount) > 0 then
            BankAction:FireServer("depo", tostring(math.floor(amount)))
        else
            BankAction:FireServer("depo", tostring(999999999))
        end
    end)
end

task.spawn(function()
    while true do
        if getgenv().StudioPayFarm then
            local StudioPayFolder = workspace:FindFirstChild("StudioPay") and workspace.StudioPay:FindFirstChild("Money")
            if StudioPayFolder then
                local enabledPrompts = {}
                for _, studioPayModel in pairs(StudioPayFolder:GetChildren()) do
                    for _, part in pairs(studioPayModel:GetChildren()) do
                        if part:IsA("BasePart") then
                            local prompt = part:FindFirstChild("Prompt")
                            if prompt and prompt:IsA("ProximityPrompt") and prompt.Enabled then
                                table.insert(enabledPrompts, {part = part, prompt = prompt})
                            end
                        end
                    end
                end
                if #enabledPrompts > 0 then
                    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                    local root = character:FindFirstChild("HumanoidRootPart")
                    if root then
                        local originalPos = root.Position
                        for _, entry in ipairs(enabledPrompts) do
                            SafeTeleportTo(CFrame.new(entry.part.Position + Vector3.new(0, 3, 0)))
                            task.wait(0.2)
                            interactPrompt(entry.prompt)
                            task.wait(0.1)
                        end
                        SafeTeleportTo(CFrame.new(originalPos))
                    else
                        task.wait(3)
                    end
                else
                    task.wait(1)
                end
            else
                task.wait(1)
            end
        else
            task.wait(1)
        end
    end
end)


local AutoStealLootbagsLoop = task.spawn(function()
    while task.wait(1) do
        if AutoStealLootbags then
            local storage = workspace:FindFirstChild("Storage")
            if not storage then continue end
            for _, item in ipairs(storage:GetChildren()) do
                if not AutoStealLootbags then continue end
                local prompt = item:FindFirstChild("stealprompt", true)
                if prompt and prompt:IsA("ProximityPrompt") and prompt.Enabled then
                    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                    local root = character:FindFirstChild("HumanoidRootPart")
                    if not root then continue end
                    local oldCFrame = root.CFrame
                    getgenv().SwimMethod = true; task.wait(0.3)
                    local targetPart = prompt.Parent:IsA("BasePart") and prompt.Parent or item:FindFirstChildWhichIsA("BasePart", true)
                    if not targetPart then continue end
                    root.CFrame = targetPart.CFrame + Vector3.new(0, 2, 0); task.wait(0.3)
                    fireproximityprompt(prompt); task.wait(0.3)
                    root.CFrame = oldCFrame; task.wait(0.5)
                    getgenv().SwimMethod = false
                end
            end
        end
    end
end)


-- ========== LOOT / HEIST LOOPS ==========
getgenv().AutoLootStudio = false
task.spawn(function()
    while task.wait(0.5) do
        if getgenv().AutoLootStudio then
            local StudioPayFolder = workspace:FindFirstChild("StudioPay") and workspace.StudioPay:FindFirstChild("Money")
            if StudioPayFolder then
                for _, model in pairs(StudioPayFolder:GetChildren()) do
                    for _, part in pairs(model:GetChildren()) do
                        if part:IsA("BasePart") then
                            local prompt = part:FindFirstChild("Prompt")
                            if prompt and prompt:IsA("ProximityPrompt") and prompt.Enabled then
                                Config:Teleport(part.CFrame + Vector3.new(0, 3, 0)); task.wait(0.2)
                                pcall(function() fireproximityprompt(prompt) end); task.wait(0.1)
                            end
                        end
                    end
                end
            end
        end
    end
end)

getgenv().AutoLootHouse1 = false
getgenv().AutoLootHouse2 = false
local function findHouseModels()
    local found = {}
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("Model") or v:IsA("Folder") then
            local n = v.Name:lower()
            if n:find("house") and not n:find("pent") and not n:find("ware") then
                if v:FindFirstChildWhichIsA("ProximityPrompt", true) then table.insert(found, v) end
            end
        end
    end
    table.sort(found, function(a, b) return a.Name:lower() < b.Name:lower() end)
    return found
end
local function lootHouse(houseFolder)
    if not houseFolder then return end
    for _, item in pairs(houseFolder:GetDescendants()) do
        if item:IsA("ProximityPrompt") and item.Enabled then
            local part = item.Parent
            if part and part:IsA("BasePart") then
                Config:Teleport(part.CFrame + Vector3.new(0, 3, 0)); task.wait(0.2)
                pcall(function() fireproximityprompt(item) end); task.wait(0.25)
            end
        end
    end
end
task.spawn(function()
    while task.wait(1) do
        if getgenv().AutoLootHouse1 or getgenv().AutoLootHouse2 then
            local houses = findHouseModels()
            if getgenv().AutoLootHouse1 and houses[1] then pcall(function() lootHouse(houses[1]) end) end
            if getgenv().AutoLootHouse2 and houses[2] then pcall(function() lootHouse(houses[2]) end) end
        end
    end
end)

getgenv().AutoLootTrash = false
task.spawn(function()
    while task.wait(0.5) do
        if getgenv().AutoLootTrash then
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Part") and (v.Name:lower():find("trash") or v.Name:lower():find("dumpster")) then
                    local prompt = v:FindFirstChildWhichIsA("ProximityPrompt", true)
                    if prompt and prompt.Enabled then
                        Config:Teleport(v.CFrame + Vector3.new(0, 3, 0)); task.wait(0.3)
                        pcall(function() fireproximityprompt(prompt) end); task.wait(0.5)
                    end
                end
            end
        end
    end
end)

getgenv().AutoSellLoot = false
task.spawn(function()
    while task.wait(1) do
        if getgenv().AutoSellLoot then
            pcall(function()
                local pawnRemote = ReplicatedStorage:FindFirstChild("PawnRemote")
                if pawnRemote then for _, item in pairs(LocalPlayer.Backpack:GetChildren()) do pawnRemote:FireServer(item.Name) end end
            end)
        end
    end
end)

getgenv().AutoRobBank = false
task.spawn(function()
    while task.wait(0.5) do
        if getgenv().AutoRobBank then
            pcall(function()
                for _, v in pairs(workspace:GetDescendants()) do
                    if v:IsA("ProximityPrompt") and v.Enabled and (v.ActionText:lower():find("rob") or v.ActionText:lower():find("crack") or v.ActionText:lower():find("drill")) then
                        Config:Teleport(v.Parent.CFrame + Vector3.new(0, 3, 0)); task.wait(0.2)
                        fireproximityprompt(v); task.wait(0.3)
                    end
                end
            end)
        end
    end
end)

getgenv().AutoRobStore = false
task.spawn(function()
    while task.wait(0.5) do
        if getgenv().AutoRobStore then
            pcall(function()
                local selectedStore = flags["TargetStoreSelect"] or "Store1"
                local storeModel = workspace:FindFirstChild(selectedStore) or (workspace:FindFirstChild("Stores") and workspace.Stores:FindFirstChild(selectedStore))
                if storeModel then
                    for _, v in pairs(storeModel:GetDescendants()) do
                        if v:IsA("ProximityPrompt") and v.Enabled and (v.ActionText:lower():find("rob") or v.ActionText:lower():find("steal") or v.ActionText:lower():find("register")) then
                            Config:Teleport(v.Parent.CFrame + Vector3.new(0, 3, 0)); task.wait(0.2)
                            fireproximityprompt(v); task.wait(0.3)
                        end
                    end
                end
            end)
        end
    end
end)

-- ========== AUTO GRAB DROPPED CASH ==========
task.spawn(function()
    while task.wait(1) do
        if AutoGrabMoney then
            local dollas = workspace:FindFirstChild("Dollas")
            if not dollas then continue end
            for _, item in ipairs(dollas:GetChildren()) do
                if not AutoGrabMoney then continue end
                if (item.Name == "DeadMoney" or item.Name == "Money") and item:IsA("BasePart") then
                    local prompt = item:FindFirstChildWhichIsA("ProximityPrompt", true)
                    if prompt and prompt:IsA("ProximityPrompt") and prompt.Enabled then
                        local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                        local root = character:FindFirstChild("HumanoidRootPart")
                        if not root then continue end
                        local oldCFrame = root.CFrame
                        getgenv().SwimMethod = true; task.wait(0.3)
                        root.CFrame = item.CFrame + Vector3.new(0, 2, 0); task.wait(0.3)
                        fireproximityprompt(prompt); task.wait(0.3)
                        if AutoCleanMoney then local amt = GetPlayerMoneyAmount(); autoDepositAmount(amt) end
                        root.CFrame = oldCFrame; task.wait(0.5)
                        getgenv().SwimMethod = false
                    end
                end
            end
        end
    end
end)






local WeaponMods = { HitboxSize = 1, InfiniteAmmo = false, InfiniteClips = false, Bullets80k = false, ModifyRecoilValue = false, ModifySpreadValue = false, DisableJamming = false, InstantReload = false, InstantEquip = false, ModifyFireRate = false, Automatic = false, DamageAmplified = false, SpoofAccuracy = false, HitboxExpander = false, HitboxVisible = false, Hitmarkers = false, ReloadSpeed = 0.2, EquipSpeed = 0.2 }
local GunOriginalColors = {}
local GunChams = false
local RainbowGun = false

local function getEquippedGunSetting()
    local character = LocalPlayer.Character
    if not character then return nil, nil end
    local tool = character:FindFirstChildOfClass("Tool")
    if not tool or not tool:FindFirstChild("Setting") then return nil, nil end
    local ok, settings = pcall(require, tool.Setting)
    if ok and type(settings) == "table" then return tool, settings end
    return nil, nil
end

local function applyAllWeaponMods()
    local tool, settings = getEquippedGunSetting()
    if not settings then return end
    pcall(function()
        if WeaponMods.InfiniteAmmo then settings.Ammo = 99999; settings.AmmoPerMag = 99999; settings.LimitedAmmoEnabled = false end
        if WeaponMods.InfiniteClips then settings.Ammo = 80000; settings.AmmoPerMag = 80000; settings.LimitedAmmoEnabled = false end
        if WeaponMods.Bullets80k then
            settings.Ammo = 80000; settings.AmmoPerMag = 80000; settings.LimitedAmmoEnabled = false
            local gunLocal = tool:FindFirstChild("GunScript_Local")
            if gunLocal and type(getsenv) == "function" then
                pcall(function()
                    local env = getsenv(gunLocal)
                    if env and type(env.Reload) == "function" and type(debug.setupvalue) == "function" then
                        debug.setupvalue(env.Reload, 1, 80000); debug.setupvalue(env.Reload, 3, 80000)
                    end
                end)
            end
        end
        if WeaponMods.ModifyRecoilValue then settings.Recoil = 0; settings.CameraRecoilingEnabled = false end
        if WeaponMods.ModifySpreadValue then settings.Spread = 0; settings.SpreadX = 0; settings.SpreadY = 0; settings.Accuracy = 1 end
        if WeaponMods.DisableJamming then settings.JamChance = 0 end
        if WeaponMods.InstantReload then settings.ReloadTime = WeaponMods.ReloadSpeed or 0.1 end
        if WeaponMods.InstantEquip then settings.EquipTime = WeaponMods.EquipSpeed or 0.1; settings.EquippingTime = WeaponMods.EquipSpeed or 0.1 end
        if WeaponMods.ModifyFireRate then settings.FireRate = 0 end
        if WeaponMods.Automatic then settings.Auto = true end
        if WeaponMods.DamageAmplified then settings.BaseDamage = 10000 end
        if WeaponMods.SpoofAccuracy then settings.Accuracy = 1 end
    end)
end

LocalPlayer.CharacterAdded:Connect(function(character)
    character.ChildAdded:Connect(function(child)
        if child:IsA("Tool") then task.defer(applyAllWeaponMods) end
    end)
end)
if LocalPlayer.Character then
    LocalPlayer.Character.ChildAdded:Connect(function(child)
        if child:IsA("Tool") then task.defer(applyAllWeaponMods) end
    end)
end

-- ============================================================================
-- PLAYER TAB UI
-- ============================================================================
local GunModsSection = PlayerTab:Section({Name = "Weapon Modifications", Side = 1})
local function bindWeaponToggle(name, key, flag)
    GunModsSection:Toggle({Name = name, Flag = flag, Default = false, Callback = function(state) WeaponMods[key] = state; applyAllWeaponMods() end})
end
bindWeaponToggle("Infinite Ammo", "InfiniteAmmo", "WM_InfiniteAmmo")
bindWeaponToggle("Infinite Mag", "InfiniteClips", "WM_InfiniteClips")
bindWeaponToggle("80k Bullets", "Bullets80k", "WM_Bullets80k")
bindWeaponToggle("No Recoil", "ModifyRecoilValue", "WM_NoRecoil")
bindWeaponToggle("No Spread", "ModifySpreadValue", "WM_NoSpread")
bindWeaponToggle("No Jam", "DisableJamming", "WM_NoJam")
bindWeaponToggle("Instant Reload", "InstantReload", "WM_InstantReload")
bindWeaponToggle("Instant Equip", "InstantEquip", "WM_InstantEquip")
bindWeaponToggle("Full Auto", "Automatic", "WM_FullAuto")
bindWeaponToggle("Fast Fire Rate", "ModifyFireRate", "WM_FastFireRate")
bindWeaponToggle("One Tap Damage", "DamageAmplified", "WM_OneTapDamage")
bindWeaponToggle("Spoof Accuracy", "SpoofAccuracy", "WM_SpoofAccuracy")

local GunModSettings = PlayerTab:Section({Name = "Weapon Settings", Side = 2})
GunModSettings:Slider({Name = "Reload Speed", Flag = "WM_ReloadSpeed", Min = 0.01, Max = 1, Default = 0.2, Callback = function(value) WeaponMods.ReloadSpeed = value; applyAllWeaponMods() end})
GunModSettings:Slider({Name = "Equip Speed", Flag = "WM_EquipSpeed", Min = 0.01, Max = 1, Default = 0.2, Callback = function(value) WeaponMods.EquipSpeed = value; applyAllWeaponMods() end})

-- ============================================================================
-- MAIN TAB UI (VISUAL ONLY)
-- ============================================================================
local DupeSection = MainTab:Section({Name = "Select Dupe Method", Side = 2})
DupeSection:Dropdown({Name = "Select Dupe Method", Flag = "DupeMethod", Items = {"Default", "Safe", "Fast"}, Default = "Safe", Callback = function() end})
DupeSection:Button({Name = "Dupe Tools", Callback = function() end})
DupeSection:Toggle({Name = "Goto Safe", Flag = "GotoSafe", Default = false, Callback = function() end})
DupeSection:Toggle({Name = "Store Duped tool in Safe", Flag = "StoreDupedSafe", Default = true, Callback = function() end})

local RetreatSection = MainTab:Section({Name = "Retreat & Pickups", Side = 2})
RetreatSection:Label({Name = "[Retreat HP]", Alignment = "Left"})
RetreatSection:Label({Name = "[Number]", Alignment = "Left"})
RetreatSection:Toggle({Name = "Auto Retreat", Flag = "AutoRetreat", Default = false, Callback = function() end})
RetreatSection:Toggle({Name = "Anti Knockback", Flag = "AntiKnockback", Default = false, Callback = function() end})
RetreatSection:Toggle({Name = "God Mode [NEW!]", Flag = "GodMode", Default = true, Callback = function() end})
RetreatSection:Toggle({Name = "Auto Pickup Tools", Flag = "AutoPickupTools", Default = false, Callback = function() end})
RetreatSection:Toggle({Name = "Steal tools [NEW!]", Flag = "StealTools", Default = false, Callback = function() end})

local GunCheatsSection = MainTab:Section({Name = "GunCheats", Side = 2})

local QuickOutfitSection = MainTab:Section({Name = "Quick Outfits", Side = 2})
QuickOutfitSection:Button({Name = "Spiderman", Callback = function() end})
QuickOutfitSection:Button({Name = "All Black", Callback = function() end})
QuickOutfitSection:Button({Name = "Blu Moncler Drip", Callback = function() end})
QuickOutfitSection:Button({Name = "Amiri Star Drip", Callback = function() end})
QuickOutfitSection:Button({Name = "Red Sp5der Drip", Callback = function() end})
QuickOutfitSection:Button({Name = "Green Sp5der Drip", Callback = function() end})
QuickOutfitSection:Button({Name = "Blue Sp5der Drip", Callback = function() end})
QuickOutfitSection:Button({Name = "Red Zipped Drip", Callback = function() end})

local TeleportSection = MainTab:Section({Name = "Teleports", Side = 2})
local dummyTeleports = {"Deli Market", "Bank", "Ice Box", "Mini Mansion", "Pent House"}
TeleportSection:Dropdown({Name = "Select Location", Flag = "TeleportLocation", Default = dummyTeleports[1], Items = dummyTeleports, Callback = function() end})
TeleportSection:Button({Name = "Teleport To", Callback = function() end})

local MoneySection = MainTab:Section({Name = "Money", Side = 2})
MoneySection:Button({Name = "Check My Money", Callback = function() end})
MoneySection:Button({Name = "Check Target Money", Callback = function() end})
MoneySection:Button({Name = "Manual [Inf Money]", Callback = function() end})
MoneySection:Button({Name = "Teleport [Cook Pot]", Callback = function() end})
MoneySection:Button({Name = "Buy Ice-Fruit [ITEMS]", Callback = function() end})
MoneySection:Button({Name = "Clean All Filthy Money", Callback = function() end})
MoneySection:Button({Name = "Generate Max Illegal Money", Callback = function() end})
MoneySection:Toggle({Name = "Auto Drop 10k", Flag = "AutoDrop10k", Default = false, Callback = function() end})

-- ============================================================================
-- AUTOFARM TAB UI (FUNCTIONAL)
-- ============================================================================
local AutofarmsSection = FarmingTab:Section({Name = "Autofarms", Side = 1})
local dumpsterRunningFlag = false
AutofarmsSection:Button({Name = "Dumpster Autofarm", Callback = function()
    if not dumpsterRunningFlag then dumpsterRunningFlag = true; startDumpsterAutofarm(); sendNotification("Autofarm", "Dumpster autofarm started.")
    else dumpsterRunningFlag = false; stopDumpsterAutofarm(); sendNotification("Autofarm", "Dumpster autofarm stopped.") end
end})
AutofarmsSection:Button({Name = "Studio Autofarm", Callback = function()
    getgenv().StudioPayFarm = not getgenv().StudioPayFarm
    sendNotification("Autofarm", "Studio autofarm " .. (getgenv().StudioPayFarm and "started." or "stopped."))
end})
local constructionRunningFlag = false
AutofarmsSection:Button({Name = "Construction Autofarm", Callback = function()
    if not constructionRunningFlag then constructionRunningFlag = true; startConstructionAutofarm(); sendNotification("Autofarm", "Construction autofarm started.")
    else constructionRunningFlag = false; stopConstructionAutofarm(); sendNotification("Autofarm", "Construction autofarm stopped.") end
end})

local FarmUtilsSection = FarmingTab:Section({Name = "Farm Utilities", Side = 1})
FarmUtilsSection:Toggle({Name = "Instant Interaction", Flag = "InstantInteraction", Default = false, Callback = function(state) refreshPrompts(state); notifyToggle("Instant Interaction", state) end})
FarmUtilsSection:Toggle({Name = "Studio Money Notify", Flag = "StudioMoneyNotify", Default = false, Callback = function(state)
    StudioMoneyNotifyEnabled = state; cleanupStudioMoneyNotify()
    if state then
        for _, name in ipairs({"StudioPay1", "StudioPay2", "StudioPay3"}) do watchStudioPayPrompt(name) end
        local moneyFolder = workspace:FindFirstChild("StudioPay") and workspace.StudioPay:FindFirstChild("Money")
        if moneyFolder then
            local conn = moneyFolder.ChildAdded:Connect(function(child)
                if not StudioMoneyNotifyEnabled then return end
                if child.Name == "StudioPay1" or child.Name == "StudioPay2" or child.Name == "StudioPay3" then watchStudioPayPrompt(child.Name) end
            end)
            table.insert(StudioMoneyNotifyConnections, conn)
        end
    end
    notifyToggle("Studio Money Notify", state)
end})
FarmUtilsSection:Toggle({Name = "Auto Steal Lootbags", Flag = "AutoStealLootbags", Default = false, Callback = function(state) AutoStealLootbags = state end})
FarmUtilsSection:Toggle({Name = "Auto Pickup Bags", Flag = "AutoPickupBags", Default = false, Callback = function(state) AutoPickupBags = state end})
FarmUtilsSection:Toggle({Name = "Auto Deposit Picked Cash", Flag = "AutoDepositPickedCash", Default = false, Callback = function(state) AutoCleanMoney = state end})
FarmUtilsSection:Toggle({Name = "Auto Steal Dropped Cash", Flag = "AutoStealDroppedCash", Default = false, Callback = function(state) AutoGrabMoney = state end})

local LootSection = FarmingTab:Section({Name = "Auto Loot", Side = 2})
LootSection:Toggle({Name = "Auto Loot Studio", Flag = "AutoLootStudio", Default = false, Callback = function(state) getgenv().AutoLootStudio = state; notifyToggle("Auto Loot Studio", state) end})
LootSection:Toggle({Name = "Auto Loot House [1]", Flag = "AutoLootHouse1", Default = false, Callback = function(state) getgenv().AutoLootHouse1 = state; notifyToggle("Auto Loot House 1", state) end})
LootSection:Toggle({Name = "Auto Loot House [2]", Flag = "AutoLootHouse2", Default = false, Callback = function(state) getgenv().AutoLootHouse2 = state; notifyToggle("Auto Loot House 2", state) end})
LootSection:Toggle({Name = "Auto Loot Trash", Flag = "AutoLootTrash", Default = false, Callback = function(state) getgenv().AutoLootTrash = state; notifyToggle("Auto Loot Trash", state) end})
LootSection:Toggle({Name = "Auto Sell Loot", Flag = "AutoSellLoot", Default = false, Callback = function(state) getgenv().AutoSellLoot = state; notifyToggle("Auto Sell Loot", state) end})

local HeistSection = FarmingTab:Section({Name = "Heist Tools", Side = 2})
HeistSection:Dropdown({Name = "Heist Tool", Flag = "HeistToolSelect", Default = "C4", Items = {"C4", "Drill", "Thermite", "Crowbar"}, Callback = function(v) flags["HeistToolSelect"] = v end})
HeistSection:Toggle({Name = "Auto Rob Bank", Flag = "AutoRobBank", Default = false, Callback = function(state) getgenv().AutoRobBank = state; notifyToggle("Auto Rob Bank", state) end})
HeistSection:Dropdown({Name = "Target Store", Flag = "TargetStoreSelect", Default = "Store1", Items = {"Store1", "Store2", "Store3", "DollarGeneral", "McDonalds"}, Callback = function(v) flags["TargetStoreSelect"] = v end})
HeistSection:Toggle({Name = "Auto Rob Store", Flag = "AutoRobStore", Default = false, Callback = function(state) getgenv().AutoRobStore = state; notifyToggle("Auto Rob Store", state) end})
HeistSection:Button({Name = "Auto Grind Construction", Callback = function()
    if not constructionRunningFlag then constructionRunningFlag = true; startConstructionAutofarm(); sendNotification("Construction", "Auto grind construction started.")
    else constructionRunningFlag = false; stopConstructionAutofarm(); sendNotification("Construction", "Auto grind construction stopped.") end
end})



-- ============================================================================
-- MOBILE SUPPORT / CLOSE BUTTON
-- ============================================================================
local mobileGui = Instance.new("ScreenGui")
mobileGui.Name = "MianiFootballMobileToggleMenu"
mobileGui.ResetOnSpawn = false
mobileGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
mobileGui.DisplayOrder = 99999

pcall(function()
    local success = pcall(function() local _ = CoreGui.Name end)
    if success then
        mobileGui.Parent = CoreGui
    else
        mobileGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
    end
end)

local toggleButton = Instance.new("ImageButton")
toggleButton.Size = UDim2.new(0, 68, 0, 68)
toggleButton.Position = UDim2.new(0, 12, 0.5, -34)
toggleButton.Image = "rbxassetid://109948061273706"
toggleButton.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
toggleButton.BackgroundTransparency = 0.15
toggleButton.ScaleType = Enum.ScaleType.Stretch
toggleButton.Active = true
toggleButton.Parent = mobileGui
local corner = Instance.new("UICorner"); corner.CornerRadius = UDim.new(0, 18); corner.Parent = toggleButton
local stroke = Instance.new("UIStroke"); stroke.Color = Color3.fromRGB(255, 0, 0); stroke.Thickness = 3; stroke.Parent = toggleButton
local textBadge = Instance.new("TextLabel")
textBadge.Size = UDim2.new(1, 20, 0, 20); textBadge.Position = UDim2.new(0, -10, 1, 4)
textBadge.BackgroundColor3 = Color3.fromRGB(18, 18, 22); textBadge.Text = "CLOSE"
textBadge.TextColor3 = Color3.fromRGB(255, 255, 255); textBadge.Font = Enum.Font.GothamBold; textBadge.TextSize = 11
textBadge.BorderSizePixel = 0; textBadge.Parent = toggleButton
Instance.new("UICorner", textBadge).CornerRadius = UDim.new(0, 6)
local badgeStroke = Instance.new("UIStroke", textBadge); badgeStroke.Color = Color3.fromRGB(255, 0, 0); badgeStroke.Thickness = 1.5

local uiVisible = true
Library.MenuKeybind = "None"
local function toggleUI()
    uiVisible = not uiVisible
    pcall(function()
        if Window and Window.Elements and Window.Elements["MainFrame"] then
            Window.Elements["MainFrame"].Instance.Visible = uiVisible
        end
    end)
    textBadge.Text = uiVisible and "CLOSE" or "MENU"
    textBadge.BackgroundColor3 = uiVisible and Color3.fromRGB(18, 18, 22) or Color3.fromRGB(150, 20, 20)
end
toggleButton.MouseButton1Click:Connect(toggleUI)

local dragging, dragStart, startPos
toggleButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = input.Position; startPos = toggleButton.Position
        input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
    end
end)
game:GetService("UserInputService").InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        toggleButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
game:GetService("UserInputService").InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.RightShift or input.KeyCode == Enum.KeyCode.RightControl or input.KeyCode == Enum.KeyCode.Insert then toggleUI() end
end)


Library:Init()

task.spawn(function()
    for i = 1, 15 do
        task.wait(0.5)
        pcall(function()
            if Window and Window.Elements and Window.Elements["Title"] then
                Window.Elements["Title"].Instance.RichText = true
            end
            
            local uiScreen = nil
            local success = pcall(function() local _ = CoreGui.Name end)
            if success then
                uiScreen = CoreGui:FindFirstChild("Thugsense")
            else
                local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
                if playerGui then uiScreen = playerGui:FindFirstChild("Thugsense") end
            end
            
            if uiScreen then
                local uiScale = uiScreen:FindFirstChildWhichIsA("UIScale", true)
                if uiScale and uiScale.Scale < 1 then uiScale.Scale = 1 end
                
                for _, v in pairs(uiScreen:GetDescendants()) do
                    if v:IsA("TextLabel") and string.find(v.Text, "miani football") then
                        v.RichText = true
                    end
                end
            end
        end)
    end
end)
